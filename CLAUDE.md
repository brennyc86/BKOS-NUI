# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project: BKOS-NUI (Boordcomputer Besturingssysteem)

Een ESP32-S3 gebaseerde boordcomputer voor schepen met een 7-inch touchscreen (800×480 RGB panel), UART-communicatie naar een ATtiny3217 bijkaart voor IO-aansturing, WiFi, OTA-updates via GitHub en een modulair schermensysteem.

Actieve GitHub repo: `https://github.com/brennyc86/BKOS-NUI`
Referentie (volledige werkende versie): `https://github.com/BrendanKoster86/BKOS4`
Referentie (stripped base): `https://github.com/BrendanKoster86/BaseKOS`

---

## ⚠️ "Een app voor BKOS" = altijd een Lua-app in het bestaande App Systeem

Als Brendan vraagt om **"een app te maken/bouwen voor BKOS"** (of vergelijkbaar:
"app-idee", "app hiervoor"), bedoelt hij ALTIJD een **Lua-app** binnen het
al bestaande, uitgebreide App Systeem van dit project — nooit een nieuwe
firmware-module, geen losse mobiele/webapp, geen nieuw scherm hardcoded in
C++. Vraag dit niet na — ga er meteen van uit en verwijs zo nodig naar:

- **Volledige, actueel gehouden spec (manifest-formaat, complete Lua BKOS-API,
  callbacks, schalen, publiceren naar de app store, en een aparte
  "API-referentie voor AI-systemen"-sectie — precies voor dit doel)**:
  **`docs/BKOS_APP_HANDLEIDING.md`**. Dit IS de bron van waarheid voor app-
  ontwikkeling; lees dit bestand eerst bij elk verzoek om een app.
- Beknopte architectuur/overzicht staat ook in sectie [App Systeem](#app-systeem-sessie-13)
  verderop in dit bestand, maar bij twijfel of een detail ontbreekt: de handleiding wint.
- **Waar apps leven**: `/apps/<id>/manifest.json` + `/apps/<id>/main.lua` op
  het apparaat zelf (SPIFFS/LittleFS); voor de publieke app store de map
  `appstore/` in deze repo-root.
- **Runtime-code**: `lua_runtime.cpp/h` (Lua 5.4 + BKOS-API-bindings),
  `app_manager.cpp/h` (installeren/beheren/manifest-parsing), `screen_apps.ino`
  (APPS-scherm: lijst, installeren, instellingen), `libraries/LuaBKOS/`
  (sketch-local Lua-library, vereist `--library BKOS_NUI/libraries/LuaBKOS`
  bij arduino-cli anders is `LUA_BESCHIKBAAR=0`).
- Belangrijk: dit is bewust een apart ontwikkeltraject van de BKOS-firmware
  zelf — een Lua-app updaten/toevoegen betekent GEEN firmware-versiebump en
  kan (en moet, zie Brendans eigen wens) in een aparte sessie/gesprek
  gebeuren, los van dit BKOS-NUI-firmwarewerk.

---

## Werkwijze

Na elk afgerond stuk werk: altijd committen en pushen naar `main`. GitHub Actions compileert dan automatisch de firmware.bin zodat Brendan het kan bekijken en OTA ophalen.

```bash
git add <gewijzigde bestanden>
git commit -m "vX: korte omschrijving"
git push
```

Versienummer verhogen in `ota.h` (`BKOS_NUI_VERSIE`) bij elke push.

**NOOIT** `firmware/versie_*.txt` handmatig aanpassen — die worden uitsluitend bijgewerkt door GitHub Actions na een succesvolle compilatie. Handmatig schrijven zorgt dat het apparaat een update ziet die nog niet gecompileerd is.

### Versienummer — twee vormen (kanaaldetectie via aantal punten)

De firmware kiest zijn OTA-kanaal op het **aantal punten** in het versienummer. Dit is
hard in de code (`ota_setup()` telt de punten); het is geen losse afspraak.

| Vorm | Formaat | Punten | Voorbeeld | Kanaal |
|---|---|---|---|---|
| **Werkversie (beta)** | `MAJOR.MINOR.JJMMDD.I` | 3 | `0.1.260606.1` | beta |
| **Stabiele release** | `MAJOR.MINOR.PATCH` | 2 | `0.1.6` | stabiel |

- `MAJOR.MINOR` = release-niveau (nu `0.1`).
- `JJMMDD` = bouwdatum (bijv. `260606` = 6 juni 2026).
- `I` = iteratienummer op die dag, begint bij 1.
- `PATCH` = patch binnen de serie, begint bij `1` (let op: niet semver-`0`).

**Dagelijks ophogen (beta):** bij elke firmware-push alleen `BKOS_NUI_VERSIE` in `ota.h`
verhogen — zelfde dag `I+1`, nieuwe dag nieuwe datum met `I=1`. CI schrijft daarna
`firmware/versie_*.txt`. Niet-firmware pushes (docs, installer) krijgen **geen** bump
(die triggeren de CI niet, dus er is geen nieuwe firmware).

**Stabiel verklaren** is een aparte, bewuste handeling — zie *OTA via GitHub*. Nooit
autonoom: alleen op expliciete opdracht van Brendan, en altijd code-identiek aan een
door hem geteste beta.

### Appstore-apps hebben hun EIGEN versienummer — nooit BKOS_NUI_VERSIE in het commitbericht

Een Lua-app in `appstore/apps/<id>/` (zie *App Systeem*) is een los, onafhankelijk
gebruikt versienummer, ook al leeft de code in dezelfde repo. Een commit die
uitsluitend een app raakt (main.lua/manifest.json/index.json, geen `BKOS_NUI/**`)
gebruikt in het commitbericht het **app-versienummer** (`manifest.json`'s `versie`,
`MAJOR.MINOR.PATCH`), nooit `BKOS_NUI_VERSIE` — die twee staan volledig los van
elkaar, net zoals BKOS-Blanco of de installer hun eigen versienummer hebben.
Voorbeeld: `fotolijst v1.2.0: besturingsbalk + instelbaar interval`, niet
`v0.2.260916.4: ...`.

Raakt een commit ZOWEL firmware (`BKOS_NUI/**`, bv. een nieuwe `bkos.*`-API) ALS een
app in dezelfde push, noem dan beide versienummers expliciet en apart (bv.
`v0.2.260916.3 + fotolijst v1.1.0: ...`) — nooit alleen de firmwareversie voor een
wijziging die net zo goed (of vooral) de app raakt.

---

## Compileren & Uploaden

### Board & Toolchain
- **Arduino IDE** met **ESP32 Arduino Core versie 2.0.17** voor ALLE platforms (incl. S3).
  - Core 3.x werd kort gebruikt voor de bounce buffer, maar brak WiFi/netwerk op de S3
    (meteo update niet, OTA "GitHub fout -1"). Daarom terug naar core 2.0.17 (Sessie 30).
  - De code is **dual-compatibel** via `#if ESP_ARDUINO_VERSION_MAJOR >= 3` guards (o.a.
    de RGB-panel-constructor in `hw_scherm.ino`), zodat core 3.x later weer kan zonder code-surgery.
- Board: `ESP32S3 Dev Module` (of ESP32-8048S070C profiel)
- Partition scheme: **eigen 16MB-schema** (`BKOS_NUI/partitions_s3_16mb.csv`) — zie *Partitieschema S3 (16MB)* hieronder. FQBN-optie `PartitionScheme=default_8MB` staat nog in `build.yml`/de boardinstellingen maar is functioneel genegeerd zodra dit bestand als `partitions.csv` in de sketchmap staat.
- Upload speed: 921600
- Flash mode: **DIO** (S3 op core 2.x; QIO gaf instabiliteit bij sommige S3-modules)

### Verplichte bibliotheken
- `Arduino_GFX_Library` **versie 1.3.7** (S3 op core 2.x). De RGB-panel-constructor zonder
  bounce buffer (core 3.x/GFX 1.6.5 met bounce buffer staat achter een version-guard).
- `WiFiManager` 2.0.17
- `ArduinoOTA`
- `HTTPClient` (onderdeel van ESP32 core)
- `Preferences` (onderdeel van ESP32 core)

### Partitieschema S3 (16MB)
De 7" S3-module heeft fysiek 16MB flash, maar de firmware gebruikte tot Sessie 41 het
`default_8MB`-schema (3MB/3MB APP, 1,5MB SPIFFS) — de resterende ~8MB boven de eerste
8MB werd domweg niet aangesproken. Nieuw `BKOS_NUI/partitions_s3_16mb.csv`: identieke
APP-slotgrootte als `default_8MB` (2× 0x330000 = 3264KB, dus **OTA-gedrag/grootte-limiet
ongewijzigd**), maar SPIFFS is opgerekt tot het complete restdeel van de 16MB
(0x980000 = **9728KB**, was 1536KB) + coredump-partitie behouden.

```
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x330000,
app1,     app,  ota_1,   0x340000, 0x330000,
spiffs,   data, spiffs,  0x670000, 0x980000,
coredump, data, coredump,0xFF0000, 0x10000,
```

**Waarom geen boards.txt-aanpassing**: de arduino-esp32 prebuild-hook kopieert automatisch
een bestand met de exacte naam `partitions.csv` uit de sketchmap (`BKOS_NUI/`) naar de
build-map, ongeacht de `PartitionScheme`-FQBN-waarde. Dat werkt zowel lokaal (Arduino IDE
of arduino-cli) als in CI, zonder boards.txt te hoeven wijzigen (niet portable/versioned).

**Waarom niet permanent als `BKOS_NUI/partitions.csv` in de repo**: alle 6 platforms
compileren dezelfde sketchmap. Een permanent aanwezig `partitions.csv` zou ook de andere,
kleinere ESP32-platforms (WROOM/CYD28/CYD40H/CYD40V, 4MB flash) dit 16MB-schema opleggen
en hun build laten falen ("partition table exceeds flash size"). Daarom:
- Bewaard onder de andere naam `BKOS_NUI/partitions_s3_16mb.csv` (git-getrackt, ongevaarlijk).
- **CI** (`build.yml`, job `build_esp32s3`): een losse stap kopieert dit bestand naar
  `BKOS_NUI/partitions.csv` vlak vóór de compile-stap — alleen in die job's (verse) checkout.
- **Lokaal compileren voor S3** (Arduino IDE of losse arduino-cli-run): kopieer zelf eerst
  `BKOS_NUI/partitions_s3_16mb.csv` → `BKOS_NUI/partitions.csv` in de sketchmap. Dit pad
  staat in `.gitignore` — nooit per ongeluk meecommitten (zou dezelfde CI-breuk geven voor
  de andere platforms als iemand toevallig van deze checkout pusht).

### Scherm-stabiliteit (S3 RGB paneel)
De ESP32-S3 RGB-paneel deelt de Octal SPI bus met PSRAM. De **bounce buffer** (LCD-DMA via
intern SRAM) loste tearing/crashes op, maar vereist core 3.x — en core 3.x brak het netwerk.
Op core 2.x is er geen bounce buffer; in plaats daarvan PCLK 10MHz + ruime sync-porches om de
PSRAM-bus contention te beperken (zie de `#else`-tak in `tft_setup()`). Komt de
scherminstabiliteit terug, dan is dat de afweging om opnieuw te bekijken.

### OTA via GitHub — twee kanalen
- **Beta kanaal** (tussenversies, X.Y.YYMMDD.I): `firmware/versie_*.txt` + `firmware/bkos_*.bin`
- **Stabiel kanaal** (officiële releases, X.Y.Z): `firmware/versie_stable_*.txt` + git-tag URL `v{versie}/firmware/bkos_*.bin`
- **Release-index**: `firmware/releases.json` — lijst van alle stabiele releases met tag-URL's per platform
- **Beta-historie**: `firmware/beta_historie.json` — lijst van betaversies; bij promotie krijgt de betaregel het stabiele nummer dat eruit voortkwam (`"gepromoot"`). De link beta→stabiel leeft **alleen hier**.
- Auto-detectie: 2 punten in versienummer = stabiel, 3 punten = beta → toggle in OTA scherm
- OTA controle op configureerbaar interval (`ota_check_interval_min`); `ota_auto_update` staat standaard **uit** (apparaat flasht niet vanzelf — gebruiker drukt zelf op UPDATE)

**Grondregels voor promoten (stabiel verklaren) — Brendan's voorwaarden:**
- **Nooit autonoom.** Promoten gebeurt alleen op expliciete opdracht van Brendan, nadat hij de betaversie zelf getest en goedgekeurd heeft. Liever een bekende bug in de laatste stabiele versie dan een ongeteste promotie.
- **Code-identiek aan een geteste beta.** De stabiele release is bit-voor-bit dezelfde code als de goedgekeurde beta; het *enige* verschil is het versienummer (van `0.1.JJMMDD.I` → `0.1.6`).
- **Zit er in de beta iets dat nog niet uit mag?** Dan eerst een nieuwe beta maken waarin die functionaliteit is uitgeschakeld → Brendan test die → pas die beta promoten. Tijdens het promoten zelf wijzigt nooit functionaliteit.
- **Promoot altijd vanaf de huidige `main`-HEAD** en bevestig met Brendan welke exacte beta + welk doelnummer. Staat er nieuwer (nog niet uit te brengen) werk op `main`, dan geldt de vorige regel.
- **Nooit een lager nummer in `versie_stable_*.txt`** — de OTA-check is "anders dan", niet "nieuwer dan"; een lager nummer downgradet alle stabiele apparaten.
- **Beta op `main` blijft staan.** Promoten verplaatst de beta niet: `BKOS_NUI_VERSIE` en `firmware/versie_*.txt` op `main` blijven het iteratienummer (bv. `0.1.260609.4`). Wie de beta installeert krijgt exact die versie; wie stabiel installeert krijgt `0.1.6`. De stabiele build (versie `0.1.6` ingebakken) leeft **alleen op de git-tag** `v0.1.6`, gebouwd op een aparte release-branch — nooit op `main` (anders zou de CI de beta-binaries overschrijven).

**Werkwijze nieuwe stabiele release (bijv. promoten van `0.1.260609.4` → `0.1.6`) — branch-methode, laat de beta op `main` met rust:**
1. Release-branch van de geteste `main`-HEAD: `git checkout -b release/0.1.6`.
2. Op die branch: `BKOS_NUI_VERSIE` in `ota.h` op `"0.1.6"` zetten **én** in `.github/workflows/build.yml` de push-trigger `branches: [main, 'release/**']` maken (zodat de CI op deze branch bouwt en de `0.1.6`-binaries naar de branch terugcommit i.p.v. naar `main`). Commit + push de branch → **wacht op groene CI (alle 6 platforms)**.
3. **Pas ná de CI-firmware-commits** taggen — anders bevat de tag geen `.bin` en krijgt elk stabiel apparaat **404** (de stabiele URL is `…/v0.1.6/firmware/bkos_*.bin`):
   `git fetch origin release/0.1.6 && git tag -a v0.1.6 origin/release/0.1.6 -m "gepromoot van 0.1.260609.4" && git push origin v0.1.6`.
   Verifieer met `git show v0.1.6:firmware/versie_esp32s3.txt` (= `0.1.6`) en een `curl -I` op de raw tag-URL (HTTP 200). Daarna mag de branch weg: `git push origin --delete release/0.1.6` (de tag behoudt de binaries).
4. Nu op `main` (deze bestanden raken geen `BKOS_NUI/**` of `build.yml`, dus **geen** CI-trigger): `firmware/versie_stable_*.txt` op `0.1.6` voor alle 6 platforms
   (`versie_stable_esp32s3.txt`, `_wroom`, `_cyd28`, `_cyd40h`, `_cyd40v`, `_pico`) en een entry toevoegen aan `firmware/releases.json` (nieuwste boven) met alle platform-URLs:
   ```json
   {"versie":"0.1.6","datum":"JJJJ-MM-DD",
    "url_s3":    "https://raw.githubusercontent.com/brennyc86/BKOS-NUI/v0.1.6/firmware/bkos_esp32s3_8048s070.bin",
    "url_wroom": "https://raw.githubusercontent.com/brennyc86/BKOS-NUI/v0.1.6/firmware/bkos_esp32wroom2432.bin",
    "url_cyd28": "https://raw.githubusercontent.com/brennyc86/BKOS-NUI/v0.1.6/firmware/bkos_esp32cyd28.bin",
    "url_cyd40h":"https://raw.githubusercontent.com/brennyc86/BKOS-NUI/v0.1.6/firmware/bkos_esp32cyd40h.bin",
    "url_cyd40v":"https://raw.githubusercontent.com/brennyc86/BKOS-NUI/v0.1.6/firmware/bkos_esp32cyd40v.bin",
    "url_pico":  "https://raw.githubusercontent.com/brennyc86/BKOS-NUI/v0.1.6/firmware/bkos_pico1w2432.bin"}
   ```
   Laat `url_*` leeg (`""`) voor platforms die niet in deze release zitten.
5. **Beta-historie bijwerken**: in `firmware/beta_historie.json` bij de gepromote beta het stabiele nummer invullen: `{"versie":"0.1.260609.4", … ,"gepromoot":"0.1.6"}`. (Het stabiele overzicht / `releases.json` benoemt de onderliggende beta **niet** — de link leeft alleen aan de betakant.)
6. Push naar `main` → stabiele apparaten zien de update via CONTROLEREN; installer toont 'm automatisch. De beta blijft op het iteratienummer staan.

**De installer (`installer/index.html`) hoeft verder niet bijgewerkt** — hij leest `versie_*.txt` en `releases.json` dynamisch. Hij opent standaard op **"Stabiele release"**; "Laatste build (test)" (beta) blijft kiesbaar maar is niet voorgeselecteerd.

**Pico OTA bestaat niet** — `ota_download_toepassen()` geeft op Pico "n.v.t."; Pico updatet altijd handmatig via UF2/installer. ESP32-S3/WROOM/CYD kunnen wél OTA.

**Stabiele releases** worden getagd met `git tag vX.Y.Z` zodat gebruikers altijd kunnen terugkeren naar een goedgekeurde versie.

### BKOS Blanco — leveringsbasis

BKOS Blanco is een schone basisconfiguratie voor het opleveren van nieuwe boordcomputers (geen kanaalnamen, geen bootnaam, geen WiFi-instellingen). De installer toont BKOS Blanco als keuze naast BKOS-NUI.

**Bestandsstructuur:**
- Beta-versie per platform: `firmware/versie_blanco_{platform}.txt` + `firmware/bkos_blanco_{platform}.bin`
- Stabiele releases: `firmware/blanco_releases.json`

**Werkwijze nieuwe Blanco-release:**
1. Bouw de blanco firmware in Arduino IDE → exporteer per platform als `bkos_blanco_{platform}.bin`
2. Kopieer binaries naar `firmware/` en update de versiebestanden:
   ```
   firmware/versie_blanco_esp32s3.txt   ← versienummer
   firmware/versie_blanco_wroom.txt
   firmware/versie_blanco_cyd28.txt
   firmware/versie_blanco_cyd40h.txt
   firmware/versie_blanco_cyd40v.txt
   firmware/versie_blanco_pico.txt
   ```
3. Voeg entry toe aan `firmware/blanco_releases.json`:
   ```json
   {"versie":"1.0","datum":"YYYY-MM-DD",
    "url_blanco_s3":    "https://raw.githubusercontent.com/brennyc86/BKOS-NUI/vblanco-1.0/firmware/bkos_blanco_esp32s3.bin",
    "url_blanco_wroom": "...",
    "url_blanco_cyd28": "...",
    "url_blanco_cyd40h":"...",
    "url_blanco_cyd40v":"...",
    "url_blanco_pico":  "..."}
   ```
4. Push → installer toont BKOS Blanco automatisch als beschikbaar

**Zolang de versiebestanden leeg zijn** toont de installer "niet beschikbaar voor dit platform" — dat is de standaardstaat totdat Brendan de blanco firmware aanlevert.

---

## Architectuur

### Bestandsstructuur in `BKOS_NUI/`
Elk module bestaat uit een `.h` (declaraties + defines) en `.ino` (implementatie) paar.

```
BKOS_NUI.ino        ← minimale entry point: roept hw_setup() en hw_loop() aan
hardware.h/.ino     ← centrale coördinatie: init volgorde, hoofd loop, touch dispatcher
app_state.h/.ino    ← globale toestand: actief_scherm, vaarmodus, licht, IO-arrays
ui_colors.h         ← RGB565 kleurpalet (donker marine thema)
ui_draw.h/.ino      ← herbruikbare teken-primitieven voor knoppen, labels, bars

hw_scherm.h/.ino    ← TFT display init (Arduino_GFX, RGB panel 800×480, 16MHz pclk)
hw_touch.h/.ino     ← touchscreen init en uitlezen
hw_io.h/.ino        ← UART2 naar ATtiny3217 (9600 baud, RX=16, TX=17), module discovery

io.h/.ino           ← IO logica: cyclus (50ms), kanaal namen, apparaat toggle, licht status
wifi.h/.ino         ← WiFiManager, NTP (CET/CEST), verbindingsstatus
ota.h/.ino          ← GitHub versiecheck, firmware download+flash, ArduinoOTA (push)

nav_bar.h/.ino      ← vaste navigatiebalk onderaan (scherm-knoppen)
screen_main.h/.ino  ← hoofdscherm: bootsschema met lichten, vaarmodus knoppen, verlichting
screen_io.h/.ino    ← IO kanalen lijst (scrollbaar, 8 rijen/pagina, 44px rijhoogte)
screen_meteo.h/.ino ← METEO scherm (3 tabs: WEER / GETIJ / LOCATIE)
screen_config.h/.ino← instellingen scherm
screen_ota.h/.ino   ← OTA update scherm met voortgangsbalk
screen_info.h/.ino  ← device informatie scherm
screen_wifi.h/.ino  ← WiFi configuratie scherm

meteo.h/.ino        ← weer + getij module: locatie (ip-api.com), Open-Meteo API, harmonische getijberekening
getijdata.cpp/h     ← Rijkswaterstaat getijextremen API module (aparte HTTP-opvrager)

provider.h/.ino     ← lichtgewicht achtergrond-scheduler (alleen actief als tft_actief)
victron_ble.h/.ino  ← passieve Victron BLE scan + AES-128-CTR decryptie + Preferences opslag
slaap.h/.ino        ← ESP32 slaapstand: GEEN/LIGHT(~2mA)/DEEP(~10µA), timer+GPIO wake, ATtiny SLP
screen_victron.h/.ino ← Victron scherm: DATA tab + CONFIG tab (discovery + hex-toetsenbord)

bkos_net.h/.ino     ← ESP-NOW multi-device: master/slave rollen, mesh routing, tijdsync (NET_MSG_TIJD), state-sync (NET_MSG_STATE_REQ), achtergrond-router op Core 0
data_store.cpp/h    ← gestructureerde persistente JSON-opslag in SPIFFS (/bkos_data.json), key-value + tijdstempel, door Lua leesbaar via bkos.data.*
fout_log.cpp/h/.ino ← foutlog naar GitHub Issues API (repo: brennyc86/BKOS-NUI-logs)
app_manager.cpp/h   ← Lua app-beheer: installeren/activeren/deactiveren, leest manifest.json + main.lua uit SPIFFS /apps/<id>/
lua_runtime.cpp/h   ← Lua 5.4 runtime met volledige BKOS API bindings (scherm, IO, data, systeem)
platform.h          ← één plek voor alle platform-afhankelijke defines (ESP32-S3, WROOM, CYD28, CYD40H, CYD40V, Pico)
platform_fs.h       ← platform-afhankelijke filesystem-bindings (SPIFFS vs LittleFS per platform)

screen_calibratie.h/.ino ← 5-punts affiene touch-kalibratie, apart opgeslagen per oriëntatie
screen_netwerk.h/.ino    ← Netwerk scherm: verbonden peers, IP/MAC info, SCREEN_SMALL layout, refresh elke 60s
screen_apps.h/.ino       ← Apps scherm: Lua app-lijst, installeren/starten/stoppen vanuit app store of SPIFFS

bkos_brug.h/.ino    ← WiFi-brug via Raspberry Pi Zero 2W: BLE GATT Central, Pi discovery, SSID+wachtwoord overdracht, netwerk-scan, status-notificaties
screen_brug.h/.ino  ← Brug scherm: status-weergave, netwerkkeuze-lijst, wachtwoord-keyboard overlay
```

### Scherm-dispatch patroon
Elk scherm heeft twee functies:
- `screen_X_teken()` — volledig hertekenen (aangeroepen als `herteken == true`)
- `screen_X_run(x, y, aanraking)` — touch verwerken en incrementele updates

`hardware.ino` dispatcht op basis van `actief_scherm` enum (`SCHERM_MAIN`, `SCHERM_IO`, etc.).
Touch debouncing via `touch_verwerkt` flag; eerste touch na display wake wordt genegeerd.

### IO-systeem (UART ↔ ATtiny3217)
- Max 30 modules × 8 kanalen = 240 kanalen
- Module types: `LOGICA8`, `LOGICA16`, `HUB8`, `HUB_AN`, `HUB_UART`, `SCHAKEL8`, `SCHAKEL16`
- Kanaal namen max 12 chars, prefix-gebaseerde herkenning: `L_` (licht), `IL_wit`, `IL_rood`, apparaatnamen
- Cyclus elke 50ms; rediscovery elke 30 seconden
- Uitvoer states: normaal, geïnverteerd, geblokkeerd
- Licht states (voor visuele feedback): `LICHT_UIT`, `LICHT_NAGLOEIT`, `LICHT_GEEN_TERUGKOPPELING`, `LICHT_AAN`

### Persistentie
- `Preferences` library voor opslaan van: WiFi credentials (via WiFiManager), kanaal namen, vaarmodus, verlichting instelling, lokale IO fallback states
- Config bestand WiFiManager: `/bkos_nui.json`

---

## Globale staat (app_state.h)

| Variabele | Type | Betekenis |
|---|---|---|
| `actief_scherm` | enum | Huidig zichtbaar scherm |
| `herteken` | bool | Forceer volledig hertekenen |
| `vaarmodus` | enum | HAVEN / ZEILEN / MOTOR / ANKER |
| `verlichting` | enum | UIT / AAN / AUTO |
| `ota_status` | enum | OTA update toestand |
| `wifi_verbonden` | bool | WiFi actief verbonden |
| `io_kanaal_count` | int | Aantal gevonden IO kanalen |
| `io_uitvoer[]` | array | Uitvoer states per kanaal |
| `io_invoer[]` | array | Invoer feedback per kanaal |
| `io_naam[]` | array | Kanaalnamen (max 12 chars) |

---

## Taakoverzicht

Volledige geschiedenis: zie [docs/TAAKHISTORIE.md](docs/TAAKHISTORIE.md) (sessies 1–22).

Recente taken:

| # | Sessie | Taak |
|---|---|---|
| 128 | Sessie 22 | UI scaling (UI_SCX/UI_SCY) uitgebreid naar alle schermen voor CYD40H compatibiliteit |
| 129 | Sessie 22 | Tijddeling via ESP-NOW: NET_MSG_TIJD broadcast + ntp_vanaf_net() — slave/extra deelt NTP-tijd |
| 130 | Sessie 22 | Staat-herstel bij herstart: NET_MSG_STATE_REQ — master vraagt vaarmodus/verlichting op bij slave na pairing |
| 131 | Sessie 22 | Touch kalibratie CYD40H: TOUCH KAL knop toegevoegd aan cfg_instellingen_teken voor PLATFORM_XPT2046 |
| 132 | Sessie 22 | Lua melding: OPEN-knop toont duidelijke foutmelding als LUA_BESCHIKBAAR=0 (OTA build vereist) |
| 133 | Sessie 23 | Lua altijd mee in firmware + app-netwerk achtergrond-router (Core 0 FreeRTOS taak voor slave→slave routing) |
| 134 | Sessie 23 | 5-punts affiene touch kalibratie, apart per oriëntatie |
| 135 | Sessie 23 | Portret nav bar met iconen en vaste knoppen (240px: 1L+1R, 320px: 2L+2R) |
| 136 | Sessie 23 | Netwerk scherm SCREEN_SMALL layout + klok fix CYD40H + peer info refresh elke 60s |
| 137 | Sessie 23 | LuaBKOS naar sketch-local library (BKOS_NUI/libraries/LuaBKOS/) — Lua werkt nu ook bij lokale Arduino IDE compilatie |
| 138 | Sessie 23 | lua_net_q queue-clearing fix: alleen verwerkte berichten verwijderen, niet de hele queue |
| 139 | Sessie 23 | OTA verbeteringen: betere foutmeldingen, CYD platform support in releases.json, timeout 20s, HTTPC_FORCE_FOLLOW_REDIRECTS |
| 140 | Sessie 24 | GETIJ portret 1-kolom tabel + partial redraw scroll + OTA portret VORIGE VERSIES overlay + oriëntatie-wissel CYD40 |
| 141 | Sessie 24 | PCLK 12MHz + backlight-blanking tijdens hertekenen (PSRAM-bus contention verminderen) |
| 142 | Sessie 24 | hw_scherm: PCLK 10MHz + vsync_back_porch 23 (minder onderkant-flikkering) |
| 143 | Sessie 24 | Boot-lamp posities gecorrigeerd (hek→achtersteven x=4, navi→boeg x=108); sectortekening heklicht 120°, stoomlicht 240°, 3kl wit/rood/groen, nav rood/groen; SCREEN_SMALL support |
| 144 | Sessie 25 | Zeeslag v2.0: flat arrays voor CYD40V, computer-missers zichtbaar (blauw), gezonken schip groot kruis |
| 145 | Sessie 25 | Zeeslag v3.0: grafische schepen (boeg/hek/details), handmatig plaatsen met validatie, ongeldige schepen rood |
| 146 | Sessie 25 | ESP32 slaapstand: GEEN/LIGHT(~2mA)/DEEP(~10µA), timer+GPIO wake, ATtiny SLP, configuratie UI instellingen-tab |
| 147 | Sessie 26 | Compilatiefout opgelost: `max(10UL, (uint32_t)val)` → `max((uint32_t)10, (uint32_t)val)` in app_state.ino — ESP32 core 2.x template type deduction vereist identieke types (`unsigned long` ≠ `uint32_t`) |
| 148 | Sessie 27 | Installerprobeem opgelost: OTA-partitie reset (8KB 0xFF naar 0xE000) voor juiste factory boot bij verse flash |
| 149 | Sessie 27 | Upgrade naar ESP32 core 3.3.8 + Arduino_GFX 1.6.5 + bounce_buffer_size_px=8000 — elimineert PSRAM bus-conflict op S3 RGB panel |
| 150 | Sessie 27 | IDF 5.x compile fixes: esp_now_recv_info_t → struct esp_now_recv_info, ESP_PD_DOMAIN_RTC_FAST/SLOW_MEM bewaakt met #if IDF < 5, BLE onResult/getManufacturerData core 3.x types |
| 151 | Sessie 28 | BKOS-Brug integratie: BLE GATT Central voor Pi Zero 2W WiFi-brug, victron_scan_pauzeer/hervatten, bkos_brug + screen_brug modules, BRUG in nav bar |
| 152 | Sessie 29 | Compile-fix alle 6 platforms: arduino-cli auto-prototype hoisting (bkos_brug BLE-types + bkos_client WStype_t) + Pico bkos_client guards in hardware.ino |
| 153 | Sessie 29 | VEILIGHEID: ingangskanaal mag nooit als uitgang aangestuurd worden. Centrale io_drijf_hoog() in io_cyclus() (enig drive-punt, forceert ingang=LAAG); io_verlichting_update/io_apparaat_toggle/io_actie_uitvoeren slaan ingangen over. Bug: "**motor" als ingang kreeg toch stroom in vaarmodus MOTOR |
| 154 | Sessie 30 | Versionerings-/promotieproces vastgelegd voor testers: twee-vormen-regel expliciet, promotie-grondregels (nooit autonoom, code-identiek aan geteste beta, feature-uit-beta-first, tag-ná-CI ivm 404, nooit lager nummer). firmware/beta_historie.json (beta→stabiel link alleen aan betakant). Installer default naar stabiele release |
| 155 | Sessie 30 | OTA "GitHub fout -1" opgelost: ota_git_check/ota_laad_releases/ota_download_toepassen gebruiken nu een eigen WiFiClientSecure+setInsecure() (zoals meteo http_get) i.p.v. enkelvoudige http.begin(url); de interne TLS-client faalde op de handshake. +setTimeout/useHTTP10 + 1 retry bij verbindingsfout. Vereist eenmalig USB-flash want oude firmware heeft dezelfde OTA-bug |
| 156 | Sessie 30 | S3 terug naar core 2.0.17: core 3.x brak WiFi/netwerk op S3 (meteo update niet, OTA fout -1; 29.8 was laatste goede = laatste core-2.x). build.yml S3-job → core 2.0.17/GFX 1.3.7/FlashMode dio. hw_scherm RGB-constructor dual-compat via ESP_ARDUINO_VERSION_MAJOR guard (core 2.x = geen bounce buffer, pclk 10MHz). Alle 6 platforms compileren lokaal op core 2.0.17 |
| 157 | Sessie 30 | Meldingen-engine (CallMeBot Signal/WhatsApp): melding.h/.ino — ontvangers {tel,key,dienst} eigenaar+4 extra, /bkos_melding.csv, niet-blokkerende wachtrij, verzending via gewone HTTP vanuit netwerk-taak. IO-trigger via bestaande io_alert. Hartslag bij opstart + dagelijks/wekelijks. Eigenaar-tel uit info e_tel |
| 158 | Sessie 30 | MELDINGEN instellingenscherm (screen_melding): scrollbaar, hoofdschakelaar/opstart/hartslag/eigenaar dienst+key/4× extra/TEST/OPSLAAN, hergebruikt config-toetsenbord. SCREEN_MELDING + nav-item "MELD". Deel 1 van meldingen compleet; deel 2 (bericht aan eigenaar) + deel 3 (extern via BT/WiFi) volgen later |
| 159 | Sessie 30 | Configureerbare PANEEL-knoppen stap 1: paneel.h/.ino (6 namen, /bkos_paneel.csv, default oorspronkelijke 5), screen_main adaptieve layout 1→1/2→2/3→1x3/4→2x2/5→3+2/6→2x3 (landscape + SCREEN_SMALL), draw+hit-test via _paneel_rect, dev_lokaal[6] |
| 160 | Sessie 30 | Configureerbare PANEEL-knoppen stap 2: screen_paneel — 6 naam-slots via config-toetsenbord (net als IO-namen) + OPSLAAN. SCREEN_PANEEL + nav-item "PANEEL". Knop schakelt alle IO-kanalen met die naam; lege slots verborgen. Bijzondere/afwijkende knopfuncties = later |
| 161 | Sessie 31 | IO-REGRESSIE opgelost (werkte op v0.1.5, daarna dood op S3): de gate `if (bkoss_actief) io_detect()` in io_boot() brak alle IO zodra de "?"-versiehandshake faalde terwijl de ATtiny wél aangesloten was → io_kanalen_cnt bleef 0 → io_zichtbaar()==0 → io_cyclus() deed niets. io_detect() loopt nu altijd (eigen timeouts, hangt niet zonder hardware; op S3 is IO_SERIAL hardware UART0 dus flush() blokkeert nooit), en bkoss_actief wordt afgeleid uit io_kanalen_cnt>0 zodat splash/INFO/CONFIG/slaap kloppen ook zonder versiestring |
| 162 | Sessie 32 | Stabiele release 0.1.6 gepromoot van geteste beta 0.1.260609.4 via **branch-methode** (release/0.1.6 + CI bouwt 0.1.6-binaries → tag v0.1.6 → branch weg), zodat de beta op `main` ongemoeid 0.1.260609.4 blijft. versie_stable_*.txt→0.1.6, releases.json-entry, beta_historie 260609.4 gepromoot=0.1.6. Promotieprocedure in CLAUDE.md herschreven naar deze branch-methode |
| 163 | Sessie 32 | CallMeBot alt telefoon/code per dienst: naast elke token (eigenaar + 4 extra ontvangers) optioneel een Signal- en WhatsApp-specifiek nummer/code dat niet aan cijfers gebonden is (alfanumeriek toetsenbord), voor het geval CallMeBot een nummer als code met letters/`-` ziet (bv. Signal-UUID). Leeg = fallback op gewoon nummer. signal_tel/whatsapp_tel in struct + eigenaar, MELDING_TEL2_LEN=40, CSV eig_st/eig_wt + x{i}_st/x{i}_wt, _verstuur_allen kiest per dienst |
| 164 | Sessie 32 | Boottype in 2 stappen (categorie → model). Nieuw `boot_modellen.h/.cpp`: data-gedreven model-register (segmenten + patrijspoorten + lamp-posities), één `boot_model_teken`/`_silhouet` voor groot/pico/mini-preview. `boot_type` → `boot_cat`+`boot_model` (app_state, met migratie van oude btype). Per categorie lichtprofiel (motor: geen 3-kleuren; klein: 1 witte lamp) + vaarmodi (motor: geen ZEILEN) — `boot_lichten_teken` leest lamp-posities/profiel uit model/categorie, vaarmodus-grid herpakt zichtbare modi. screen_config 2-staps keuze (CATEGORIE+MODEL rij, beide layouts) met silhouet-preview. Startset: Zeil(Westerly/Jachtschouw/Catamaran), Motor(Kruizer/Doerak), Kl.zeil(Open zeilboot), Kl.motor(Open sloep/Speedboat); rest volgt iteratief. Prototype-hoisting fix: boot_modellen.h via app_state.h vroeg zichtbaar |
| 165 | Sessie 32 | Spooklijnen-oorzaak gevonden: BKOS4 `tekenItem` gebruikt pen-toggle (pen begint uit, dubbel punt = pen aan/uit → lijn-onderbreking); de BKOS-NUI-port miste dat en tekende altijd door. `_pad()` herkent nu een dubbel beginpunt en past de BKOS4-toggle toe (anders doorlopende lijn). Westerly teruggezet op de EXACTE BKOS4 `teken_boot`-data (per rol gekleurd) + 2 ronde patrijspoorten → identiek aan BKOS4. Nieuw `tools/boot_editor.html`: standalone editor (web + offline) — plak `int NAAM[][2]`-arrays, zie de tekening, klik op een lijn → springt naar de coördinaten in de code; live preview, zelfde toggle-semantiek als firmware |
| 166 | Sessie 33 | STROMING: nieuwe 4e tab onder METEO (vaartijd vs getijstroom). `stroming.h/.cpp`: offline reken-engine (5-min integratie) + parametrisch stroommodel per traject; ijkt op HW uit de bestaande `getijdata`-cache (geen live stroombron — RWS geeft alleen waterstand, stroming zit enkel in DONAR). Eerste beta `0.1.260701.1` |
| 167 | Sessie 33 | STROMING herbouw naar **havengraaf**: 17 jachthavens NL+BE als knopen op een backbone (kust + rivieren + Oosterschelde), BFS-route, per-leg stroom met eigen ijkstation, absolute-tijd integratie via HW-provider (1 referentiestation + `stroming_hwlag[]`). Twee-staps UI (vertrek→aankomst met min-max vaartijd per kandidaat, getint <8/8-12/12-16u, >16u verborgen →tabel). NL/BE-vlaggen contextueel, sluis-tijdverlies per haven. Hoek v.Holland vervalt als haven (blijft interne ijk); Wad overgeslagen. Beta `0.1.260701.2`. Stroomwaarden (m.n. rivieren/België) = schattingen, per traject te tunen |
| 168 | Sessie 33 | STROMING: **routekeuze** (edge-sluizen + `stroming_zoek_routes` DFS-enumeratie → fase 2 kiest route), Bruinisse (2 routes: Grevelingensluis/Oosterschelde óf Brouwerssluis/Grevelingenmeer) + Waddenhavens/zeegaten (indicatief, droogvallende platen niet gemodelleerd). Beta `0.1.260702.1` |
| 169 | Sessie 33 | STROMING UI: vertrekhavens gesorteerd (NL→BE→DE, alfabetisch); aankomst gesorteerd op reistijd (dichtstbij eerst); **dagkeuze** (◀/▶, tot 13 dagen); tweeniveau-tabel: **uur-overzicht** (min-max per uur, grid zonder scrollen) → tik uur → **5/10-min-detail** met vorig/volgend uur. Beta `0.1.260703.1` |
| 170 | Sessie 33 | STROMING details: vertrekhaven toont provincie-afkorting (NH/ZH/Zld/Fr/Gr/An/WV) + sortering op (land, provincie, naam); route-label noemt nu álle vaarwegen in volgorde van aandoen (i.p.v. één middelpunt); uur-overzicht heeft kleurcodering snel→langzaam (groen→rood heatmap) met groene/rode rand voor snelste/langzaamste uur. Beta `0.1.260703.2` |
| 171 | Sessie 33 | STROMING weer+dag/nacht in uur-overzicht én uur-detail: kleinste symbool = slechtste weer over de overtocht (onweer→bliksem, regen→bui met druppels naar intensiteit, helder→zon; bewolkt/mist=niks) uit `meteo_uur_wcode`. Dag/nacht-indicator vóór (vertrek) en ná (aankomst) het weericoon: maan=donker, zonsopkomst-symbool=vóór zonsopkomst (dageraad), zonsondergang-symbool=na zonsondergang (schemer). Zon-op/onder **astronomisch** berekend (werkt zonder weerdata; alg. gevalideerd Scheveningen). Zonder weerdata: enkel de zon/maan-indicator. Beta `0.1.260703.3` |
| 172 | Sessie 33 | Locatie als spil: coördinaten toegevoegd aan RWS-getijstations (getijdata.h) én havens (stroming.cpp). Bij weerlocatie-wissel (ip-api/geocode) wordt automatisch het dichtstbijzijnde RWS-getijstation gekozen (`getijdata_dichtstbij`). LOCATIE-stationlijst gesorteerd op afstand tot weerlocatie; stationnaam in getij-tab klikbaar → LOCATIE-tab. Vertrekhaven-sortering nu op afstand tot weerlocatie (i.p.v. provincie). Plaatsnaam in statusbalk klikbaar → weerlocatie wijzigen. Detail uur-tabel op 10 min. Beta `0.1.260703.4/.5` |
| 173 | Sessie 33 | STROMING/weer: plaatsnaam verplaatst van statusbalk naar het WEER-scherm zelf (klikbaar → weerlocatie wijzigen). Uur-symbolen: bij geheel donkere momenten (vertrek én aankomst in het donker) nu een maan als hoofdicoon i.p.v. zon, als signaal dat het geen fijn vaarmoment is. Beta `0.1.260703.6` |
| 174 | Sessie 33 | STROMING: Duitse Wadden + extra NL wad-havens. 2 NL (Noordpolderzijl, Termunterzijl) + 18 DE (Borkum, Juist, Norderney, Norddeich, Greetsiel, Baltrum, Langeoog, Bensersiel, Spiekeroog, Neuharlingersiel, Harlesiel, Wangerooge, Hooksiel, Wilhelmshaven, Emden, Bremerhaven, Cuxhaven) via Oost-Friese kust/eilanden. DE-vlag toegevoegd; ijk op Delfzijl (geen DE-getijstations → grover naar het oosten, indicatief). Totaal 48 havens |
| 175 | Sessie 33 | WEER plaats-picker: klik op plaatsnaam → lijst van havens gesorteerd op afstand (met vlag + km) → kies plaats (zet weerlocatie + dichtstbij getijstation), of "Typ plaats..." voor geocode-zoeken zoals voorheen. Beta `0.1.260707.1` |
| 176 | Sessie 33 | Scherm 180° draaien: knop in WEERGAVE & ENERGIE (`tft_gedraaid`, opgeslagen in app_state config `draai=`). `tft_rotatie_toepassen()` zet basis+180° (SPI setRotation base+2; S3 RGB rot 2), toegepast in tft_setup én na state_load (vóór splash). Touch-flip (x/y invert) in `ts_touched()` beide takken. Live toggle + gegarandeerd na herstart. Beta `0.1.260707.2` |
| 177 | Sessie 34 | **Ongekoppelde slave schakelde niets**: een verse unit staat standaard op `NET_SLAVE`, en de `net_io_*()`-functies in `bkos_net.ino` stuurden alleen lokaal aan bij `MASTER \|\| STANDALONE` — anders gingen ze via ESP-NOW, wat direct terugkeerde op `!net_gepaard`. Gevolg: PANEEL-knoppen en AAN/UIT in het IO-scherm deden niets, terwijl vaarmodus/verlichting wél werkten (die schrijven `io_output[]` altijd lokaal). Nieuwe `net_io_lokaal()` = `MASTER \|\| STANDALONE \|\| !net_gepaard` — exact dezelfde voorwaarde die `io.ino` al gebruikt in `_io_achtergrond_taak()` (r269) en `io_verlichting_update()` (r554). Beta `0.1.260818.1` |
| 178 | Sessie 34 | **Tolerante naamherkenning** voor apparaatnamen: nieuwe `io_naam_match()` naast `io_naam_is()` — negeert spaties vooraan/achteraan, hoofdletterongevoelig, en de `**`-markering is optioneel aan beide kanten (knop "USB" vindt kanaal "**USB" en omgekeerd). Gebruikt door PANEEL-knoppen, `io_apparaat_toggle/staat3`, Lua `io.readName/writeName` en de ESP-NOW naam-commando's. De vaste systeemnamen (`**L_...`, `**haven`, `**IL_...`) blijven `io_naam_is()` gebruiken, zodat een kanaal dat gewoon "anker" heet niet door de vaarmodus geschakeld wordt |
| 179 | Sessie 34 | **Chips in PANEEL-toetsenbord**: `cfg_kb_chips` toont de kanaalchips ook in info-mode, zodat een PANEEL-knopnaam met één tik de volledige naam (`**USB`) krijgt i.p.v. `**` handmatig via SYM. Tekenen en aanraken delen nu één voorwaarde (`cfg_chips_zichtbaar()`) — voorheen liep de klikzone uit de pas met de tekening, wat onzichtbare maar actieve chip-zones opleverde die de invoer overschreven. Alle andere info-mode toetsenborden (INFO, MELDING, WIFI) zetten de vlag expliciet uit |
| 180 | Sessie 34 | **Dynamo-bekrachtiging op `**motor`**: de laadlampdraad ontbreekt, waardoor de dynamo zichzelf niet bekrachtigt en D+ laag blijft ook als de motor loopt. `io_dynamo_loop()` (io.ino) zet daarom periodiek 1s spanning op het `**motor`-*ingangs*kanaal, laat los, wacht 4s en meet dan op een verse `io_cyclus` of er nog spanning staat → `motor_draait`. Zolang de motor draait wordt niet meer gepulst. Instelbaar interval (UIT/1/2/5/10/15/30 min) via `dynamo_puls_min`, opgeslagen als `dynpuls=` in de app-config. **Veiligheid**: `io_drijf_hoog()` blijft het enige drive-punt; de enige uitzondering op "een ingang wordt nooit aangestuurd" is `io_dynamo_drijft()`, die uitsluitend het door de sequentie gekozen kanaal en uitsluitend binnen een puur op tijd getoetst venster van 1s hoog laat — een blijvende drive is daardoor onmogelijk, ook als de toestandsmachine zou blijven hangen. Ingangsacties en meldingen op dat kanaal worden tijdens de sequentie onderdrukt (`io_dynamo_bezig()`), anders vuurt elke puls `IO_ACTIE_MODUS_MOTOR` af. Beta `0.1.260818.2` |
| 181 | Sessie 34 | Dynamo-instelling in het **IO CFG-overlay** van `**motor` (niet in WEERGAVE & ENERGIE: die pagina zit vol — de laatste rij eindigt op y=432, nav bar begint op 438). Alleen zichtbaar als het kanaal INGANG is én `**motor` heet; volgt de bestaande OPSLAAN/SLUITEN-semantiek van het overlay. Beide UI-varianten (groot 800×480 en SCREEN_SMALL) |
| 182 | Sessie 34 | **Kanaallabel A1/B3/...**: nieuwe `io_kanaal_label()` (io.ino) leidt uit `io_aparaten[]` de module af waarin een kanaalindex valt en geeft letter (A=module 0, B=module 1, ..., Z, dan AA/AB/...) + volgnummer binnen die module vanaf 1 (1–8, of 1–16 bij LOGICA16/SCHAKEL16). Kanalen voorbij de laatst gedetecteerde module (alleen zichtbaar via de handmatige `io_kanalen_cfg`-override) tellen als opeenvolgende virtuele 8-kanaals modules. In het **IO-scherm** (`screen_io.ino`) vervangt dit label het technische kanaalnummer in de rijweergave, beide UI-varianten. In **IO CONFIGURATIE** (`screen_io_cfg.ino`) staan technisch nummer én label naast elkaar — in de rijlijst gestapeld (nr erboven, label in cyaan eronder) en in de overlay-titel samen (`Kanaal 12 (B5): naam`) |
| 183 | Sessie 34 | **Dynamo-status "blijft actief" in de UI opgelost**: de statusweergave voor `**motor` las overal de ruwe `io_input[]` — die staat tijdens/vlak na de bekrachtigingspuls kort hoog omdat de puls zelf de spanning zet, wat niets zegt over de echte dynamostand. Nieuwe `io_kanaal_input_effectief(kanaal)` (io.ino) geeft voor het dynamokanaal `motor_draait` terug (de laatst bevestigde meting uit `io_dynamo_loop`, alleen bijgewerkt in rust en 4s ná loslaten) i.p.v. de ruwe ingang; voor elk ander kanaal ongewijzigd `io_input[]`. Gebruikt door `io_licht_staat()` (io.ino) en de input-dot in `screen_io.ino` (beide UI-varianten). Daarnaast is `io_licht_staat()` nu richting-bewust: een INGANG toonde voorheen "KOELT" (een gekleurde, actief-ogende badge) zodra de ingang toevallig hoog was — dat "naijl"-concept hoort bij een zojuist uitgeschakelde UITGANG, niet bij een sensor-ingang. Een ingang toont nu gewoon AAN/UIT op basis van de effectieve stand. Beta `0.1.260818.4` |
| 184 | Sessie 34 | **Dynamo bleef écht aan staan (niet enkel UI)**: v0.1.260818.4 loste de weergave op, maar `motor_draait` kon nog steeds vast blijven staan op "aan" — `DYN_RUST` zette `motor_draait = io_input[k]` bij elke tick buiten de sequentie, een pure passieve aflezing van een kanaal dat de rest van de tijd niet gegarandeerd hoogohmig/stabiel is. Ontdekte hij die ooit als hoog, dan bleef hij vastzitten (`if (motor_draait) return;` sloeg elke volgende puls-poging over — geen mechanisme om ooit terug te vallen). Fix: die passieve lezing + de "sla over als motor al draait"-kortsluiting zijn verwijderd. `motor_draait` wordt nu UITSLUITEND gezet door `DYN_METEN` (de puls-loslaat-4s-wacht-meet-cyclus), en dat gebeurt bij ELK interval opnieuw, ook als de vorige meting "aan" gaf — dus zelfcorrigerend binnen één interval, nooit meer een vaste waarde zonder verse meting erachter. Bij opstarten is `motor_draait` het onaangetaste `false`-startwaarde (UIT) totdat het eerste interval verstrijkt en een echte meting plaatsvindt. Beta `0.1.260818.5` |
| 185 | Sessie 34 | **Lokale webapp (afstandsbediening via WiFi)**: nieuw `webapp.h/.ino` — HTTP-server (poort 80, ESP32 core `WebServer`) die één statische pagina serveert (`webapp_html.h`, PROGMEM, geen externe dependencies zodat het ook zonder internet werkt). De pagina praat verder uitsluitend met de WebSocket-server op poort 8080, die al bestond (`bkos_client.h/.ino`, gebouwd voor "BKOS Brug") maar **volledig dood was**: gated achter `ESP_ARDUINO_VERSION_MAJOR >= 3`, en sinds Sessie 30 draaien alle platforms weer op core 2.0.17 — de guard is verwijderd, `bkos_client_setup/loop()` draait nu gewoon mee. Protocol uitgebreid: `r[]` (richting) en `lbl[]` (kanaallabel A1/B3/…) toegevoegd aan `io_full`; nieuwe `paneel`-broadcast + `paneel_toggle`-commando voor de PANEEL-knoppen; `io`-arrays gebruiken nu `io_kanaal_input_effectief()` i.p.v. ruwe `io_input[]` (zelfde dynamo-fix als het scherm, zie taak 183). **PIN-gate**: alles wat status leest blijft voor iedereen op het lokale netwerk zichtbaar zonder inloggen; schrijfcommando's (`io_toggle`, `paneel_toggle`, `set_modus`, `set_licht`) vereisen `{"t":"auth","pin":"…"}` met dezelfde 4-cijferige PIN als het CONFIG-scherm (`pin_lezen_pub()`, `/bkos_pin.txt`, default "0000" als niet ingesteld). **Bugfix onderweg**: `_ws_klanten`/`_ws_ontgrendeld` stonden hardcoded op `[4]` terwijl de WebSockets-library `WEBSOCKETS_SERVER_CLIENT_MAX=5` toestaat — een 5e gelijktijdige klant schreef buiten de array. Nu `[WEBSOCKETS_SERVER_CLIENT_MAX]`. Bereikbaar via `http://<ip-van-het-apparaat>/` (IP zichtbaar op het WIFI-scherm, nu ook met een directe "Afstandsbediening: http://…" regel) of `http://<bootnaam>.local/` (mDNS — werkt op iPhone Safari zonder aparte app of browser, Bonjour is ingebouwd in iOS). Uitsluitend bereikbaar binnen het WiFi-netwerk van de boot (of via de BKOS-Bridge AP) — geen internet-toegang, geen relay. Beta `0.1.260819.1` |
| 186 | Sessie 34 | **Dynamo-puls: 3 loslaat-cycli i.p.v. 1**: Brendan meldde dat `**motor` na de bekrachtigingspuls (5 min-interval) niet meer uitging. `DYN_LOSLATEN` draaide tot dan toe maar één `io_cyclus()` om het kanaal weer laag te zetten; een enkele UART-cyclus die niet aankomt (denkbaar: dit kanaal zit op de D+ lijn van de dynamo zelf, een bekende bron van elektrische ruis) laat de "aan"-drive dan onopgemerkt staan tot de volgende volledige cyclus. Nieuwe `DYN_LOSLAAT_CYCLI=3`: na de 1s-puls draait `DYN_LOSLATEN` nu drie volledig afgeronde, losse cycli op rij (elk met eigen `DYN_CYCLUS_MAX`-timeout) vóór de 4s-wachttijd/meting start. De harde veiligheidsgarantie blijft ongewijzigd: `io_dynamo_drijft()` is en blijft een pure tijdstoets (max 1s vanaf pulsstart), dus de fysieke drive vervalt hoe dan ook — de 3 cycli verbeteren alleen de kans dat het "laag"-commando de hardware daadwerkelijk bereikt. Beta `0.1.260819.2` |
| 187 | Sessie 34 | **NOODFIX — bootlus door webapp/WebSocket-revival**: Brendan meldde dat het apparaat na v0.1.260819.x bleef hangen/loopen op het opstartscherm. `bkos_client_setup()`/`webapp_setup()` draaien synchroon in `hw_setup()`, vóór `if (splash) delay(1000)` — een crash/hang daar verklaart exact dat symptoom, en `wifi_taak_start()` (net ervoor) start WiFi asynchroon, dus een race tussen die achtergrondtaak en de synchrone `_ws.begin()`/`_http.begin()`-aanroepen is aannemelijk. Dit was de EERSTE keer dat deze code (oorspronkelijk gebouwd voor core 3.x, sinds Sessie 30 al maanden dood) daadwerkelijk draaide op core 2.0.17 — nooit eerder getest op echte hardware. Nieuwe schakelaar `BKOS_REMOTE_ENABLED` (bkos_client.h, nu `0`) rond de `_setup()`/`_loop()`-aanroepen in `hardware.ino`: de globale `WebSocketsServer`/`WebServer`-objecten blijven bestaan (constructie was al maandenlang bewezen veilig — dat is precies hoe `bkos_client.ino` al sinds de core-2.x-rollback meedraaide, alleen zonder dat `bkos_client_setup()` ooit werd aangeroepen), maar `.begin()`/mDNS lopen niet meer automatisch mee bij opstarten. Herstelt het gedrag van vóór Sessie 34's webapp-werk. Webapp-code blijft in de repo staan voor later, maar moet eerst op echte hardware met een seriële monitor onderzocht worden vóór de schakelaar weer aan mag. Beta `0.1.260819.3` |
| 188 | Sessie 34 | **`**motor` blijft écht hoog — ontbrekende afzender-verificatie in bkos_net.ino**: derde melding van hetzelfde symptoom; Brendan bevestigde nu dat een herstart het weer laag maakt, wat op corruptie van `io_richting[]` tijdens runtime wijst (niet een dynamo-state-machine-bug — die is al 2x nagelopen en is wiskundig begrensd tot 1s drive). Gevonden: `NET_MSG_IO_STATE` (en `IO_NAMEN`, `IO_TOGGLE`, `IO_NAAM`, beide takken van `APP_STATE`) verwerkten inkomende ESP-NOW pakketten **zonder te controleren of de afzender daadwerkelijk de eigen gepaarde master/bevestigde slave is** — alleen `net_modus` werd gecheckt, niet de MAC. `IO_STATE` schrijft `io_richting[]` **onvoorwaardelijk** over met de inhoud van het pakket; een pakket van een willekeurige andere ESP-NOW-afzender (bv. een los testbordje in bereik, mogelijk zelfs een oude/vergeten pairing) kon zo `io_richting[]` overschrijven — en als **motor's index daarbij op UITGANG belandt, pakt `io_verlichting_update()`'s vaarmodus-relaislogica hem over en zet `io_output[**motor]=AAN` zodra `vaar_modus==MODE_MOTOR`, **buiten de hele dynamo-toestandsmachine om**. Een herstart laadt `io_richting[]` weer correct uit de persistente config (`hw_io_cfg_laden()`, alleen bij boot aangeroepen) → weer laag, exact het gemelde gedrag. Fix: `IO_STATE`/`IO_NAMEN`/`APP_STATE`(master→slave) vereisen nu `net_master_bekend() && _mac_gelijk(mac, net_master_mac)`; `IO_TOGGLE`/`IO_NAAM`/`APP_STATE`(slave→master) vereisen nu `idx >= 0 && net_peers[idx].bevestigd` — hetzelfde patroon dat al bestond bij `NET_MSG_INFO_UPDATE` maar niet consequent was toegepast. **Alleen relevant als het apparaat niet STANDALONE is** (`net_loop()` slaat ESP-NOW helemaal over bij STANDALONE) — nog te bevestigen bij Brendan of zijn eenheid als MASTER/SLAVE draait en of er een ander BKOS-bordje in ESP-NOW-bereik stond. Beta `0.1.260819.4` | Bevestigd door Brendan: eenheid draait als MASTER, geen actieve slave in de buurt (maar wel eentje voorhanden) — met de fix werkt het nu goed |
| 189 | Sessie 34 | **Vaarmodus-actie van `**motor` gedebounced + globale AUTO-schakelaar**: `io_actie_aan/uit` op een ingangskanaal (bv. `**motor` → `IO_ACTIE_MODUS_MOTOR`) vuurde af op elke ruwe `io_input`-overgang die `io_cyclus()` zag — dat gebeurde dus ook via gewone hartslag-cycli (elke 30-120s) tussen twee dynamo-pulsen door, op een niet-gedebouncede, mogelijk onbetrouwbare aflezing. Nieuwe `io_dynamo_debounced()` (io.ino, vervangt `io_dynamo_bezig()`) onderdrukt voor het dynamokanaal ALTIJD de ruwe-overgang-actie in `io_cyclus()`; de actie/melding vuurt voortaan uitsluitend in `DYN_METEN` af, en dan alleen als de gedebouncede `motor_draait`-waarde daadwerkelijk verandert — dus precies één keer per interval. Nieuwe globale `vaarmodus_auto` (bool, persistent, `app_state`) is de master-schakelaar: staat hij uit, dan doet `io_actie_uitvoeren()` niets bij `IO_ACTIE_MODUS_*` (`IO_ACTIE_OUTPUT_*` blijft altijd werken). Handmatig een modus kiezen via de knoppen zet `vaarmodus_auto` nooit uit — alleen de nieuwe knop zelf doet dat. UI: ronde knop op het kruispunt van de 2×2 vaarmodus-grid (groot scherm, `AUTOMODUS_CX/CY/R` in `screen_main.h`, cirkel-hittest vóór de rechthoekige knoppen gecheckt om overlap in de hoeken op te lossen); op SCREEN_SMALL (geen kruispunt-layout, verticale stapel) een aparte `PICO_AKNOP`-rij tussen de 4 modus-knoppen en de verlichtingsknop. Ook: `io_actie_uitvoeren()`'s MODUS_*-cases riepen voorheen geen `io_verlichting_update()`/`net_app_staat_sturen()` aan (in tegenstelling tot de handmatige knop) — nu gelijkgetrokken. Beta `0.1.260819.5` |
| 190 | Sessie 34 | **AUTO-knop + vaarmodus onthouden als één instelling; idle-bewaking met 3-op-rij-bevestiging**: Brendan wilde (a) dat het opslaan van `vaarmodus_auto` en de actuele `vaar_modus` een instelling wordt i.p.v. altijd/nooit, en (b) dat gewone hartslag-cycli tussen dynamo-pulsen door wél naar wijzigingen blijven kijken (taak 189 schakelde dat volledig uit — te grof), maar tegen spookwaarnemingen pas een afwijking accepteren na 3 metingen op rij die hetzelfde zeggen, elk 3s uit elkaar (wijkt een tussenmeting af, dan telt die als nieuwe kandidaat en begint de reeks opnieuw). (a): `vaarmodus_auto` valt nu onder dezelfde `onthoud_licht_modus`-vlag als `vaar_modus`/`licht_instelling` (config-key blijft `onthlicht`) — UI-label omgedoopt van "LICHTMODUS AAN/UIT" naar "MODUS ONTHOUDEN AAN/UIT" (`screen_config.ino`) omdat het al langer meer dan alleen licht omvatte. (b): `io_dynamo_bezig()` (hernoemd van `io_dynamo_debounced()`, zelfde onvoorwaardelijke onderdrukking in `io_cyclus()`) blijft de ENIGE plek die ruwe overgangen negeert; nieuwe `io_dynamo_bewaking()` draait continu tijdens `DYN_RUST` en implementeert de 3-op-rij-logica (`DYN_KAND_MS`=3s, `DYN_KAND_NODIG`=3) door elke bevestigingsstap een eigen `io_cyclus()` te forceren en op afronding te wachten (zelfde patroon als `DYN_LOSLATEN`). Nieuwe gedeelde `_dyn_actie_vuur()` (actie+melding+`motor_draait`-update) wordt aangeroepen vanuit zowel `DYN_METEN` (één directe, al gevalideerde meting — geen 3x nodig) als `io_dynamo_bewaking()`. Bewaking pauzeert zodra een puls start (`dyn_kand_teller=0` bij de overgang naar `DYN_PULS`) en hervat na terugkeer naar `DYN_RUST` — dekt zo vanzelf "tijdens en de eerste paar seconden na de puls" (de bestaande 4s `DYN_WACHT`-fase valt daar al binnen). Beta `0.1.260820.1` |
| 191 | Sessie 34 | **AUTO-knop groter + met marge**: straal `AUTOMODUS_R` van 22 naar `UI_SCY(28)`, plus een achtergrond-halo (`AUTOMODUS_HALO_R` = straal + 6px marge, in `C_BG` getekend vóór de knop zelf) die de binnenhoeken van de 4 vaarmodus-knoppen rond de cirkel wegneemt — geeft het "netjes afgeknipt" effect i.p.v. strak tegen de knoppen aan. Om hiervoor ruimte te maken is de kloof tussen de 2×2-knoppen zelf ook vergroot: nieuwe `MKNOP_XGAP` (`UI_SCX(32)`, was hardcoded `8`) en `MKNOP_GAP` (`UI_SCY(32)`, was `UI_SCY(8)`) — `MKNOP_W` iets smaller als gevolg, ruim binnen de marge op alle platforms (kleinste geval CYD40H: knop ±179px→165px breed). Tikzone volgt nu ook de halo (`AUTOMODUS_HALO_R`), dus een grotere, vergevingsgezinde knop. Alleen het grote scherm (2×2-grid); SCREEN_SMALL gebruikt nog steeds de rechthoekige AUTO-rij, ongewijzigd. Beta `0.1.260820.2` |
| 192 | Sessie 34 | **AUTO-knop naar 65px, tussenruimte 4 knoppen teruggedraaid**: Brendan wilde de knop nog veel groter (60-70px straal) mét minstens 4px vrije ruimte eromheen, maar expliciet zónder de bestaande 4 vaarmodus-knoppen verder uit elkaar te zetten — taak 191's aanpak (kloof vergroten) ging daar juist tegenin. `MKNOP_XGAP`/`MKNOP_W`/`MKNOP_GAP` terug naar de oorspronkelijke, kleine kloof (`8`/`UI_SCY(8)`, ongedaan gemaakt). `AUTOMODUS_R` naar `UI_SCY(65)` (65px op de 7"/S3); de knop snijdt daardoor bewust flink in de binnenhoeken van de 4 knoppen — de achtergrond-halo (`AUTOMODUS_MARGIN`=4, getekend vóór de knop) "knipt" die hoeken weg en levert zo het gat + de vrije ruimte, zónder de knoppen zelf te verplaatsen. Icoon/tekst van elke modus-knop staan ver genoeg van de hoek (icoon/label horizontaal gecentreerd, niet in de hoek) om nooit geraakt te worden — geverifieerd voor zowel S3 als CYD40H (kleinste 2×2-grid-platform: knopbreedte 111px, marge 8px t.o.v. nav bar, ongewijzigd t.o.v. vóór Sessie 34). **Bijvangst**: taak 191's kloof-vergroting bleek de apparaat-knoppen op CYD40H al met 8px onder de nav bar te duwen (niet eerder opgemerkt/getest) — dat is met deze terugdraai ook meteen gecorrigeerd. AUTO-tekst nu size 2 i.p.v. 1, passend bij de grotere knop. Beta `0.1.260820.3` |
| 193 | Sessie 34 | **AUTO-knop naar 55px + AAN/UIT-icoon los van kleur**: `AUTOMODUS_R` van `UI_SCY(65)` naar `UI_SCY(55)`. Brendan vond de kleurstatus (cyaan vs. grijs) niet intuïtief genoeg (kleur is afhankelijk van het actieve kleurenschema, dus niet altijd eenduidig) — nu een expliciet icoon los van kleur: AAN toont een rond-draai-icoon (twee ~130°-ringsegmenten via `tft.fillArc()`, elk afgesloten met een pijlpunt via nieuwe `_pijlpunt_teken()`, die met sin/cos de tangentiële richting berekent) rond het woord "AUTO"; UIT toont een rood kruis (`C_RED_BRIGHT`, 3 evenwijdige lijnen per diagonaal voor dikte) dwars over de tekst. Beta `0.1.260820.4` |
| 194 | Sessie 34 | **`**motor` — vierde melding, code grondig herverifieerd**: Brendan had opnieuw het idee dat `**motor` ergens actief werd gehouden. Volledige herinspectie: de netwerk-MAC-verificatie (taak 188) staat nog intact, geen nieuwe schrijfplekken naar `io_richting[]`, de puls/loslaat/meet-sequentie ongewijzigd correct. Wél gevonden: `dyn_kand_cyclus_bezig` (idle-bewaking, taak 190) werd niet gereset bij het pauzeren van een lopende bevestiging (overgang `DYN_RUST`→`DYN_PULS`, of als het kanaal wegvalt) — een volgende bewakingsronde zou daardoor kunnen denken dat een allang-vervallen cyclus-aanvraag nog loopt en zijn eigen verse aanvraag overslaan. Nu consequent gereset op alle plekken waar `dyn_kand_teller` al gereset werd. Dit verklaart geen "blijvend actief" gedrag op zichzelf (de 3-op-rij-eis blijft gelden), maar is wel een reële inconsistentie, nu gefixt. **Kon geen nieuwe hoofdoorzaak vinden** — gevraagd aan Brendan: staat `vaarmodus_auto` aan (dan kan MOTOR-modus via een echte detectie legitiem actief worden, geen bug), is dit via de UI/badge of fysiek gemeten, en verhelpt een herstart het (net als de taak 188-bug) of blijft het ook dan actief. Beta `0.1.260820.5` |
| 195 | Sessie 35 | **`**motor` — vijfde melding, echte hoofdoorzaak gevonden: DYN_METEN vuurde ongedebounced**: Brendan verduidelijkte na een korte discussie het bedoelde model expliciet: de status van `**motor` moet continu bepaald worden op momenten dat de startpuls NIET gegeven wordt (idle-bewaking, 3 metingen op rij met 3s tussenpauze — dus taak 190's `io_dynamo_bewaking()` was al het juiste ontwerp), en de puls zelf dient uitsluitend om de dynamo te bekrachtigen, niet als meetmoment. Bug gevonden in `DYN_METEN` (io.ino): die riep na de puls-sequentie nog steeds direct `_dyn_actie_vuur()` aan op basis van één ongeconfirmeerde meting — precies op het moment dat de D+ lijn (bekende ruisbron) het gevoeligst is voor een spookwaarneming, zonder de 3-op-rij-bescherming die overal elders wél gold. Fix: `DYN_METEN` bepaalt de status niet langer zelf; na de meting gaat de sequentie terug naar `DYN_RUST` en neemt de bestaande `io_dynamo_bewaking()` de statusbepaling volledig over, ook voor de eerste meting na een puls. Gecompileerd en geverifieerd op alle 5 ESP32-platforms. Beta `0.1.260821.1` |
| 196 | Sessie 35 | **Herstelmenu tijdens opstarten**: nieuw `recovery.h/.ino`. Raakt de gebruiker binnen 3s na het opstartlogo het midden van het scherm aan (middelste 1/3×1/3 van het scherm), dan verschijnt een keuzemenu: GEWOON OPSTARTEN, BKOS VERWIJDEREN (zet blanco firmware terug, `BLANCO_BIN_URL` uit `ota.h`, met JA/ANNULEREN-bevestiging) of UPDATE (haalt en flasht de nieuwste STABIELE GitHub-release, forceert `ota_beta_kanal=false` voor de duur van de check zodat herstel nooit een beta pakt, ongeacht de normale kanaalinstelling van het apparaat). Doel: niet afhankelijk zijn van de USB-poort als de hoofdsoftware kapot is. Om die reden draait dit met UITSLUITEND scherm (`tft_setup`), touch (`ts_setup`) en WiFi geladen — de check zit vóór `io_boot()`, `app_setup()`, `net_setup()`, `lua_runtime`, victron/brug/data_store enz. in `hw_setup()` (`hardware.ino`), zodat een bug daar het herstelpad niet kan blokkeren. WiFi verbindt blokkerend/synchroon via nieuwe `wifi_verbind_opgeslagen()` (wifi.ino, dunne wrapper om de al bestaande `_wifi_verbinden_intern()` — dezelfde cache+opgeslagen-netwerken-logica, zonder de achtergrondtaak nodig te hebben die pas later start). Flashen hergebruikt de bestaande, al geteste `ota_download_toepassen()`/`ota_git_check()`/`ota_git_update()` uit `ota.ino` (zelfde voortgangsscherm + herstart-bij-succes als het gewone OTA-scherm) — geen nieuwe flash-logica. UI met `ui_knop_groot()`, twee losse handmatig afgestemde layout-constantensets (`#if SCREEN_SMALL`) omdat één formule voor zowel de 240×320- als 800×480-referentie knoppen van het scherm af liet lopen. Splash-tekening uitgesplitst naar nieuwe `_splash_teken()` (hardware.ino) zodat die na een geannuleerd/mislukt herstelbezoek opnieuw getekend kan worden. Gecompileerd en geverifieerd op alle 5 ESP32-platforms. Beta `0.1.260821.2` |
| 197 | Sessie 36 | **Tijd zelfherstellend ophalen + nieuw TIJD-instelmenu**: `ntp_loop()` (wifi.ino) startte tot nu toe de SNTP-sync alleen bij het opstarten (`netwerk_taak`); mislukte die ene poging (bv. router na stroomstoring trager dan het scherm), dan bleef de klok voorgoed op "--:--" staan, ook als WiFi later alsnog verbond. `ntp_loop()` detecteert nu zelf de overgang naar `wifi_verbonden` en start dan alsnog `ntp_start_sync()` (nieuwe centrale functie — `configTzTime()` op ESP32, ongewijzigde `configTime()`-pad op Pico). Klok in de statusbalk aantikken (elk scherm, behalve WIFI/INFO die de hoek al gebruiken) opent nieuw `screen_tijd.h/.ino` (`SCREEN_TIJD`): TIJD OPHALEN (WIFI, blokkerend zoals screen_wifi, geen pincode), HANDMATIG INSTELLEN (`tijd_handmatig_zetten()`, stappers per veld — geen pincode zólang de tijd nog nooit bekend was, wél pincode als een bekende tijd overschreven wordt), TIJDZONE WIJZIGEN (3 Europese presets + vaste UTC±N-offset, altijd pincode; eigen kleine PIN-pad, niet de CONFIG-schermoverlay want die is aan CFG_CONT_Y gekoppeld) en WIFI-NETWERKEN (springt naar `SCREEN_WIFI`). Tijdzone-string is nu overal een echte POSIX TZ-regel (`tijdzone_toepassen()`, persistente `tzidx`/`tzuur` in `bkos_config.csv`) i.p.v. de vaste `NTP_GMT_OFFSET+NTP_DST_OFFSET` som, die het hele jaar UTC+2 gaf i.p.v. alleen 's zomers — loste een sluimerende offset-bug op die niets met de oorspronkelijke vraag te maken had maar wel meteen meespeelde zodra tijdzone instelbaar werd. Layout-constanten voor het PIN-pad en de handmatige-tijd-rij hebben eigen `#if SCREEN_SMALL`-sets (zelfde reden als screen_config's `PIN_KW`/`PICO_PIN_KW`: één UI_SCX-waarde die op de 800px-referentie past, overflowt op de 240px-referentie). Gecompileerd en geverifieerd op alle 6 platforms (S3/WROOM/CYD28/CYD40H/CYD40V/Pico). Beta `0.1.260826.1` |
| 198 | Sessie 36 | **Dubbele buffering (experimenteel) tegen "trillen" tijdens verversing op S3**: Brendan meldde dat het beeld af en toe zichtbaar schudt bij een refresh, en stelde zelf voor eerst alles klaar te zetten en dan pas in één keer toe te passen. Op core 2.x (geen bounce buffer) schrijft elke tekenopdracht rechtstreeks in dezelfde PSRAM-buffer die de RGB-DMA continu uitleest — dat is de bekende bus-contentie waar al meerdere sessies (PCLK/porches) aan getuned is. Nieuwe optie (`hw_scherm.h/.ino`): `Arduino_Canvas` (GFX 1.3.7) wikkelt de echte `Arduino_RGB_Display` in; `tft_p` wijst dan naar de canvas, dus alle bestaande `tft.xxx`-aanroepen tekenen ongewijzigd naar de schaduw-buffer. Nieuwe `tft_flush(forceer)` zet die pas over naar het echte scherm — geforceerd na een volledige hertekening (`scherm_bouwen`), snelheidsbegrensd (max ~25/s, `TFT_FLUSH_MIN_MS`) na losse tik- en periodieke updates, zodat een idle scherm niet continu de volle 800×480-buffer blijft overkopiëren (dat zou de contentie juist verergeren). Twee aanroepen volstonden in `hardware.ino` (`_gui_taak`, ESP32-only) — geen enkel schermbestand aangepast. Valt terug op direct tekenen als `ps_malloc()` voor de canvas-buffer mislukt (te weinig PSRAM) i.p.v. te crashen op een lege framebuffer. Alleen relevant op S3 core 2.x (core 3.x heeft al de bounce buffer; andere platforms zijn SPI-displays zonder gedeelde DMA-buffer) — overal elders `#if PLATFORM_ESP32 && !PLATFORM_WROOM && !PLATFORM_CYD && ESP_ARDUINO_VERSION_MAJOR < 3` uitgeschakeld. Instelbaar via nieuwe "DUBBELE BUFFERING AAN/UIT"-rij in CONFIG → WEERGAVE & ENERGIE (onder SCHERM PCLK, S3-only), default **UIT** — moet door Brendan zelf op de boot getest worden (geen hardware hier om te verifiëren); UIT-zetten herstelt het oude gedrag zonder nieuwe flash. Gecompileerd op S3 (met de nieuwe optie) en WROOM (ongewijzigd pad, spot-check). Beta `0.1.260826.2` |
| 199 | Sessie 36 | **Twee bugs uit taak 198 direct gemeld door Brendan, beide gefixt**: (1) Na het testen van DUBBELE BUFFERING draaide het scherm terug naar normaal terwijl de touch 180° gedraaid bleef — herstarten hielp niet, teruggaan naar 0.1.7 wel, opnieuw updaten bracht het direct terug (dus een echte codefout, geen toeval). Oorzaak: `Arduino_Canvas` in GFX 1.3.7 past `_rotation` niet toe in zijn `writePixel`/`writeFastHLine`-overrides (pas in latere GFX-versies toegevoegd) — `tft_rotatie_toepassen()` riep `tft_p->setRotation()` aan, en zodra `tft_p` de canvas was (dubbele buffering aan) had die aanroep dus geen enkel effect op het getekende beeld, terwijl `tft_gedraaid` (en dus de losse touch-flip in `hw_touch.ino`, die dat booleantje rechtstreeks leest) wél gewoon op AAN stond. Fix: nieuwe `_tft_echt_display` (hw_scherm.ino) onthoudt het echte `Arduino_RGB_Display`-object; `tft_rotatie_toepassen()` roteert voortaan altíjd dát object, nooit de canvas. `Arduino_RGB_Display::draw16bitRGBBitmap()` past rotatie wel correct toe, dus `flush()` roteert het beeld alsnog goed bij het overzetten — de canvas zelf blijft op rotatie 0. Geen gedragsverandering als dubbele buffering uit staat (dan is het dezelfde object). (2) Een nieuwe rij (DUBBELE BUFFERING) in CONFIG → WEERGAVE & ENERGIE duwde de onderste rij (SCHERM 180° DRAAIEN) voorbij `NAV_Y` de universele navigatiebalk-tikzone in (`ts_y >= NAV_Y-8` in `hardware.ino` onderschept dat vóór het tabblad het ziet) — onbereikbaar, tikken kwam in plaats daarvan op het "BERICHT"-navigatie-icoon terecht. Brendan vroeg expliciet om een structurele oplossing i.p.v. rijen weer kleiner maken: nieuwe `cfg_we_scroll_y` + `ui_scrollbar()`/`ui_scrollbar_klik()` (bestaande componenten, al gebruikt op de PICO-instellingenpagina) op het tabblad, met swipe-scroll (`hw_touch_drag_dy`) en scrollbalk-pijltjes rechts. `CFG_WE_INHOUD_H`/`CFG_WE_MAX_SCROLL` (nieuwe macro's, zelfde aanpak als bestaande `PICO_CFG_INS_H`) moeten worden bijgewerkt als er een rij bijkomt/verdwijnt. Gecompileerd op S3 en WROOM. Beta `0.1.260826.3` |
| 200 | Sessie 37 | **Automatische schermhelderheid: dagdeel + vaarmodus**: nieuw `helderheid_auto` (default AAN) met 3 instelbare niveaus in `hw_scherm.h/.ino` — `held_dag` (standaard 100%), `held_nacht_anker` (nacht, niet varend, standaard 50%) en `held_nacht_varend` (nacht, varend: zeilen/motor, standaard 25%). `tft_helderheid_auto_loop()` berekent elke 5s (nieuwe `held_auto_ms`-timer, beide `hw_loop()`-paden zoals bestaande `licht_auto_ms`) een curve op basis van `meteo_zonsopgang`/`meteo_zonsondergang`: volledig dag van zonsopgang+1u t/m zonsondergang-1u, volledig nacht van zonsondergang+30min t/m zonsopgang-30min, daartussen lineair glijdend — omdat de functie gewoon periodiek herrekent tegen de actuele klok is die overgang vanzelf in kleine stapjes, geen aparte smoothing-logica nodig. De nacht-waarde zelf (anker vs. varend, `vaar_modus==MODE_ZEILEN\|\|MODE_MOTOR`) wordt elke aanroep vers bepaald en dus NIET gladgestreken — een vaarmodus-wissel werkt direct door, zoals gevraagd. Zonder bekende zontijden (nog geen weer opgehaald) valt de functie terug op alleen `meteo_is_dag` (geen schemer-overgang mogelijk zonder astro-data). Update van `tft_helderheid` zelf wordt NIET gepersisteerd bij elke stap (flash-slijtage bij honderden schrijfacties per dag/nacht-overgang) — alleen handmatige wijzigingen via de sliders roepen `state_save()` aan, exact zoals de bestaande knop dat al deed. UI: 1 toggle + 3 sliders (-/waarde/+, zelfde patroon als SCHERM PCLK) toegevoegd aan zowel de normale WEERGAVE & ENERGIE-tab (`CFG_WE_INHOUD_H`/scrollbar uit taak 199 bijgewerkt) als de compacte PICO-instellingenlijst (`PICO_CFG_INS_H` +120, eigen `_held_slider_pico_teken()`-helper). Gecompileerd op S3, CYD28 (SCREEN_SMALL-pad) en WROOM. Beta `0.1.260902.1` |
| 201 | Sessie 37 | **Splash/herstelmenu onbereikbaar + herontwerp trigger + dubbele-buffering-flush-gaten**: Brendan meldde dat hij het opstartlogo niet meer zag en het herstelmenu niet meer kon bereiken, plus een "fout met updaten". Hoofdoorzaak gevonden in `slaap.ino`: `_rtc_deep_wake` stond `RTC_DATA_ATTR` en werd alleen ooit op `true` gezet (`if (esp_reset_reason()==ESP_RST_DEEPSLEEP) _rtc_deep_wake=true;`), nooit terug op `false` — zodra het apparaat ÉÉN keer uit een echte deep sleep ontwaakte, bleef die vlag permanent `true` "plakken" over elke volgende ZACHTE herstart heen (OTA-herstart, watchdog, reset-knop — het RTC-domein verliest daarbij geen stroom), waardoor `splash = !slaap_was_deep_wake()` voorgoed `false` bleef: geen logo, en (aangezien `recovery_check()` daar direct binnenin zit) ook geen herstelmenu meer, blijvend, ook ná updaten. Fix: geen `RTC_DATA_ATTR` meer, `slaap_setup()` bepaalt de vlag onvoorwaardelijk elke boot vers uit `esp_reset_reason()` — die functie is zelf al betrouwbaar per boot, had nooit RTC-persistentie nodig. Herstelmenu-trigger op expliciet verzoek herontworpen (`recovery.ino`): logo blijft nu 2s zichtbaar (`RC_VENSTER_MS` 5000→2000; was via een eerdere, nooit gecommitte lokale wijziging al van 3000 naar 5000 gegaan) en een tik ÉRGENS op het scherm (niet meer alleen het middelste 1/3×1/3-vlak) opent het menu. **Los daarvan**, tweede echte gat gevonden: `_splash_teken()`/heel `recovery.ino`/`ota_download_toepassen()` draaien vóór of buiten de normale GUI-taak om (die na elke tekening automatisch `tft_flush()` aanroept) — zonder een EIGEN expliciete flush blijft al hun werk onzichtbaar in de schaduw-buffer zodra DUBBELE BUFFERING (taak 198) aan staat, zowel de splash/herstelmenu als de voortgangsbalk tijdens een gewone (niet-herstel) firmware-update. Flush toegevoegd aan `_rc_titel()`/`_rc_regel()` (dekt bijna alle herstelschermen) + losse aanroepen bij de knoppenrijen die daarbuiten vallen, aan `_splash_teken()`, en aan `ota_download_toepassen()` (initieel, snelheidsbegrensd per %-update, en bij "Klaar!"). **Bijvangst**: `recovery.ino` bevatte al enige tijd een nooit-gecommitte lokale wijziging (versie-kiezer `_rc_versie_kiezen()` met lijst van alle stabiele releases i.p.v. alleen de nieuwste, en een herbruikbare `_rc_bevestig()`-dialoog) — niet te scheiden van deze fix, dus in dezelfde commit meegenomen. PIN-invoer-kleurfeedback (derde meldpunt) kon niet reproduceren via code-inspectie — zowel het bestaande CONFIG-ontgrendel-pincodescherm als het nieuwe TIJD-instelmenu-pincodescherm tekenen na elke toets een bijgewerkte kleur-indicator; aan Brendan gevraagd op welk van de twee schermen dit gebeurde. Gecompileerd op S3 en WROOM. Beta `0.1.260902.2` |
| 202 | Sessie 37 | **Versie-bump zonder codewijziging, uitsluitend om taak 201's OTA-voortgangsbalk-fix te kunnen testen**: Brendan bevestigde dat updaten zelf werkt maar het vollopende balkje tijdens het updaten niet meer verscheen. Verklaring: die specifieke update naar `0.1.260902.2` werd nog UITGEVOERD door de OUDE (nog niet gefixte) `ota_download_toepassen()`-code — taak 201's flush-fix zit pas ván `0.1.260902.2` af in de firmware die de eerstvolgende download daadwerkelijk draait, dus kon nog niet zichtbaar zijn tijdens de update die daar zelf naartoe bracht. Deze lege bump geeft iets om nu mee te updaten en de fix in de praktijk te bevestigen. Beta `0.1.260902.3` |
| 203 | Sessie 37 | **Herstelmenu-debounce gefixt + druk-vasthoud toetsfeedback op beide PIN-schermen**: Brendan meldde dat het herstelmenu "heel moeilijk werkbaar" was en dat BKOS VERWIJDEREN niet meer werkte. Oorzaak in `recovery.ino`: elke wacht-op-tik-functie (`_rc_wacht_knop`/`_rc_wacht_lijst`) had zijn EIGEN lokale `vorige`-status die bij elke nieuwe aanroep weer op `false` begon — bleef een vinger van de vorige knop nog aangeraakt op het moment dat het volgende scherm verscheen (heel gewoon bij een fysieke tik), dan zag de eerstvolgende wacht-functie meteen `touched=true, vorige=false` en telde diezelfde aanhoudende aanraking meteen weer als een nieuwe, ongedebouncede tik — vaak met nog instabiele coördinaten, dus het menu "schoot door" of raakte de verkeerde knop. Precies het patroon dat BKOS VERWIJDEREN (VERWIJDER → bevestigingsdialoog, twee knoppen na elkaar) onbetrouwbaar zou maken. Fix: nieuwe gedeelde, doorlopende debounce-status (`_rc_tik_gedebounced()`, `RC_DEBOUNCE_MS=300`) die over schermovergangen heen blijft bestaan — een volgende tik telt pas weer zodra het scherm minstens 300ms ononderbroken losgelaten is geweest. Toegepast op `_rc_wacht_knop`/`_rc_wacht_lijst`/`recovery_check()` (die laatste triggert nog wel op de eerste aanraking, maar zaait de gedeelde status meteen goed voor het menu erna). **Tweede, apart gemeld punt**: druk-vasthoud toetsfeedback — een ingedrukte toets moet van kleur blijven zolang je 'm vasthoudt, i.p.v. een vast flitsje, zodat duidelijk is voor welke toets de aanraking geregistreerd wordt. Toegepast op de cijfertoetsen van beide PIN-schermen: het bestaande CONFIG-ontgrendel-pincodescherm (`pin_overlay_run()`, `delay(60)` vervangen door `while (ts_touched()) delay(15)`, zowel groot als PICO-scherm) en het nieuwe TIJD-instelmenu-pincodescherm (`screen_tijd.ino`, had nog helemaal geen druk-feedback — dat verklaart alsnog de eerder gemelde "geen kleurverandering" op dát scherm). Elke druk-en-wacht-plek kreeg een expliciete `tft_flush(true)` vóór de wachtlus, anders blijft de gemarkeerde toets onzichtbaar in de schaduw-buffer als dubbele buffering aanstaat (zelfde patroon als taak 201). Gecompileerd op S3 en WROOM. Beta `0.1.260902.4` |
| 204 | Sessie 37 | **Stabiele release 0.1.8 gepromoot van geteste beta 0.1.260902.4** (expliciete opdracht Brendan: "deze werkt nu goed"), via de branch-methode: `release/0.1.8` (versienummer `"0.1.8"` + tijdelijke CI-trigger voor `release/**`) → CI groen op alle 6 platforms → tag `v0.1.8` ná de CI-firmware-commits (geverifieerd: `git show v0.1.8:firmware/versie_esp32s3.txt` = `0.1.8`, raw tag-URL HTTP 200) → release-branch verwijderd. Op `main`: `versie_stable_*.txt` → `0.1.8` (alle 6 platforms), nieuwe entry in `releases.json`, en `beta_historie.json` aangevuld met alle betas van deze sessie (`0.1.260826.1-3`, `0.1.260902.1-4`) met `0.1.260902.4` gemarkeerd `"gepromoot":"0.1.8"`. De beta op `main` (`BKOS_NUI_VERSIE`) bleef ongemoeid op het iteratienummer, zoals de procedure voorschrijft |
| 205 | Sessie 37 | **`recovery_check()` debounce goed zaaien bij vasthangende aanraking**: losse fix (via `git stash` apart gehouden van de 0.1.8-promotie, zodat de stabiele release code-identiek aan de geteste beta bleef) voor Brendans melding dat "BKOS-NUI installeren" vanuit BKOS-blanco in werkelijkheid soms blanco terugzette. Vermoedelijke oorzaak: een vinger die nog op het scherm rust op het moment dat de verse BKOS-NUI-firmware opstart (net na "INSTALLEREN" in de blanco-kiezer) kon door `recovery_check()` als een bewuste eerste tik op het logo gelezen worden — `_rc_klaar_voor_tik` stond bij een verse boot standaard op `true`. De gedeelde debounce-status wordt nu bij het starten van `recovery_check()` gezaaid met de actuele aanraakstand (`_rc_touch_vorige = ts_touched(); _rc_klaar_voor_tik = !_rc_touch_vorige;`), zodat een al vasthangende aanraking als "nog vast" telt i.p.v. als nieuwe tik. Nog niet door Brendan bevestigd of dit het gemelde gedrag daadwerkelijk verhelpt. Gecompileerd op S3 en WROOM. Beta `0.1.260902.5` |
| 206 | Sessie 38 | **Opstartinstellingen vaarmodus/verlichting/AUTO + per-IO-kanaal opstartgedrag**: nieuwe `boot_vaar_modus`/`boot_licht_instelling`/`boot_vaarmodus_auto` (app_state.h/.ino, persistent `bootvm=`/`bootlt=`/`bootam=`) zijn de standaardwaarden die `state_load()` gebruikt zolang `onthoud_licht_modus` (MODUS ONTHOUDEN) uit staat — voorheen was dat hardcoded HAVEN/AUTO/uit, nu instelbaar via CONFIG → WEERGAVE & ENERGIE (3 nieuwe rijen, PIN-gated, `CFG_WE_INHOUD_H`/`CFG_WE_MAX_SCROLL` bijgewerkt). **PICO/SCREEN_SMALL had de MODUS ONTHOUDEN-toggle nooit gehad** (alleen op het grote scherm sinds sessie 34) — nu ook daar toegevoegd, samen met de 3 opstartrijen (`PICO_CFG_INS_H` +120). Per IO-uitgangskanaal (alleen UITGANG — een INGANG volgt `io_output[]` toch nooit, zie `io_drijf_hoog()`) is nu instelbaar `IO_BOOT_UIT`/`IO_BOOT_AAN`/`IO_BOOT_ONTHOUDEN` (hw_io.h/.ino, nieuwe `io_boot_gedrag[]`/`io_boot_waarde[]`, uitbreiding van het bestaande `idx:richting:alert:actie_aan:actie_uit:param`-regelformaat in `/io_cfg.csv` met 2 extra velden, achterwaarts compatibel — ontbrekende velden in oude bestanden lezen als 0/UIT). `hw_io_cfg_laden()` past het gekozen opstartgedrag meteen toe op `io_output[]` vóór de eerste cyclus; bij ONTHOUDEN scant een nieuwe periodieke check in `io_loop()` (5s, zelfde patroon als de bestaande zekeringscheck, master/standalone-only) of de actuele stand afwijkt van de laatst opgeslagen en schrijft alleen dan naar flash — geen constante wear bij elke handmatige schakeling. UI in beide IO CFG-overlays (groot scherm + PICO) als 3-knops rij, alleen zichtbaar in de UITGANG-tak (ruim voldoende vrije ruimte t.o.v. de INGANG-tak die al vol zit met actie/dynamo-instellingen). Lokaal gecompileerd op alle 4 geteste platforms (S3, WROOM, CYD28, Pico) — CYD40H/CYD40V niet apart getest (delen dezelfde codepaden). Beta `0.1.260904.1` |
| 207 | Sessie 38 | **CYD40H bleek NIET dezelfde codepad te delen als CYD28/WROOM/Pico — grote scroll/overflow-bugfix in heel CONFIG + WIFI**: Brendan meldde dat hij "nog steeds" geen scroll-knoppen in de instellingen had en niet bij lang niet alle instellingen kon komen. Root cause: `SCREEN_SMALL = PICO \|\| WROOM \|\| CYD28 \|\| CYD40V` — **CYD40H (480×320, liggend) zit daar NIET in** en hergebruikt dus de "grote-scherm" layout die overal is uitgeschreven met kale pixelwaarden op de 800×480 S3-referentie, zonder UI_SCX/UI_SCY-schaling en zonder scrollbar. Op CYD40H's 320px hoogte (NAV_Y=278 i.p.v. 438) viel daardoor structureel een groot deel van de instellingen letterlijk voorbij de navigatiebalk. Vier concrete plekken gevonden en gefixt, allemaal met dezelfde aanpak (scrollbare middenzone + `ui_scrollbar`/swipe, vaste kop/voet tekent overheen bij scroll zodat maskeren vanzelf gaat): (1) **CONFIG-hoofdtab** (`cfg_hoofd_teken`) — de 4 categorieknoppen (72px elk, hardcoded) + WiFi-rij vielen al ~150px voorbij NAV_Y; helderheidbalk blijft nu vast bovenaan (sticky), rest scrolt (`cfg_hoofd_scroll_y`, nieuwe `CFG_HOOFD_TOP`/`CFG_HOOFD_INHOUD_H`/`CFG_HOOFD_MAX_SCROLL`); ook `pan_w=500` van het BEVEILIGD-slotpaneel begrensd tot `TFT_W-40` (viel over de rand op een 480px-breed scherm). (2) **BOOT-tab** (`cfg_boot_teken`) — PANEEL-KNOPPEN viel net voorbij NAV_Y; **bijvangst**: de PANEEL-KNOPPEN-knop stond getekend op `CFG_SUB_Y0+186` maar de hittest verwachtte 'm op `CFG_SUB_Y0+226` (40px mismatch, dus de knop was op ELK platform al onaanraakbaar op de plek waar hij zichtbaar was) — nu laat de tekenfunctie dezelfde `y+50`-doortelling volgen als de hittest, plus scroll (`cfg_boot_scroll_y`/`CFG_BOOT_MAX_SCROLL`). (3) **VERBINDINGEN-tab** (`cfg_update_teken`) — stond al fractie-krap (8px marge op CYD40H, kon breken bij BRUG un-hardcoding later); nu ook scrollbaar (`cfg_update_scroll_y`/`CFG_UPDATE_MAX_SCROLL`). (4) **IO CFG-overlay** (`iocfg_overlay_teken`, groot scherm) — `OV_H` is op CYD40H maar 236px i.p.v. 396px; zowel de bestaande ALERT/ACTIE/DYNAMO-inhoud als de nieuwe STATUS-BIJ-OPSTARTEN-rij (taak 206) vielen over de OPSLAAN/SLUITEN-knoppen heen. Titel+RICHTING en OPSLAAN/SLUITEN blijven nu vast (getekend ná de scrollbare middeninhoud, dus maskeren automatisch bleed-through), middeninhoud scrolt (`iocfg_ov_scroll_y`/`iocfg_ov_max_scroll`, runtime berekend want de inhoudslengte hangt af van INGANG/UITGANG + wel/niet dynamo + wel/niet actiekanaal). (5) **WIFI-scherm** (`wifi_lijst_teken`, `WIFI_ST_IDLE`) — ernstigste bug: SCANNEN/WIFI-WISSEN stonden vast op `CONTENT_Y+238`, wat op CYD40H al vóór NAV_Y (236px beschikbaar) lag **met nul opgeslagen netwerken** — WiFi beheren was op dat platform simpelweg onmogelijk. Nu scrollbaar (`wifi_idle_scroll_y`/`wifi_idle_max_scroll`, runtime want afhankelijk van aantal opgeslagen netwerken). **Bijvangst**: `WIFI_RIJEN_N` (scanresultatenlijst) was ook hardcoded op 6 rijen (312px) — nu `(NAV_Y-WIFI_LIST_Y)/WIFI_RIJ_H`, schaalt vanzelf mee (3 op CYD40H) i.p.v. voorbij de navigatiebalk te tekenen. **Niet aangepakt**: overige schermen (PANEEL, MELDINGEN, TIJD, OTA, INFO, METEO, VICTRON, NETWERK, …) zijn niet stuk-voor-stuk nagelopen op dezelfde CYD40H-aanname-bug — aan Brendan gevraagd welk scherm concreet nog vastloopt als dit onvoldoende is, dat scheelt blind overal gaan zoeken. Lokaal gecompileerd en groen op alle 5 platforms (S3, CYD40H, WROOM, CYD28, Pico) — WROOM/CYD28/Pico-binaries byte-identiek aan vóór deze wijziging (bevestigt dat alleen de niet-SCREEN_SMALL padden zijn geraakt). Beta `0.1.260904.2` |
| 208 | Sessie 38 | **Scrollbar-knoppen te klein — te makkelijk ernaast tikken met "gekke resultaten"**: Brendan gaf direct na taak 207 aan dat de scrollbalk/pijlknoppen te klein zijn om betrouwbaar te raken. `UI_SB_W` (ui_draw.h, breedte van de hele scrollbalkkolom — bepaalt ook de tikzone-breedte in `ui_scrollbar_klik()`) van 16 → 34px, en de hoogte van de ^/v-knoppen (was een losse `const int BTN=18` in zowel `ui_scrollbar()` als `ui_scrollbar_klik()`, nu één gedeelde `UI_SB_BTN=40`-macro zodat teken en hittest nooit uit de pas kunnen lopen) van 18 → 40px. Pijl-glyphs naar textSize 2 (was 1) zodat ze bij de grotere knop passen; `btn = min(UI_SB_BTN, h/2)` voorkomt dat de twee knoppen samen de beschikbare hoogte overschrijden op een heel lage scrollstrip (kwam al voor bij de IO CFG-overlay op CYD40H, `OV_CONTENT_BOT-OV_CONTENT_TOP`≈80px). Omdat elke aanroeper (`screen_config.ino`, `screen_io_cfg.ino`, `screen_wifi.ino`, `screen_apps.ino`, `screen_meteo.ino`) al rekent met `TFT_W - UI_SB_W` i.p.v. een letterlijk breedtegetal, schaalt de rest van elk scherm automatisch mee zonder losse aanpassingen. Lokaal gecompileerd en groen op alle 5 platforms; WROOM/CYD28/Pico niet meer byte-identiek (verwacht, dit component wordt overal gedeeld). Beta `0.1.260904.3` |
| 209 | Sessie 38 | **PANEEL-knoppen max 6→9 (3×3 op groot scherm) + opstart-AUTOMODUS als vinkje bij OPSTART VAARMODUS**: twee verzoeken. (1) `PANEEL_KNOP_MAX` (paneel.h) 6→9. `_paneel_rect()` (screen_main.ino, groot scherm) generiek gemaakt: rijen = ceil(totaal/3) (max 3), binnen die rijen zo gelijk mogelijk verdeeld met het restant in de eerste rij(en) — zelfde balanceringslogica die al voor 1-6 knoppen bestond (4→[2,2], 5→[3,2], 6→[3,3]), nu ook 7→[3,2,2] 8→[3,3,2] 9→[3,3,3]. Nieuwe `DKNOP_Y3`/`INT_STATUS_Y3` (screen_main.h) — de interieur-lichtstatusbalk schuift dynamisch (op basis van `paneel_aantal()`) naar onder de 3e knoppenrij en gebruikt zijn bestaande "niet genoeg ruimte"-guard om zichzelf te verbergen als dat niet past. **screen_paneel.ino** (naam-instelscherm, 1 layout voor alle platforms, geen SCREEN_SMALL-variant) overschreed met 9 rijen + OPSLAAN zelfs de 800×480 S3-referentie (492px vs NAV_Y 438px) — scrollbaar gemaakt (`pn_scroll_y`/`pn_max_scroll`, zelfde patroon als de rest van deze sessie). **Niet aangepakt**: PICO/SCREEN_SMALL's apparaat-knoppenrij (`pico_apparaten_teken()`) blijft één rij die alle geconfigureerde knoppen over de volledige breedte perst — bij 7-9 knoppen op een 240px-scherm worden dat er zeer smalle (~20px), rechtstreeks in strijd met taak 208's "knoppen te klein"-fix; een 2-rijen-variant paste niet binnen het bestaande, al krappe verticale budget (2px marge) zonder een risicovolle herindeling van het hele PICO-hoofdscherm — aan Brendan voorgelegd of dit acceptabel is of dat de PICO-layout alsnog moet worden herzien. (2) `boot_vaarmodus_auto` kreeg een eigen "OPSTART AUTOMODUS AAN/UIT"-rij in taak 206; Brendan wilde in plaats daarvan een vinkje ín de OPSTART VAARMODUS-rij zelf. Losse rij verwijderd (CFG_WE_INHOUD_H -44), vinkje (24×24 rondje + handgetekend vinkteken van 4 lijnstukken, i.p.v. de AAN/UIT-pil-stijl die de rest van de app gebruikt — bewust een ander visueel idioom voor "vink knopje") rechts van de 4 modus-knoppen in dezelfde rij, label "OOK AUTO". Alleen op het grote scherm — PICO's `OP.VM:`-rij had letterlijk 0px vrije breedte over (4 knoppen vullen 't al helemaal), dus de aparte PICO `OP.AUTO:`-rij (ook uit taak 206) blijft daar ongewijzigd staan. Lokaal gecompileerd en groen op alle 5 platforms. Beta `0.1.260904.4` |
| 210 | Sessie 38 | **Genummerde IL-lampgroepen (individueel schakelbare lampen los van de kleurkeuze) + onafhankelijke interieurverlichting op het hoofdscherm**: Brendan wil losse lampen/lampengroepen kunnen schakelen vanaf de boordcomputer (concrete aanleiding: net 1 lamp bekabeld die nog geen fysieke schakelaar heeft) en de interieurverlichting op het hoofdscherm kunnen zetten los van de buitenverlichting. Twee samenhangende features. **(A) Genummerde lampen**: kanaalnaam `**IL_wit<N>`/`**IL_rood<N>` (N=1-99, `LAMP_MAX`) markeert een uitgang als lamp/lampgroep N — nieuwe `_il_kleur_lamp_nr()` (io.ino, static) parst de suffix (-1=ongenummerd/bestaand gedrag, 0=geen match, 1-99=lampnummer). Zo'n kanaal volgt de kleur (wit/rood) alleen als lamp N ook `lamp_aan[N]` is; ongenummerde `**IL_wit`/`**IL_rood` blijven de hoofdverlichting (zie B). Schakelen gaat via de virtuele naam `**IL_<N>` (geen fysiek kanaal) — nieuwe `io_il_lamp_nr()` (io.h/.ino, niet-static, ook gebruikt door bkos_net.ino) herkent dit patroon en wordt onderschept in `io_apparaat_staat3()`/`io_apparaat_toggle()` (de centrale doorgeefluiken die PANEEL-knoppen, de webapp én Lua's `io.wissel_naam` al gebruikten — dus alle drie werken meteen, geen aparte integratie nodig) én in bkos_net.ino's `NET_MSG_IO_NAAM`-handler (master-kant voor een gepaarde slave die zo'n knop indrukt). Nieuw `lamp.h/.ino`: `lamp_aan[]` (live), `lamp_boot_aan[]` (opstartstand), `lamp_naam[]` (max 11 tekens), `/bkos_lampen.csv`. Nieuw **LAMPEN-instellingenscherm** (`screen_lampen.h/.ino`, bereikbaar via CONFIG→BOOT→LAMPEN naast PANEEL-KNOPPEN, `CFG_BOOT_INHOUD_H` +44) — scant automatisch welke lampnummers al in de IO-configuratie voorkomen (`io_il_kanaal_lamp_nr()`) en toont per gevonden lamp naam + OPSTART AAN/UIT; scrollbaar (geen SCREEN_SMALL-variant, dus ook op het kleinste scherm), AAN/UIT-pil bewust smal (60px) gehouden zodat een volle 11-tekens naam er op 240px nog naast past. Op het hoofdscherm toont een PANEEL-knop `**IL_<N>` nu een lampje-icoon (`I_LAMP`, nieuw in `teken_icoon()`) met de ingestelde lampnaam eronder, of het kale nummer zolang er geen naam is (`_paneel_label_effectief()`) — i.p.v. het kaal-gestripte "IL_1". **(B) Interieurverlichting onafhankelijk van buitenverlichting**: nieuwe `interieur_modus` (app_state, persistent `intmodus=`, default AUTO) met UIT/WIT/ROOD/AUTO — vervangt de oude situatie waarin de interieurverlichting altijd aan stond (wit) tenzij er 's nachts gevaren werd (dan rood), zonder enige UIT-stand en volledig afhankelijk van `licht_instelling`. AUTO behoudt exact het oude gedrag (rood bij varen na zonsondergang, anders wit) maar berekend zonder `nav_licht_ok`/`licht_instelling` — pure functie van `vaar_modus` + tijd. WIT/ROOD forceren een vaste kleur, UIT dooft de hoofdverlichting (genummerde lampen blijven onafhankelijk schakelbaar — een losse lamp mag aan blijven ook als de hoofdverlichting uit staat, ze delen alleen de kleur). Bediening: de bestaande `interieur_status_teken()`-statusbalk (hoofdscherm, groot scherm) is nu aantikbaar (linkerdeel, rechterdeel blijft het Victron-ministatusje) en cyclet UIT→WIT→ROOD→AUTO→UIT — hergebruik van een bestaand widget i.p.v. een nieuwe knoppenrij, dus geen extra schermruimte nodig. **Niet aangepakt**: PICO/SCREEN_SMALL heeft geen equivalent van dit statuswidget (rechterkolom zit al bijna vol, ~2px marge) — interieurverlichting is op die platforms dus nog niet vanaf het hoofdscherm te bedienen; aan Brendan gemeld als bekende beperking. Lokaal gecompileerd en groen op alle 5 platforms. Beta `0.1.260904.5` |
| 211 | Sessie 38 | **Lampje-icoon herontworpen (peertje + kleurstralen) + OPSLAAN-knop PANEEL/LAMPEN vast onderaan (was onbereikbaar/overlapte)**: drieledige feedback. (1) Het lampje-icoon (taak 210) vond Brendan niet mooi — nieuwe `teken_icoon_lamp()` (screen_main.ino, losstaand van de generieke `teken_icoon()` omdat die maar één kleurparameter heeft en dit icoon de ACTUELE verlichtingskleur moet tonen): een peervormige bol die bij "aan" gevuld oplicht in wit of rood (nieuwe globale `interieur_kleur_rood`, io.h/.ino, gezet door `io_verlichting_update()`) met 5 lichtstralen over de bovenste helft, bij "uit" alleen een omtrek in dimgrijs — i.p.v. het vorige omtrek+kruis-filament-icoon in de generieke aan/uit-cyaan-kleur. (2) **Echte bug gevonden**: de OPSLAAN-knop op zowel het PANEEL-scherm als het nieuwe LAMPEN-scherm stond ALS LAATSTE RIJ in de scrollende lijst — bij PANEEL_KNOP_MAX=9 (taak 209) viel die knop daardoor standaard (scroll=0) al ~60px voorbij NAV_Y, in de universele navbar-tikzone (`hardware.ino`'s `ts_y >= NAV_Y-8` onderschept dat vóór het scherm het ziet) — precies Brendans "moet net wat hoger" melding. Op LAMPEN kwam daar nog een tweede bug bovenop: bij 0 gevonden lampen stond de (nutteloze) OPSLAAN-knop bovenop/over de lege-staat-uitleg getekend. Beide schermen herontworpen naar het patroon dat de IO CFG-overlay al gebruikte: OPSLAAN staat nu VAST net boven de navbar (`PN_OPSLAAN_Y`/`LP_OPSLAAN_Y` = `NAV_Y - knophoogte - 8`, dus altijd bereikbaar ongeacht scroll of aantal rijen), de rijenlijst scrolt binnen een vast kijkvenster erboven (`PN_LIST_BOT`/`LP_LIST_BOT`) met een clip-check in de tekenlus (rijen buiten het venster worden niet eens getekend, i.p.v. op te lossen door de vaste voet ze te laten overschilderen) en de tikafhandeling test nu expliciet tegen de vaste OPSLAAN-rechthoek i.p.v. een rij-index-berekening die toevallig op de oude vaste positie uitkwam. (3) **PANEEL-knoppen die "niet onthouden werden" bij een update**: geen reproduceerbare bug gevonden in `paneel_laden()`/`paneel_opslaan()` zelf, en `ota_download_toepassen()`'s `Update.begin()` gebruikt (zonder tweede argument) standaard `U_FLASH` — een gewone firmware-OTA raakt de SPIFFS-partitie (waar `/bkos_paneel.csv` staat) dus niet aan. Waarschijnlijkste verklaring: de al bekende en in `0.1.260902.5` (taak 205) aangepakte herstelmenu-debounce-bug, waarbij een nog vasthangende aanraking na "BKOS-NUI installeren" vanuit BKOS-blanco per ongeluk als tik op het opstartlogo gelezen kon worden en zo alsnog blanco terugzette (blanco heeft uiteraard geen PANEEL-knoppen ingesteld) — dat zou het gemelde symptoom exact verklaren, maar is destijds niet door Brendan bevestigd als verholpen. Eén reëel restrisico blijft staan en is niet aangepast (te risicovol om blind te wijzigen): `platform_fs.h`'s `SPIFFS.begin(true)` — `formatOnFail=true` — formatteert (wist) de hele SPIFFS-partitie stilzwijgend als mounten ooit mislukt; dat is nodig voor een fabrieksverse/ongeformatteerde plaat, maar zou op een al lang lopend apparaat evengoed alles wissen bij een zeldzame mount-fout. Geen automatische fix hiervoor gemaakt zonder hardware om te verifiëren — aan Brendan gevraagd te melden of dit zich op de huidige firmware herhaalt. Lokaal gecompileerd en groen op alle 5 platforms. Beta `0.1.260905.1` |
| 212 | Sessie 38 | **BETA INSTALLEREN toegevoegd aan het herstelmenu**: vierde knop naast OPSTARTEN/VERWIJDER/UPDATE. Anders dan UPDATE (`_rc_versie_kiezen()`, lijst van alle stabiele releases uit `releases.json`) bestaat er voor beta geen historie van individueel terug te halen binaries — elke CI-build overschrijft dezelfde URL — dus nieuwe `_rc_beta_installeren()` haalt gewoon de nieuwste betaversie op (`ota_git_check()` met `ota_beta_kanal` tijdelijk op `true` gezet en na afloop teruggezet, zodat de normale kanaalvoorkeur van de gebruiker niet verandert) en flasht die na bevestiging, zelfde opzet als `_rc_verwijderen()`. **Bijgevangen bug tijdens het bouwen**: `_rc_wacht_knop()` gebruikte intern altijd de globale `RC_BTN_GAP`-constante i.p.v. een parameter — met 4 in plaats van 3 hoofdmenuknoppen (nieuwe, kleinere `RC_MENU_BTN_H`/`RC_MENU_GAP` nodig om te passen) zou knop 2-4's tikzone daardoor exact dezelfde soort mismatch-bug hebben gekregen als zonet gevonden bij PANEEL/LAMPEN's OPSLAAN-knop (taak 211) — nu een `gap`-parameter (default `RC_BTN_GAP`, dus de bestaande 1-2-knops dialogen `_rc_bevestig()`/`_rc_melding_terug()` ongewijzigd) zodat tekenen en hittest altijd dezelfde tussenruimte gebruiken. Lokaal gecompileerd en groen op alle 5 platforms. Beta `0.1.260905.2` |
| 213 | Sessie 38 | **PANEEL/LAMPEN-opslaan verifieert nu daadwerkelijk of het gelukt is**: Brendan meldde dat aanpassingen aan PANEEL-knoppen bij hem al bij een gewone herstart verloren gaan. Code-review van `paneel_laden()`/`paneel_opslaan()` (en de OPSLAAN-knop-hittest, net gefixt in taak 211) laat geen bug zien — beide zijn symmetrisch en eenvoudig, en `ota_download_toepassen()` raakt de SPIFFS-partitie niet aan (blijft dus ook bij een gewone OTA-update intact). Kon het dus niet reproduceren via code-inspectie alleen. Wel gevonden: `paneel_opslaan()`/`lamp_opslaan()` gaven nooit terug of de schrijfactie ook echt lukte — een mislukte write (bv. een volle SPIFFS-partitie, wat met 240 IO-kanaalnamen + IO CFG + lampdata + Lua-apps + meldingen niet ondenkbaar is op de 2MB-partitie) toonde gewoon dezelfde groene "Opgeslagen"-melding als een geslaagde. Beide functies retourneren nu `bool`: na het schrijven wordt het bestand meteen teruggelezen en byte-voor-byte vergeleken met wat er in het geheugen staat (vangt ook een write die qua bytenaantal klopte maar er na sluiten toch niet goed op flash bleek te staan). Bij een mislukte save tonen beide schermen 4s een rode balk i.p.v. de groene "Opgeslagen" — op ESP32-platforms met het aantal vrije bytes erbij (`SPIFFS.totalBytes()-usedBytes()`; op Pico/LittleFS bestaat die aanroep niet op het gedeelde `fs::FS`-type, dus daar alleen "opslag vol?" zonder getal). Dit legt een eventuele oorzaak nu bloot i.p.v. hem te verbergen, maar is GEEN bevestigde fix voor het gemelde verlies — als het probleem echt bij "gewoon herstarten" zit (niet bij een OTA of iets bijzonders), dan zal deze verificatie dat de eerstvolgende keer laten zien; gevraagd aan Brendan of de rode melding verschijnt wanneer het weer misgaat. Toegelicht waarom er niet is overgestapt op de Arduino EEPROM-library/NVS: dit project deed dat eerder bewust weg bij IO-kanaalnamen (NVS-namespacelimiet van 126 entries brak bij 240 kanalen, zie project-geheugen) — SPIFFS-bestanden zijn hier het bewust gekozen, robuustere mechanisme. Behoud over een volledig andere firmware heen (bv. BKOS-blanco) is expliciet niet toegezegd: blanco is bewust een schone lei zonder BKOS-NUI-configuratie. Lokaal gecompileerd en groen op alle 5 platforms (de Pico-build faalde initieel op `SPIFFS.totalBytes()/usedBytes()` — niet beschikbaar op het generieke `fs::FS`-type — opgelost met een `#if PLATFORM_PICO`-tak). Beta `0.1.260905.3` |
| 214 | Sessie 39 | **Eigen kleurpatroon (PALETTE_CUSTOM)**: Brendan was niet tevreden met alleen de 7 vaste kleurenschema's — nieuw 8e schema "EIGEN" waarvan alle 10 kleuren (achtergrond/oppervlak 1-3/statusbalk/tekst/tekst gedimd/tekst donker/grijs/accent — zelf ingedeeld, geen expliciete lijst gekregen) handmatig instelbaar zijn. Datamodel: `Palette`-struct verplaatst van `ui_colors.ino` naar `ui_colors.h` (nodig buiten dat bestand), nieuwe `custom_palette`-global + `PALETTE_CUSTOM`/`PALETTE_CNT=8`; `_palet_voor()` leest `custom_palette` voor dat schema, anders de bestaande vaste rij. Persistent via 10 nieuwe `cpXX=`-regels in `bkos_config.csv` (`app_state.ino`), default = kopie van NYMBUS (`custom_palette_reset_default()`) zolang niets opgeslagen is. Nieuw scherm `SCREEN_KLEUR`/`screen_kleur.h/.ino`: lijst van de 10 velden (sticky-footer scrollpatroon, zelfde als LAMPEN/PANEEL) met een live kleurvoorbeeld per rij; een rij aantikken opent een kiezer-overlay met 20 voorgedefinieerde kleuren (grid, ook scrollbaar) plus een "HANDMATIG (HEX/RGB)"-knop die het bestaande config-toetsenbord hergebruikt voor vrije invoer (`#RRGGBB`, `RRGGBB` of `R,G,B` — `_kl_parse_kleur()`). Elke wijziging past meteen live toe (`palette_toepassen(PALETTE_CUSTOM)`, alleen zichtbaar als dat schema actief is) zonder te schrijven; OPSLAAN op de hoofdlijst persisteert via `state_save()`. Toegang tot "EIGEN KLEURPATROON BEWERKEN" via CONFIG → WEERGAVE & ENERGIE (PIN-gated, ook PICO-variant "EIGEN KLEUR"). **Bijgevangen bug**: het bestaande 7-kleuren swatch-rijtje in `screen_config.ino` had een hardcoded `sw=95px` breedte — een 8e swatch ("EIGEN") erbij had de rij op de S3-referentie (800px) laten overlopen (`80+8×101=888px`); breedte nu dynamisch berekend uit `PALETTE_CNT`, en de bijbehorende (los onderhouden) tik-hittest in `cfg_we_run()` gelijkgetrokken naar dezelfde formule — anders was dat weer een "getekend hier, hittest daar"-mismatch geworden zoals eerder deze sessie meermaals gevonden. `hardware.ino`'s 4 dispatch-plekken (2× teken, 2× run, ESP32 GUI-taak + `hw_loop()`) één-voor-één met losse `Edit`-aanroepen bijgewerkt i.p.v. een blanket replace (een eerdere blanket-replace op ditzelfde bestand voor `SCREEN_LAMPEN` had eerder deze sessie dubbele/verkeerd-ingesprongen regels opgeleverd). Lokaal gecompileerd en groen op alle 6 platforms (S3, CYD40H, CYD28, WROOM, CYD40V, Pico). Beta `0.1.260906.1` |
| 215 | Sessie 39 | **Stabiele release 0.2.0 gepromoot van geteste beta 0.1.260906.2** (expliciete opdracht Brendan, incl. de MINOR-bump naar 0.2 i.p.v. een patch binnen 0.1.x, vanwege de omvang van het werk sinds 0.1.8), via de branch-methode: `release/0.2.0` → CI groen op alle 6 platforms → tag `v0.2.0` ná de CI-firmware-commits (geverifieerd: `git show v0.2.0:firmware/versie_esp32s3.txt` = `0.2.0`, raw tag-URL HTTP 200) → release-branch verwijderd. `versie_stable_*.txt` → `0.2.0` (alle 6 platforms), nieuwe entry in `releases.json`, `beta_historie.json` bijgewerkt (`0.1.260906.2` → `gepromoot: 0.2.0`). **CI-onderweg**: de eerste build-poging voor `0.1.260906.1` faalde op `build_cyd28` in de "Install ESP32 platform"-stap — een transiënte GitHub-infrastructuurfout (Espressif package-index), niet code-gerelateerd (lokaal compileerden alle 6 platforms probleemloos, en 5/6 CI-jobs slaagden gewoon); opgelost met een no-codewijziging versie-bump naar `0.1.260906.2` om een schone CI-run te forceren (zelfde aanpak als taak 202). **Nieuw `CHANGELOG.md`** (Keep a Changelog-conventie): overzicht per stabiele release (0.1.0 t/m 0.2.0) van toevoegingen/wijzigingen/oplossingen, plus een "Bekende problemen"-tabel (probleem, aanwezig-sinds-stabiele-versie, verholpen-in-stabiele-versie). Brendan corrigeerde tijdens het opstellen: alleen problemen die daadwerkelijk in een uitgebrachte stabiele versie zaten zijn relevant, getrackt op stabiel versienummer — nooit op beta-buildnummer (vastgelegd in geheugen, zie `feedback_changelog_stabiele_versies`). Herverificatie tegen git log leverde daarbij twee echte attributiefouten op uit het eerste concept (de `**motor`-dynamofix en de vermoedelijke BKOS-Blanco-terugvalfix landden allebei ná de betabuild waarvan hun "eigen" stabiele versie was gepromoot, en horen dus bij de eerstvolgende stabiele versie) en één item dat nooit een stabiele release heeft geraakt (korte ESP32-core-3.x-regressie eind mei, binnen de betafase zelf al teruggedraaid) — verwijderd uit de tabel. **Hernummerd van 0.2.1 naar 0.2.0** op verzoek van Brendan (consistentie met het bestaande 0.1.0 — elke MINOR-serie begint bij patch 0) vlak na de eerste promotie: tag `v0.2.1` en de bijbehorende `release/0.2.1`-firmware verwijderd, promotieprocedure herhaald onder `0.2.0` |
| 216 | Sessie 40 | **HAVEN-dashboard**: nieuw "Google Home"-achtig bedieningsscherm (`SCREEN_HAVEN`/`screen_haven.h/.ino`) puur voor gebruik in de haven — tegel-grid (2-4 kolommen, schaalt met schermbreedte, scrollbaar) met: interieurverlichting (UIT/WIT/ROOD/AUTO-cyclus), alle genummerde IL-lampgroepen (auto-scan, zelfde als LAMPEN-scherm) en alle PANEEL-apparaten — met uitzondering van PANEEL-knoppen die zelf al een IL-lampgroep zijn (voorkomt dubbele tegels). Tegel-tekenprimitieven (`teken_icoon_lamp`, `paneel_icoon`, `paneel_knop_teken`) uit `screen_main.ino` los gemaakt van `static` en gedeeld via `screen_main.h`, zodat beide schermen exact dezelfde visuele taal gebruiken i.p.v. duplicatie. **Ingang**: lang indrukken (700ms, bestaand `LANG_DRUK_MS`-mechanisme) op de HAVEN- of ANKER-vaarmodusknop op het hoofdscherm opent het dashboard — `screen_main_lang_indruk()` uitgebreid met een vaarmodus-knop-hittest naast de al bestaande AUTO-licht-overlay-check, voor zowel het grote scherm als SCREEN_SMALL/PICO (die had nog geen enkele lang-indruk-functionaliteit). **Automatische navigatie**: in `io_actie_uitvoeren()` (io.ino, het exclusieve pad voor door `vaarmodus_auto`/een IO-actie getriggerde modus-wissels, losstaand van handmatige knop-taps) opent een automatische overgang naar HAVEN het dashboard altijd, en springt een automatische overgang naar ZEILEN/MOTOR terug naar `SCREEN_MAIN` — maar alléén als het HAVEN-dashboard op dat moment actief was (andere schermen als CONFIG/METEO worden niet weggekaapt). ANKER krijgt bewust alleen de handmatige lang-indruk-ingang, geen automatische dashboard-opening (expliciet zo gevraagd). **Bijgevangen bug**: `dev_lokaal[6]` (fallback-apparaatstatus zonder IO-hardware) was al sinds `PANEEL_KNOP_MAX` van 6 naar 9 ging (taak 209) een out-of-bounds read/write voor PANEEL-slots 7-9 — nu `dev_lokaal[PANEEL_KNOP_MAX]`. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.1` |
| 217 | Sessie 40 | **Lang-indrukken werkte al die tijd niet betrouwbaar — echte hoofdoorzaak in het touch-mechanisme zelf**: Brendan meldde direct dat het nieuwe HAVEN-dashboard niet opende, en (cruciale extra info) dat lang indrukken op de AUTO-knop `vaarmodus_auto` herhaaldelijk aan/uit liet klikken zonder ooit het bestaande offset-menu te openen. Dat isoleerde het probleem tot het gedeelde `LANG_DRUK_MS`-mechanisme in `hardware.ino` zelf, niet tot de nieuwe HAVEN-code. Root cause: `ts_touched()` (GT911 op S3) rapporteert tijdens een langere, stilliggende aanraking af en toe kortstondig `false` (sensorruis, geen echte loslating) — en de sessie-boekhouding (`touch_verwerkt`/`lang_druk_verwerkt`/`vorige_touch`) reset zichzelf tot nu toe op ELKE zo'n dropout. Gevolg: `touch_start_ms` (voor de 700ms-lang-indruk-klok) resette continu vóórdat de drempel gehaald werd, én `touch_verwerkt` werd tussentijds telkens vrijgegeven waardoor de normale korte-tik-afhandeling herhaaldelijk opnieuw vuurde tijdens één doorlopende aanraking — exact het waargenomen "AUTO klikt aan/uit"-gedrag. Fix: nieuwe flicker-tolerante `aanraking_vast` (`TOUCH_FLICKER_MS`=80ms — dropouts korter dan die marge tellen niet als loslaten) vervangt de ruwe `aanraking` uitsluitend in de reset-condities (nieuwe-aanraking-detectie, `vorige_touch`-update, de twee `touch_verwerkt=false`-resets, `lang_druk_verwerkt`-reset, de lang-indruk-vuurconditie) in zowel `_gui_taak()` (ESP32) als het Pico-pad in `hw_loop()`; de eigenlijke korte-tik-dispatch en alle `screen_X_run()`-aanroepen blijven bewust op de ruwe `aanraking` gebaseerd (geen wijziging in hun al jarenlang beproefde gedrag). Dit is een pre-existing bug die niet door de HAVEN-feature is geïntroduceerd — lang indrukken heeft vermoedelijk nooit betrouwbaar gewerkt sinds de AUTO-knop-overlay (taak 109) geïntroduceerd werd, gewoon nooit eerder opgemerkt omdat kortstondige tikken zelden lang genoeg vastgehouden worden om de flakiness zichtbaar te maken. Lokaal gecompileerd en groen op alle 6 platforms (incl. Pico, waar de handmatig herschreven `#else`-tak zit). Beta `0.2.260909.2` |
| 218 | Sessie 40 | **HAVEN-dashboard verfijnd na eerste feedbackronde** (Brendan, nu wél succesvol getest): (1) De volle-breedte "HAVEN"-titelbalk bovenin verwijderd ("verspilling van ruimte") — tegels beginnen nu direct onder een klein "<"-terugknopje linksboven (48×32px, i.p.v. de mega "TERUG NAAR VAARDASHBOARD"-balk onderaan). (2) Logische groepering: tegels nu onder sectietitels "VERLICHTING" (interieurverlichting + alle genummerde lampgroepen, ook de niet-op-PANEEL-staande) en "APPARATEN" (overige PANEEL-knoppen) — `screen_haven.ino`'s teken/run-logica herschreven naar een sectie-gebaseerd grid (`_hv_sectie_grid_teken`/`_hv_grid_hit`) i.p.v. één platte tegellijst. (3) **Nav bar "thuis" (PANEEL-knop) onthoudt nu HAVEN vs. MAIN**: nieuwe `laatste_hoofdscherm` (app_state, bijgewerkt in beide teken-dispatchpunten in `hardware.ino` zodra `actief_scherm` MAIN of HAVEN is) — `nav_bar_klik()`'s PANEEL-tikzone en de actief-highlight in `nav_bar.ino` gebruiken die waarde i.p.v. hardcoded `SCREEN_MAIN`, zodat door de andere navbar-tabs bladeren en terug op PANEEL tikken terugbrengt naar welk van de twee "thuis"-schermen je het laatst had. `_pnb_knop`/`_sys_knop`-lambda's in nav_bar.ino omgezet van scherm-ID-parameter naar een door de aanroeper berekende bool, zodat de PANEEL-knop op twee schermen tegelijk "actief" kan zijn zonder de andere knoppen te raken. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.3` |
| 219 | Sessie 40 | **HAVEN-dashboard tweede feedbackronde — grote herstructurering + achtergrondfoto**: (1) Terugknopje verplaatst van linksboven-in-content naar de statusbalk zelf (`sb_scherm_teken("HAVEN", C_CYAN)` + eigen "< TERUG"/"<TERUG" ernaast bij `SB_KLOK_X`, exact het bestaande WIFI/INFO/TIJD-patroon — kost daardoor geen content-ruimte meer). `hardware.ino`'s klok-tik-zone-uitzonderingslijst (2 plekken) uitgebreid met `SCREEN_HAVEN`. (2) VERLICHTING en APPARATEN nu **naast elkaar** (2 kolommen) i.p.v. onder elkaar — `_hv_layout()` werkt nu per kolombreedte (1-2 subkolommen per helft i.p.v. 1-4 over de volle breedte). (3) VERLICHTING-kolom herontworpen: bovenaan "ALGEMEEN" met losse WIT/ROOD-knoppen (sturen `interieur_modus` direct, tikken op de al-actieve kleur zet 'm UIT) + "ALLES AAN"/"ALLES UIT" voor de genummerde lampgroepen in één keer; PANEEL-knoppen met "dek" in de naam (`_hv_is_licht_naam()`, zelfde substring-herkenning als `paneel_icoon()`'s I_DEKLICHT) tellen nu ook als verlichting i.p.v. apparaat. (4) **Echte correctheidsfix**: een lampgroep-tegel toonde altijd `lamp_aan[N]` als "aan", ook als de kleurmodus geen bijpassend kanaal voor die lamp aanstuurt (bv. alleen `**IL_wit1` geconfigureerd, kleur op rood — kanaal staat dan terecht UIT via `io_verlichting_update()`, maar de tegel bleef 'm als aan tonen). Nieuwe `io_lamp_effectief_aan(nr)` (io.h/.ino) scant `io_output[]` i.p.v. de kleurlogica te herhalen, dus altijd in sync met wat er echt aangestuurd wordt; de HAVEN-lamptegel gebruikt 'm nu i.p.v. het rauwe `lamp_aan[]`. (5) **Achtergrondfoto/slideshow** (fase 1, "knoppen overheen, later mooier"): 3 royalty-vrije zeilfoto's (Unsplash-licentie) gedownload, met PIL naar 800×480 gecropt en zwaar gecomprimeerd (JPEG q45, 18-41KB per stuk — het scherm heeft toch beperkte resolutie), ingebakken als PROGMEM-byte-arrays in nieuw gegenereerd `haven_fotos.h`. Nieuw `haven_achtergrond.h/.ino`: decodeert via de nieuwe dependency `TJpg_Decoder` (Bodmer, MCU-blok-streaming, geen volledige framebuffer nodig — ~3-9KB workspace, dus ook veilig op de niet-PSRAM-platforms WROOM/CYD28/CYD40H/CYD40V) rechtstreeks vanuit het array (`TJpgDec.drawJpg`), downschaalfactor 1/2/4 o.b.v. schermbreedte, gecentreerd met C_BG-letterbox. Langzame rotatie (60s) via `haven_achtergrond_tick()`, aangeroepen vanuit de bestaande periodieke niet-aangeraakt-lus in `hardware.ino` (nieuwe `SCREEN_HAVEN`-case in de `screen_X_run(0,0,false)`-switch). `TJpg_Decoder` toegevoegd aan alle 6 CI-jobs in `build.yml`. Nog NIET gebouwd: een echte upload-mogelijkheid (SPIFFS/SD) voor eigen foto's — bewust uitgesteld tot een latere stap. Lokaal gecompileerd en groen op alle 6 platforms (flash-gebruik S3 61%, ruim voldoende marge). Beta `0.2.260909.4` |
| 220 | Sessie 40 | **HAVEN-dashboard derde feedbackronde — betere achtergrondfoto + symboolknoppen**: (1) Brendan vond de kleuren van de vorige achtergrondfoto's lelijk; oorzaak: RGB565 (16-bit, dit scherm) laat gevoelige hemel/water-verlopen banderen bij een simpele bit-truncatie. Nieuwe aanpak: elke foto krijgt eerst een Floyd-Steinberg-dithering naar het exacte RGB565-rooster (Python/PIL, per-pixel foutdiffusie) vóórdat 'ie als JPEG wordt opgeslagen — de ruis maskeert bandvorming, en omdat de kleuren al op het 565-rooster liggen voegt `TJpg_Decoder`'s eigen (ongeditherde) afronding geen nieuwe fouten meer toe. Kwaliteit ook verhoogd van q45 naar q65 (dithering bleek nauwelijks bestandsgrootte te kosten: ±37-54KB per foto, nog steeds ruim binnen budget). Foto-set vervangen: `haven_foto_1` is nu een door Brendan aangeleverde foto (traditionele zeilboot "BU 89", felle blauwe lucht — inherent minder bandinggevoelig dan de vorige zonsondergangfoto's), de andere 2 zijn de bestaande foto's opnieuw verwerkt met dezelfde betere pijplijn. (2) WIT/ROOD/ALLES AAN/ALLES UIT van tekstknoppen (`ui_knop`) naar symbolen: WIT/ROOD hergebruiken het peertje-icoon (`teken_icoon_lamp`, licht op in eigen kleur bij actief), ALLES AAN/UIT hergebruiken de bestaande verlichtingsiconen `I_LICHT_AAN`/`I_LICHT_UIT`. Daarvoor `teken_icoon()` en zijn `I_*`-defines (voorheen file-local `static` in screen_main.ino) net als de eerdere tegel-primitieven losgemaakt en gedeeld via `screen_main.h`. Nieuwe `_hv_kleur_knop()`/`_hv_actie_knop()`-helpers in screen_haven.ino — puur een teken-wijziging, de tik-hittest (dezelfde rects via `HV_ALG_ROW1_Y`/`HV_ALG_ROW2_Y`) is ongewijzigd. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.5` |
| 221 | Sessie 40 | **HAVEN-dashboard vierde feedbackronde — echte kleurenbug gevonden + 4 vierkante symboolknoppen**: (1) Brendan's terugkoppeling ("de kleuren lijken niet eens op 16-bit vertaalde versies van het origineel") wees niet op ditherkwaliteit maar op een echte bug: `TJpgDec.setSwapBytes(true)` liet de tjpgd-decoder (`haven_achtergrond.ino`) de R/G/B-bits van elk RGB565-pixel omwisselen vóórdat `Arduino_GFX::draw16bitRGBBitmap()`/`writePixel()` het kreeg — die verwachten juist een GEWONE (niet byte-swapped) RGB565-waarde, exact het formaat dat het `RGB565()`-macro in ui_colors.h overal elders in de app al produceert. Gevolg: totaal verminkte kleuren, los van de eerder aangepakte bandingkwestie. Fix: `setSwapBytes(false)`. Eén regel, maar de daadwerkelijke hoofdoorzaak van de "lelijke kleuren"-klacht — de dithering uit taak 220 was inhoudelijk al correct, kwam alleen nooit goed op het scherm terecht. (2) WIT/ROOD/ALLES AAN/ALLES UIT herontworpen naar Brendans exacte wens: 4 vierkante symboolknoppen op één rij (i.p.v. 2×2 met tekstlabels), WIT/ROOD als hecht paartje en ALLES AAN/UIT als hecht paartje met een grotere kloof tussen de twee paartjes (`HV_ALG_GAP_KL`=8px binnen een paar, `HV_ALG_GAP_GR`=24px ertussen), geen tekstlabels meer. Nieuwe schaalbare (niet vast-16px zoals de gedeelde `teken_icoon`/`teken_icoon_lamp`) tekenroutines specifiek voor deze context: `_hv_peertje()` (WIT/ROOD — in tegenstelling tot `teken_icoon_lamp` blijft de omtrek ALTIJD in de eigen kleur staan, ook uit, zodat de knoppen zonder label meteen herkenbaar zijn) en `_hv_aan_symbool()`/`_hv_uit_symbool()` (opgeschaalde varianten van I_LICHT_AAN/I_LICHT_UIT). Layout-macro's (`HV_ALG_SQ`/`_hv_alg_layout()`) gedeeld tussen teken() en run() zoals gebruikelijk in dit bestand. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.6` |
| 222 | Sessie 40 | **HAVEN-dashboard vijfde feedbackronde — gerichte hertekening i.p.v. volledige (dure) herbouw per tik**: Brendan meldde dat elke tik het hele scherm opnieuw tekende, inclusief de JPEG-decode van de achtergrondfoto — merkbaar traag. `screen_haven_teken()` (met `haven_achtergrond_teken()` erin) werd tot dan toe na ELKE actie aangeroepen. Herstructurering: nieuwe gedeelde `_hv_tegel_rect()` (tegel-positie uit index+grid, al eerder impliciet 3× los uitgeschreven) en twee nieuwe los-aanroepbare deel-tekenfuncties, `_hv_redraw_algemeen()` en `_hv_redraw_verlicht_grid()`, uit de bestaande tekenlogica geëxtraheerd (geen gedragswijziging, alleen herbruikbaar gemaakt). De tik-handlers in `screen_haven_run()` roepen nu per actie alleen het strikt noodzakelijke opnieuw aan: één lamp/apparaat-tegel tikken → alleen die ene tegel; WIT/ROOD tikken → de ALGEMEEN-rij + het hele lampgrid (kleurmodus beïnvloedt de tint van élke "aan" lamptegel, dus dat moet wel mee); ALLES AAN/UIT → alleen het lampgrid. Scrollen/swipen blijft bewust een volledige `screen_haven_teken()` (tegelposities verschuiven t.o.v. de vaste foto, dat vereist toch een volle herbouw). De foto zelf wordt dus alleen nog getekend bij het openen van het scherm en bij de 60s-slideshowwissel, niet meer bij gewone tegel-tikken. Overwogen maar bewust niet gebouwd: een losstaand "alleen de klok/statusbalk verversen"-mechanisme — geen enkel ander scherm in de app heeft een live tikkende klok tijdens stilzitten (die ververst overal alleen mee bij een toch al noodzakelijke volledige hertekening), dus dat zou een nieuw, elders niet bestaand patroon zijn geweest, buiten de scope van de gemelde klacht. Los geverifieerd op Brendans verzoek: de WIT/ROOD-tik in dit scherm raakt `vaarmodus_auto` nergens (niet direct, en `io_verlichting_update()`/`net_app_staat_sturen()`/`state_save()` lezen/schrijven alleen `interieur_modus`/`vaar_modus`/`licht_instelling`) — geen aanpassing nodig, al bevestigd veilig. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.7` |
| 223 | Sessie 40 | **HAVEN WIT/ROOD blijft AUTO op het vaardashboard — kleuroverrule i.p.v. modus-wissel**: Brendan wilde dat WIT/ROOD in HAVEN niet langer `interieur_modus` naar WIT/ROOD zet (waardoor het vaardashboard "INT: AUTO" verloor) maar AUTO blijft tonen, met alleen tijdelijk een handmatig gekozen kleur totdat de vaarmodus wijzigt — zowel bij een handmatige als een automatische wissel. Nieuw in io.h/.ino: `interieur_kleur_overrulen(bool rood)` zet `interieur_modus` altijd naar `INTERIEUR_AUTO` (nooit naar WIT/ROOD zelf) en onthoudt de gewenste kleur + de `vaar_modus` van dat moment in nieuwe statics `_int_overrule`/`_int_overrule_vaarmodus`; nogmaals dezelfde kleur tikken heft de overrule op (terug naar echte auto). In `io_verlichting_update()` overrult dit uitsluitend de berekende `auto_rood`-kleur (nooit `int_aan` — de interieurverlichting staat onder AUTO toch altijd aan, alleen de kleur varieert) en vervalt vanzelf zodra `vaar_modus` afwijkt van de vastgelegde waarde óf `interieur_modus` niet langer AUTO is (bv. via het hoofdscherm-cyclusknopje) — dat laatste voorkomt dat een oude overrule stilletjes weer actief wordt als de gebruiker later handmatig terug naar AUTO cyclet. Nieuwe getter `interieur_overrule_kleur()` (-1/0/1) vervangt in `screen_haven.ino`'s `_hv_redraw_algemeen()` de oude `interieur_modus`-vergelijking voor de WIT/ROOD-actief-highlight: toont nu specifiek "is dít een bewuste HAVEN-keuze", niet toevallig de actuele auto-kleur. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.8` |
| 224 | Sessie 40 | **HAVEN-dashboard zesde feedbackronde — doorschijnende tegels over de achtergrondfoto**: Brendan wilde de tegels "een beetje transparant" over de foto zien, zonder bij elke tik de dure JPEG-decode opnieuw te draaien. Aanpak: bij elke volle `screen_haven_teken()` worden de middelpunten van alle 4 ALGEMEEN-knoppen en alle VERLICHTING/APPARATEN-tegels (tot `HV_BG_CAP`=20 per grid) vooraf verzameld en doorgegeven aan `haven_achtergrond_samples_zet()` (nieuw in `haven_achtergrond.h/.ino`); de bestaande `TJpg_Decoder`-callback (`_hab_output`) pikt onderweg, tijdens het toch al lopende decoderen, de fotokleur op elk geregistreerd punt op (`haven_achtergrond_sample(i)`) — geen aparte decodeerpas nodig. Die kleuren worden gecachet in nieuwe statics `hv_bg_algemeen[4]`/`hv_bg_verlicht[]`/`hv_bg_paneel[]` (screen_haven.ino) en pas ververst bij de eerstvolgende volledige hertekening (schermopen, scroll, of de 60s-slideshowwissel) — precies wanneer de foto zelf ook verandert of van positie wisselt. Elke tegel mengt zijn gecachete fotokleur met de normale oppervlaktekleur via een nieuwe RGB565 5/6/5-blendfunctie (`_hv_blend()`, `HV_TILE_ALPHA`=190 ≈ 75% dekkend) in `_hv_tile_frame()`, die nu een extra `bg_foto`-parameter neemt; alle bestaande aanroepen (ALGEMEEN, lamptegels, PANEEL/APPARATEN-tegels, zowel in de volledige tekenpaden als de losse tik-hertekeningen uit taak 222) geven voortaan hun gecachete kleur mee. Architecturale bijzonderheid: `_hv_paneel_teken()` liep tot nu toe via de GEDEELDE `paneel_knop_teken()` (screen_main.ino, ook gebruikt door het hoofdscherm-paneel) die zelf een opake achtergrond tekent — daar transparantie aan toevoegen had ook het hoofdscherm geraakt, wat niet gevraagd was. Opgelost met een lokale, niet-gedeelde kopie `_hv_paneel_tegel_teken()` (identieke icoon/label-layout, maar met `_hv_tile_frame()` i.p.v. een eigen opake fill) — `paneel_knop_teken()` zelf blijft volledig ongewijzigd. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.9` |
| 225 | Sessie 40 | **HAVEN-tegels: foto zichtbaarder (50% oplichting i.p.v. blend met paneelkleur)**: Brendan zag de foto nauwelijks door de tegels heen — oorzaak: de vorige blend (`_hv_blend()`, taak 224) mengde de fotokleur met de donkere `C_SURFACE`/`C_SURFACE2`-paneelkleur op maar 25% sterkte, wat tegen een donker thema nauwelijks als tint opviel. Vervangen door `_hv_licht()`: de gecachete fotokleur wordt nu rechtstreeks als tegelvulling gebruikt, opgelicht richting wit met `HV_TILE_LICHT`=128 (≈50%, Brendans expliciete wens) i.p.v. gemengd met de paneelkleur — de foto blijft zo herkenbaar zichtbaar op elke tegel, maar licht genoeg om icoon/tekst erboven leesbaar te houden. `_hv_tile_frame()` gebruikt de aan/uit-status nu alleen nog voor de rand/accent-balk (ongewijzigd), niet meer voor de vulkleur zelf. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.10` |
| 226 | Sessie 41 | **Slaapstand teruggebracht tot alleen LIGHT + slave-scherm-actief gate + ESP-NOW-retry + GT911 direct-wake**: uitgebreide discussie met Brendan over waarom DEEP/HIBERN niet zinvol zijn (ESP-NOW/WiFi-radio zit in het niet-RTC-domein, staat dus sowieso helemaal uit in beide) leidde tot een concreet ontwerp. `SLAAP_DEEP`/`SLAAP_HIBERN` verwijderd uit `slaap.h`/`slaap.ino` (en uit beide CONFIG-UI-varianten, `screen_config.ino`); `slaap_m`-migratie in `app_state.ino` clamt een oud opgeslagen DEEP/HIBERN-waarde automatisch terug naar LIGHT. **Master blijft wakker voor een actieve slave**: nieuw `NET_MSG_SCHERM_STATUS` (slave → master, herhaald elke `NET_SCHERM_STATUS_MS`=3s zolang `tft_actief`) + `NetPeer.scherm_actief`/`scherm_actief_ms`; `net_slave_scherm_actief()` telt een peer alleen als "actief" zolang de laatste melding vers is (`NET_SCHERM_STATUS_TIMEOUT_MS`=10s) — bewust géén apart "uit"-bericht, dus een gemist pakket faalt altijd naar "mag weer slapen", nooit naar "blijft voor eeuwig wakker". `slaap_loop()` checkt dit naast de bestaande `tft_actief`-gate. Omdat de radio tijdens de losse 250ms-touchpolls te kort aanstaat om iets te ontvangen, opent de master elke `slaap_interval` (bestaande instelling, nu dus dubbel gebruikt) een `SLAAP_NET_VENSTER_MS`=4s durend venster waarin `net_loop()` actief blijft draaien. **Retry voor slave-commando's die de master gemist heeft tijdens het slapen**: `_stuur_io_toggle()`/`_stuur_io_naam()` geven elk commando een oplopend `req_id` mee en bufferen een kopie in een kleine `NET_TX_Q`-ringbuffer (4 slots); onbevestigde commando's worden elke `NET_TX_RETRY_MS`=4s herhaald (max 20×) tot een nieuw `NET_MSG_CMD_ACK` (master → slave, na verwerking van IO_TOGGLE/IO_NAAM) hetzelfde id bevestigt. **GT911 direct-wake**: `hw_touch.ino` zet bij `ts_setup()` de GT911 zelf om van fabrieksdefault edge-pulse (INT 1-5ms laag, te kort voor `EXT0`) naar level-trigger (INT blijft laag zolang aangeraakt) via rechtstreekse I2C-registerschrijf (Module_Switch_1 0x804D + herberekende checksum + CONFIG_FRESH) — bewust GEEN patch van de vendored TAMC_GT911-library (die staat niet in deze repo, zou dus niet meeverhuizen naar CI/andere machines); best-effort met het bestaande 250ms-pollvenster als vangnet als de registerwijziging om wat voor reden dan ook niet aanslaat. Lokaal gecompileerd en groen op alle 6 platforms. Nog niet op echte hardware getest (ESP-NOW-timing tussen twee fysieke units, en de GT911-registerwijziging specifiek) — dat kan hier niet geverifieerd worden. Beta `0.2.260909.11` |
| 227 | Sessie 40 | **CONFIG-scherm: header/footer overschrijven tijdens scrollen + HAVEN-tegels: echt fotomozaïek i.p.v. platte tint**: (1) Brendan meldde dat scrollen door WEERGAVE & ENERGIE de header/footer-ruimte overschreef. Oorzaak: alle vier scroll-schermen in `screen_config.ino` (`cfg_hoofd_teken`/`cfg_boot_teken`/`cfg_we_teken`/`cfg_update_teken`) tekenen scrollbare rijen op absolute y-posities zonder clipping; bij elk scroll-tikje (elke aanroep is een volledige herbouw, zie `cfg_we_run()`) konden rijen zo over de sub-header (`cfg_sub_header()`, vroeg in de functie getekend) of tot voorbij `NAV_Y` in de vaste onderste nav-balk heen schuiven — die laatste werd bovendien maar één keer getekend (bij schermentree in `screen_config_teken()`), dus bleef daarna corrupt staan tot een volgende volledige schermwissel. `cfg_hoofd_teken()` had dit voor z'n EIGEN vaste bovenbalk (`helderheid_balk_teken()`) al opgelost door 'm gewoon nog een keer ná de scrollbare inhoud te tekenen ("tekent overheen zodra iets omhoog gescrold is") — exact hetzelfde patroon nu ook toegepast op de overige 3 functies (`cfg_sub_header(titel)` + `nav_bar_teken()` aan het eind) én `nav_bar_teken()` alsnog toegevoegd aan `cfg_hoofd_teken()` zelf (had het footer-probleem ook, miste alleen nog die regel). Geen nieuwe clipping-infrastructuur nodig — bij elke scroll-herbouw is dit toch al een volledige herteken-cyclus, dus het eind-tekenen dekt de overloop meteen weer af. (2) Ondanks taak 225's 50%-oplichting zag Brendan nog steeds geen fotostructuur door de HAVEN-tegels heen — logisch, want elke tegel kreeg nog altijd maar één plat gemiddeld sample-kleurtje als vulling, geen echte foto. Brendans concrete voorstel gebouwd: per tegel niet één punt meer samplen maar een heel roostertje (`HV_MOZ_COLS`×`HV_MOZ_ROWS`=6×4=24 cellen), gecachet en bij een losse tegel-hertekening als mozaïek van `HV_MOZ_N` losse `fillRect()`-blokjes teruggetekend (elk apart opgelicht via `_hv_licht()`) — de tegel toont zo z'n eigen stukje foto, geen vlakke tint. `_hv_tile_frame()`/`_hv_lamp_teken()`/`_hv_paneel_tegel_teken()` nemen nu een `const uint16_t*` (wijst naar de 24 gecachete cellen) i.p.v. één `uint16_t`; de tegel krijgt zelf geen aparte achtergrondvulling meer — het mozaïek IS de achtergrond, er komt alleen nog de rand/accent-balk overheen. Kanttekening: de mozaïek-fill is een rechthoek (`fillRect`, geen `fillRoundRect`), dus de 8px afgeronde hoeken (`KNOP_R`) tonen in de uiterste hoekpixels nog een vierkant fotoblokje i.p.v. helemaal rond afgesneden — bewust geaccepteerd (geen aparte rond-mask-routine gebouwd) omdat het nauwelijks opvalt op deze schaal. **Geheugen-valkuil**: het roostertje per tegel (i.p.v. 1 sample) plus `HAVEN_SAMPLE_MAX` van 48 naar 1100 verhoogd deed de classic-ESP32-platforms (WROOM/CYD28/CYD40H/CYD40V) direct over hun vaste DRAM-BSS-segment heen lopen (linkerfout `region dram0_0_seg overflowed by 7352 bytes` — dat vaste segment bleek nog maar een paar honderd bytes speling te hebben, los van de veel ruimere "vrij voor lokale variabelen"-heap die de compileroutput toont). Opgelost door zowel de sample-buffers (`haven_achtergrond.ino`) als de tegel-cache (`screen_haven.ino`) van static arrays naar één keer bij eerste gebruik gealloceerde heap-buffers (`malloc`) om te zetten — functioneel identiek, maar getrokken uit de veel ruimere heap-pool i.p.v. het krappe BSS-segment; bij een (onwaarschijnlijke) malloc-mislukking valt de tegel gracieus terug op een vaste C_BG-tint i.p.v. te crashen. Lokaal gecompileerd en groen op alle 6 platforms (incl. de eerder overlopende WROOM/CYD-varianten). Beta `0.2.260909.12` |
| 228 | Sessie 40 | **Diagnose voor spontane herstart tijdens light sleep**: Brendan meldde dat de boordcomputer (losse testopstelling, geen ATtiny aangesloten) spontaan herstart zodra LIGHT sleep aanstaat, zowel op het HAVEN- als het vaardashboard — dus niet schermgebonden. Onderzoek (geen fysieke hardware beschikbaar om te reproduceren) wees op de sterkste, best gedocumenteerde kandidaat: op ESP32-S3 met de native USB-CDC-poort actief (`CDCOnBoot=default`, staat aan in de S3-build) verliest die poort tijdens light sleep zijn klok; als de host (bv. een PC tijdens bankwerk-testen via USB-kabel) daarna een bus-reset/re-enumeratie doet, triggert dat een volledige chip-reset (ROM-niveau `rst:0x15 USB_UART_CHIP_RESET`) — een bekende ESP32-S3-silicon-eigenaardigheid, geen softwarebug an sich, en zou een écht los-op-eigen-voeding werkend apparaat (geen actieve USB-datverbinding) niet moeten raken. Omdat dit niet met zekerheid vast te stellen is zonder een crashlog, is in plaats van blind te patchen een diagnosemiddel gebouwd: nieuw `slaap_reset_reden_verwerken()` (slaap.h/.ino, aangeroepen ná `fout_log_setup()` in `hw_setup()`) leest `esp_reset_reason()` bij elke boot en onthoudt 'm persistent (Preferences, namespace "diag") zodra de reden niet een gewone koude start of bewuste `esp_restart()` (OTA) is — inclusief `ESP_RST_UNKNOWN` (dekt vermoedelijk ook de USB-CDC-reset, die IDF niet fijnmaziger categoriseert). Stuurt bovendien automatisch door naar de bestaande (tot nu toe nergens aangeroepen) GitHub-foutrapportage (`fout_log_stuur(FOUT_APP_CRASH,...)`, alleen als Brendan FOUTRAP + token heeft ingesteld — bewaakt zichzelf al op die voorwaarden). Nieuwe `slaap_laatste_onverwachte_reset()`-getter toont de laatste bekende reden op het INFO-scherm (nieuwe "Reset:"-rij, systeemtab, naast Versie/BKOSS/WiFi/IO) — zo is de oorzaak na een volgende spontane herstart direct op het scherm af te lezen, ook zonder seriële monitor paraat. Brendan gevraagd om (als het zich herhaalt) dat scherm te checken, en om te testen of het probleem verdwijnt zonder actieve USB-datkabel (bv. op eigen 5V-voeding) ter bevestiging van de USB-CDC-hypothese. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.13` |
| 229 | Sessie 40 | **HAVEN-mozaïek fijner + CONFIG-scroll ronde 2 (echte statusbalk + verkeerde scrollhoogte)**: (1) Brendan vond het 6×4-mozaïek (taak 227) te grof — resolutie verhoogd naar 16×10 (`HV_MOZ_COLS`/`HV_MOZ_ROWS` in screen_haven.ino), ±9×7px per blokje op een ~150×72px tegel, merkbaar dichter bij de echte foto. `HAVEN_SAMPLE_MAX` mee opgehoogd naar 7200 (haven_achtergrond.h) — dankzij de heap-verhuizing uit taak 227 raakt dit het krappe DRAM-BSS-segment op classic ESP32 niet meer (compileroutput blijft nagenoeg ongewijzigd t.o.v. 6×4, de extra ±56KB komt uit de heap). Verder omhoog kan, maar kost lineair meer heap — bewust niet tot pixel-per-pixel resolutie doorgevoerd, dat is op de kleinste platforms niet haalbaar. (2) Brendan testte de CONFIG-scroll-fix uit taak 227 en meldde 'm nog niet volledig werkend: de écht statusbalk bovenin bleef overschreven worden, en de onderste rij kwam nooit volledig in beeld. Twee aparte oorzaken gevonden: **(a)** taak 227's fix herstelde alleen de CFG-subheader (`cfg_sub_header()`, het "WEERGAVE & ENERGIE"-balkje binnen het content-gebied) — niet de eígenlijke statusbalk erboven (`sb_scherm_teken()`, met wifi/klok); een tegel die tijdens het scrollen boven `CFG_CONT_Y` uitkomt (elke rij passeert die zone kort tijdens het scrollen, niet alleen aan de randen) overschrijft dus alsnog niet-herstelde pixels. `sb_scherm_teken("CONFIG", C_CYAN)` toegevoegd aan het eind van alle 4 scroll-functies (`cfg_hoofd_teken`/`cfg_boot_teken`/`cfg_we_teken`/`cfg_update_teken`), naast de al aanwezige sub-header/helderheidbalk/nav-balk-hertekening. **(b)** `CFG_WE_INHOUD_H` bleek al langer verouderd — de rijen SCHERM 180° DRAAIEN en DUBBELE BUFFERING zijn ooit toegevoegd zonder deze constante bij te werken, waardoor `CFG_WE_MAX_SCROLL` 92px te laag uitkwam en de allerlaatste rij (NACHT VAREND-slider) nooit volledig zichtbaar werd, hoe ver ook gescrold. Herteld door alle rijhoogtes in `cfg_we_teken()` stuk voor stuk na te lopen (62+44+34+42+44×13, S3-variant met DUBBELE BUFFERING = 710px; zonder die rij = 666px) en de macro te herschrijven met een 1-op-1 commentaar per rij, zodat een toekomstige nieuwe rij makkelijker meteen goed meegeteld wordt. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.14` |
| 230 | Sessie 40 | **KRITIEKE FIX: HAVEN-scherm crashte/herstartte bij het openen (stack-overflow)**: direct gevolg van taak 229's mozaïekresolutie-verhoging. `screen_haven_teken()` had `int16_t sx[HAVEN_SAMPLE_MAX], sy[HAVEN_SAMPLE_MAX];` als **stack-lokale** arrays — onschuldig toen `HAVEN_SAMPLE_MAX` nog 1100 was (4,4KB), maar bij de verhoging naar 7200 (taak 229) ineens 28,8KB op de stack, ruim boven een normale taakstack. Elke poging om HAVEN te openen overschreef zo de stack buiten zijn grenzen — precies het "scherm loopt vast, daarna herstart" dat Brendan meldde. Verplaatst naar dezelfde heap-aanpak die de rest van deze feature al gebruikt (nieuwe `hv_sample_sx`/`hv_sample_sy`, éénmalig gealloceerd via `_hv_sample_buf_klaar()`, valt bij een malloc-mislukking terug op 0 samples i.p.v. te crashen). Les: bij het opschalen van `HAVEN_SAMPLE_MAX` (taak 229) had elke plek waar dat als arraygrootte gebruikt wordt gecontroleerd moeten worden, niet alleen de al-heap-gealloceerde plekken — deze stack-lokale array was over het hoofd gezien. Daarnaast: het lightsleep-herstartprobleem (taak 228) blijft nog bestaan volgens Brendan, "iets minder snel" — geen nieuwe informatie om op te reageren; de diagnose uit taak 228 (INFO-scherm "Reset:"-rij) staat klaar om de daadwerkelijke oorzaak vast te leggen zodra het zich weer voordoet. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260909.15` |
| 231 | Sessie 40 | **HAVEN-tegels op volle fotoresolutie + actief/inactief-tint + hoekjes gefixt**: het mozaïek (taak 227/229) bleek Brendan nog steeds niet goed genoeg — expliciete wens: "resolutie gelijk aan die van de foto". Hele mozaïek/sample-cache-architectuur (HV_MOZ_COLS/ROWS, hv_bg_*, _hv_bg_cache_klaar, hv_sample_sx/sy) vervangen door een fundamenteel andere aanpak: `haven_achtergrond.ino` houdt nu een **persistente kopie van de laatst gedecodeerde foto op display-resolutie** bij (`hav_fb`, gealloceerd via `PLATFORM_MALLOC` — PSRAM op de S3 waar dat de volle 800x480 foto is, ±750KB; op de andere platforms is de decodeerschaal toch al lager via de bestaande `_hab_scale()`, dus blijft dat ruim binnen de gewone heap, 47-192KB). Nieuwe `haven_achtergrond_pixel(scherm_x, scherm_y)` geeft de EXACTE fotokleur op elk scherm-coördinaat terug, uit die framebuffer — dus geen sampling/mozaïek-benadering meer nodig: elke tegel vraagt gewoon zijn eigen pixels rechtstreeks op, ook bij een losse tegel-hertekening na een tik, zonder herdecoderen. `screen_haven.ino` fors vereenvoudigd (de hele sample-verzamel/cache-terugschrijf-stap in `screen_haven_teken()` is weg); tegels tekenen nu via een herbruikbare, meegroeiende heap-buffer (`hv_tegel_buf`, groeit naar de grootste gevraagde tegel) gevuld met `haven_achtergrond_pixel()`-opvragingen, in één `draw16bitRGBBitmap()`-call. **Actief/inactief kleurverschil**: Brendan wilde een duidelijker visueel verschil dan alleen de rand — actieve tegels krijgen nu een lichtgroene tint (`_hv_licht_groen()`, blend-doel R10/G63/B10), inactieve tegels de bestaande "richting wit"-tint (`_hv_licht()`) — beide via een gedeelde signed-blend-kern (`_hv_blend()`, een eerdere versie had hier een overflow-bug met een verkeerd geplaatste `(int8_t)`-cast, gefixt vóór het compileren). **Afgeronde hoeken**: de fotovulling is bewust een rechthoek (één bitmap-call, sneller), dus de 4 hoekjes buiten de KNOP_R-afronding staken er vierkant doorheen; nieuwe `_hv_hoeken_afronden()` zet die specifieke hoekpixels terug naar de ORIGINELE (ongetinte) fotokleur op exact die plek — sluit zo naadloos aan op de foto in de kloof ernaast, i.p.v. een vlakke kleur (die tegen de foto als een lelijke hap zou ogen). Lightsleep-herstart (taak 228/230) nog steeds niet bevestigd opgelost — Brendan gevraagd het nieuwe INFO-scherm "Reset:"-veld te checken zodra het zich weer voordoet, dat is de enige weg naar een zekere diagnose zonder seriële monitor. Lokaal gecompileerd en groen op alle 6 platforms (globaal geheugengebruik zelfs lager dan de mozaïek-versie, dankzij het wegvallen van de mozaïek-cache-arrays). Beta `0.2.260911.1` |
| 232 | Sessie 40 | **Eigen HAVEN-foto's uploaden via de webapp (SPIFFS)**: Brendan vroeg hoe een gebruiker (en later, als product, andere gebruikers) eigen foto's naar de boordcomputer kunnen sturen, met expliciete wens: verkleinen/ditheren naar de doelresolutie + het 16-bit RGB565-kleurenrooster gebeurt AL bij het uploaden (client-side), niet pas op het apparaat. Eerst de daadwerkelijke SPIFFS-partitiegroottes opgezocht (Brendans intuïtie dat mijn eerdere "2MB"-aanname niet klopte bleek terecht, al in de andere richting): `default_8MB` (S3) geeft 0x180000 = **1,5MB** SPIFFS; `min_spiffs` (WROOM/CYD28/CYD40H/CYD40V) geeft maar 0x20000 = **128KB** — fors kleiner dan gedacht, al is dat in verhouding minder erg dan het lijkt omdat die platforms de foto ook op een veel lagere resolutie decoderen (zie `_hab_scale()`). Nieuwe route `/haven` (webapp.ino + nieuw `webapp_haven_html.h`, puur HTML/CSS/JS zonder CDN-dependencies) laat de BROWSER het zware werk doen: canvas "cover"-crop naar exact de doelresolutie (opgevraagd bij `/haven/info`), gevolgd door een JS Floyd-Steinberg-ditherimplementatie die naar het RGB565-rooster kwantiseert (dezelfde aanpak als eerder met Python/PIL voor de ingebakken voorbeeldfoto's) vóór `canvas.toBlob(...,'image/jpeg',0.65)` en upload — maakt uploaden trager, maar de foto komt al zo klein mogelijk aan. **Belangrijke valkuil ontdekt en vermeden**: de voor de hand liggende route (`_http.arg("plain")` voor de ruwe POST-body) blijkt in de ESP32 WebServer-library de body via een **null-getermineerde String** te lezen (`String(plainBuf)`) — JPEG-bytes bevatten vrijwel altijd losse 0x00-bytes, dus dat zou de foto stilletjes afkappen/corrumperen. Opgelost door de standaard multipart/form-data-uploadroute te gebruiken (`HTTPUpload.buf`, een echte binaire `uint8_t[]`-buffer met expliciete lengte, geen String) — de PIN gaat daarom bewust via de query-string (`?pin=1234`, al bekend vóór de multipart-body begint) i.p.v. als form-veld, om onduidelijkheid over de volgorde waarin multipart-velden intern beschikbaar komen te vermijden. Nieuw in `haven_achtergrond.h/.ino`: `haven_doel_afmeting()`, `haven_spiffs_vrij()`, `haven_gebruikersfotos_scannen()` (bij boot + na elke upload/verwijdering, bestandsnamen `/haven_u<slot>.jpg`) — zodra er minstens één eigen foto is, vervangt die de ingebakken voorbeeldfoto's volledig in de slideshow. Bewust **niet** op Pico: geen webapp/upload-pad dat platform, en `TJpg_Decoder`'s `drawFsJpg()`-default-argument verwijst naar het ESP32-only `SPIFFS`-symbool — user-fotofuncties daar simpelweg gestubd (`aantal()` altijd 0, gedraagt zich exact als voorheen). `/haven/verwijder` en de nieuwe link vanaf de bestaande afstandsbediening-pagina (`webapp_html.h`) erbij. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260911.2` |
| 233 | Sessie 40 | **HAVEN-verlichting: ALLES AAN/UIT miste dek-lichten + nieuwe OVERIGE LAMPEN-tegel voor de hoofdverlichting**: Brendan meldde dat lampen "in de hoofdgroep" niet mee aan/uit gingen bij ALLES AAN/ALLES UIT, en dat er "ongedefinieerde lampen" bestaan die nergens een knop hebben. Onderzoek wees uit: (1) **echte bug** — `_hv_alles_aan()`/`_hv_alles_uit()` liepen alleen over `hv_lamp_nrs[]` (de genummerde `**IL_wit<N>`/`**IL_rood<N>`-groepen); de 'dek'-achtige PANEEL-lichten (`hv_licht_paneel_idx[]`, wél zichtbaar in dezelfde VERLICHTING-grid) werden domweg overgeslagen. Nieuwe `_hv_paneel_zet(idx, aan_gewenst)` FORCEERT i.p.v. toggelt (nodig zodat een tweede druk op ALLES AAN een al-aan lamp niet weer uitzet) en wordt nu ook in beide functies meegenomen. (2) Brendan bevestigde (via verduidelijkingsvraag) dat "ongedefinieerde lampen" de ongenummerde `**IL_wit`/`**IL_rood`-kanalen zijn (exact, geen suffix) — de "hoofdverlichting" die volgens `io_verlichting_update()` altijd al bestond (aan/uit via `int_aan`, kleur via `int_rood`) maar nooit een eigen bedienpunt kreeg: geen lampnummer dus geen PANEEL-knop mogelijk, dus onzichtbaar in zowel HAVEN als het hoofdscherm. Nieuw in io.h/.ino: `io_hoofdverlichting_aanwezig()` (bestaat zo'n kanaal — bepaalt of de tegel getoond wordt), `io_hoofdverlichting_aan()` (effectieve stand, zelfde scan-aanpak als `io_lamp_effectief_aan()`), `io_hoofdverlichting_toggle()` (wisselt `interieur_modus` tussen UIT en AUTO — vervalt dus nooit de kleuroverrule-logica uit taak 223, die blijft ongewijzigd werken). Nieuwe `_hv_overig_teken()`-tegel in screen_haven.ino, toegevoegd als laatste slot in het VERLICHTING-grid (`_hv_verlicht_totaal()` nieuwe gedeelde teller) — alleen zichtbaar als zo'n kanaal bestaat, en doet nu ook mee met ALLES AAN/UIT. De kleur-consistentieklacht ("kleur van lampen niet gelijk bij een andere vaarmodus") kon ik niet los reproduceren uit de code — `interieur_kleur_rood` is één centrale variabele die alle schermen delen, dat lijkt al correct; mogelijk was dit gewoon hetzelfde ALLES AAN/UIT-symptoom (een lamp die nooit uitging oogde ook "verkeerd gekleurd"). Brendan gevraagd opnieuw te testen na deze fix. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260911.3` |
| 234 | Sessie 40 | **Nieuw bestandsbeheerscherm (CONFIG → BESTANDEN)**: Brendan wilde in de instellingen het IP-adres kunnen zien (voor de HAVEN-webapp uit taak 232) en op het apparaat zelf inzicht in/verwijderen van opgeslagen bestanden — generiek voor SPIFFS én (waar aanwezig) de SD-kaart, met een wisselknop erboven, en bewust "uitbreidbaar" voor later. Bewust een zelfstandig topniveau-scherm (nieuwe `SCREEN_BESTANDEN`, `screen_bestanden.h/.ino`) net als HAVEN, i.p.v. genest in een van de TWEE bestaande CONFIG-implementaties — er blijkt namelijk zowel een categorie-gebaseerd groot-scherm-CONFIG (`cfg_hoofd_teken()`/`cfg_instellingen_teken()`) als een volledig aparte, monolithische eenpagina-variant voor SCREEN_SMALL (`pico_cfg_instellingen_teken()`) te bestaan; een zelfstandig scherm hoeft maar één keer geschreven te worden en is vanuit beide met één knopje te openen (nieuwe "BESTANDEN >"-categorie/regel in allebei, PIN-gated net als de andere categorieën/rijen daar). Toont: IP-adres + webapp-link, vrije/totale opslagruimte (`app_spiffs_vrij/totaal()`, hergebruikt uit app_manager — bestond al voor de Lua-appwinkel), een SPIFFS/SD-wisselknop (SD alleen zichtbaar/mogelijk op de S3, zelfde platformcheck als `app_manager.cpp`'s bestaande `app_sd_aanwezig()`), en een scrollbare bestandenlijst (root-scan via `fs->open("/", "r")` + `openNextFile()` — SPIFFS/LittleFS hebben geen echte submappen, dus dit toont in de praktijk alles) met VERWIJDER per rij. **Twee compile-valkuilen**: (1) de bestandenlijst-array (60 slots) zat eerst als static array — liet de krappe DRAM-BSS-segment op WROOM opnieuw overlopen (1808 bytes), zelfde patroon als eerder deze sessie met de HAVEN-fotobuffers — opgelost met dezelfde heap-malloc-aanpak; (2) RP2040's `fs::FS::open()` heeft geen 1-argument-overload (verplicht een mode-parameter, anders dan ESP32's SPIFFS-wrapper) — `fs->open("/")` werd `fs->open("/", "r")`. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260911.4` |
| 235 | Sessie 41 | **S3 16MB-partitieschema (grote SPIFFS)**: nieuw `BKOS_NUI/partitions_s3_16mb.csv` — APP-slots identiek aan `default_8MB` (2×3264KB, OTA-limiet ongewijzigd) maar SPIFFS opgerekt van 1,5MB naar 9,5MB (0x980000), het volledige restdeel van de fysieke 16MB flash die de 7"-module al had maar tot nu toe niet aansprak. Alleen relevant voor de S3-build; de andere 4 ESP32-platforms (WROOM/CYD28/CYD40H/CYD40V) hebben fysiek 4MB flash en blijven op hun eigen schema. Geïnstalleerd via de arduino-esp32 prebuild-hook (bestand met exacte naam `partitions.csv` in de sketchmap overschrijft het PartitionScheme-menu) — `build.yml`'s `build_esp32s3`-job kopieert het bestand daar nu naartoe vlak vóór het compileren; lokaal compileren voor S3 vereist dezelfde handmatige kopieerstap (zie *Partitieschema S3 (16MB)* hierboven). Rechtstreekse aanleiding: veel meer ruimte nodig voor Lua-apps/HAVEN-foto's/algemene bestanden (BESTANDEN-scherm, taak 234) dan de oude 1,5MB toeliet. Lokaal gecompileerd en groen op S3 (2.142.000 bytes firmware = 64% van de ongewijzigde 3.342.336-byte APP-limiet; gegenereerde partitietabel met `gen_esp32part.py` geverifieerd: app0/app1 3264K, spiffs 9728K, coredump 64K — exact zoals bedoeld) |
| 236 | Sessie 42 | **BESTANDEN-scherm: deferred loading, hotspot-busy-status, thumbnails; HAVEN-upload robuuster**: Brendan meldde dat CONFIG → BESTANDEN traag aanvoelde (klopte: `screen_bestanden_teken()` deed een volledige, blokkerende SPIFFS/SD-scan bij ÉLKE hertekening, ook bij scrollen) en dat de HOTSPOT-knop pas van kleur veranderde nadat `WiFi.softAP()`/`webapp_setup()` (blokkerend) al klaar waren. Nieuwe `_bf_herladen()`/`bf_geladen`-vlag: scherm openen, van map wisselen, SPIFFS↔SD schakelen, filter wisselen of verwijderen toont meteen "Bestanden worden geladen..." en tekent de echte lijst pas ná de eerstvolgende periodieke tik (`screen_bestanden_run(0,0,false)`, nieuwe case in `_gui_taak()`'s idle-switch, hardware.ino) — de trage scan zelf gebeurt dus nooit meer synchroon in de tik-handler. HOTSPOT-knop toont nu eerst een amber "BEZIG..."-kleur + een geforceerde `tft_flush(true)` vóórdat de blokkerende start/stop-aanroep gebeurt. Nieuwe thumbnails: kleine JPEG-voorbeeldjes per rij in de AFBEELDINGEN-filter, gedecodeerd via de gedeelde, globale `TJpg_Decoder`-instantie (ook gebruikt door haven_achtergrond.ino) met een kleine heap-gealloceerde cache (12 slots, ronde-robin) zodat scrollen dezelfde foto niet steeds opnieuw hoeft te decoderen — `_hab_init()` zet scale/callback voortaan ALTIJD opnieuw (geen "eenmalig ingesteld"-guard meer) zodat de twee schermen elkaars TJpgDec-instellingen niet stiekem overschrijven. Zelfde thumbnails ook op de HAVEN-webpagina (`/haven`) via nieuwe route `/haven/foto?naam=...` (valideert tegen de bekende fotolijst, geen vrije padtoegang). **Compile-valkuil**: `struct BfThumb` + een functie die `BfThumb*` retourneert moesten naar `screen_bestanden.h` én dat bestand moest via `hardware.h` (niet alleen `hardware.ino`) vroeg zichtbaar worden — anders plaatst Arduino's auto-prototype-generator de forward declaration vóór de struct-definitie ("BfThumb does not name a type"), hetzelfde patroon als eerdere prototype-hoisting-issues. **Los ontdekte/bevestigde oorzaak van eerdere upload-mislukkingen**: HAVEN-foto-uploads bleken vaak te groot te worden omdat de client-side Floyd-Steinberg-dithering (bedoeld tegen bandvorming) vóór het JPEG-encoderen fijne ruis toevoegt — precies wat JPEG-compressie het slechtst verdraagt, dus een 800×480-foto werd zo 3-10× groter dan nodig; dithering uit de upload-flow gehaald + een adaptieve kwaliteitsladder (tot 4% kwaliteit) die vóór het versturen al binnen de servergrens (`maxBytes`, nu ook in `/haven/info`) probeert te blijven, i.p.v. eerst uploaden en dan pas ontdekken dat het te groot is; server onderscheidt nu ook pin/te-groot/opslag-fouten (403/413/400) i.p.v. één vlag die "te groot" verkeerd als "onjuiste pincode" liet zien. Dit werkt samen met taak 235's SPIFFS-uitbreiding (1,5MB→9,5MB) — twee onafhankelijke fixes voor hetzelfde gemelde symptoom, vanuit twee verschillende sessies. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.2` |
| 237 | Sessie 42 | **On-device thumbnail-bug gefixt + webapp-toegangsmodel volledig herzien**: (1) `TJpg_Decoder::getFsJpgSize(File)` sluit het meegegeven bestandshandle zelf af zodra de afmetingen bekend zijn — screen_bestanden.ino's thumbnail-code hergebruikte daarna hetzelfde (al gesloten) handle om de foto te decoderen, wat altijd stil faalde (leeg grijs vlakje i.p.v. voorbeeld). Fix: de filename+fs-variant van getFsJpgSize gebruiken voor de afmetingen (die opent en sluit zijn eigen, weggegooide handle) en daarna een VERS handle openen voor de daadwerkelijke `drawFsJpg()`. (2) Op Brendans verzoek de webapp-toegang volledig herzien (was: alles-behalve-schrijven publiek, taak 185): **publiek** (geen pincode) is nu alleen boot/eigenaarnaam (nadrukkelijk niet adres/telefoon/e-mail — nieuwe `info_boot_type()`/`info_eigenaar_naam()` in screen_info.h/.ino) en een preset-berichtje sturen ("iets is los", hergebruikt de bestaande `bericht_verzend()`-pijplijn). **Één gedeelde pincode** (localStorage-key `bkos_pin`, zelfde op "/" en "/haven") ontgrendelt voortaan zowel het bedieningspaneel als HAVEN-foto's uploaden/verwijderen — nieuwe `POST /verify`-route laat beide pagina's een opgeslagen pincode in stilte bevestigen zonder opnieuw in te loggen; bij een foute/verlopen pincode wordt de opslag stil gewist. Pincode-velden zijn nu `type="password"` i.p.v. platte tekst. Server-side validatie per schrijfactie blijft ongewijzigd bestaan (de localStorage-laag is puur client-side UX, geen vervanging). Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.2` (webapp-toegang) — de thumbnail-fix zit in dezelfde push |
| 238 | Sessie 42 | **Hotspot standaard aan + captive portal, HAVEN-chrome verfijnd, interieurverlichting aan/uit vs. kleur ontward**: (1) Hotspot (wifi.ino) staat nu standaard AAN zodra het apparaat opstart (`wifi_hotspot_tick()`'s eenmalige vertraagde auto-start, ~3s ná opstarten, NOOIT synchroon in hw_setup() — zie [[project_bkos_webapp]] voor de bootlus-geschiedenis) i.p.v. een handmatige 30-minuten-knop; die knop blijft bestaan maar zonder tijdslimiet. Nieuwe `DNSServer` (alle namen → eigen AP-IP zolang de hotspot actief is) + de al bestaande onNotFound-redirect vormen samen een eenvoudig captive portal — een telefoon die verbindt zou zo vanzelf een inlogmelding moeten krijgen die naar de webapp leidt (geen per-OS maatwerk, de gangbare DNS+redirect-basisaanpak). Statusbalk-icoon toegevoegd (nav_bar.ino): het oude, nooit-aan-een-status-gekoppelde bluetooth-icoontje is vervangen door een functioneel hotspot-icoon (groen=actief). (2) HAVEN-dashboard chrome verder verfijnd op Brendans verzoek: header/footer-fototint fors sterker (NB_TINT_NORMAAL/ACTIEF 70/175 → 165/215), het klokvenster is nu volledig zwart tot de rechterrand én de volle statusbalkhoogte (nieuwe gedeelde macro `SB_KLOK_ZWART_X`, nav_bar.h) en iets breder, en de TERUG-knop gebruikt nu dezelfde marge (`HV_BACK_GAP`=8) zowel vanaf de bovenkant van het scherm als t.o.v. het klokvenster — voorheen was de knop verticaal gecentreerd terwijl het klokvenster inmiddels breder was geworden, wat de zichtbare tussenruimte kleiner maakte dan bedoeld. (3) **Echte bug gevonden en gefixt**: aan/uit en kleur van de interieurverlichting zaten door elkaar geklutst — `interieur_kleur_overrulen()` zette de verlichting altijd naar AAN (AUTO) ook als 'm uit stond (kleur kiezen zette dus lampen aan), en `io_verlichting_update()`'s overrule-geldigheidscheck wiste de gekozen kleur zodra ALLES UIT de hoofdverlichting naar UIT schakelde (aan/uit liet dus de kleur "vergeten" worden, waarna de eerstvolgende ALLES AAN terugviel op wit i.p.v. de eerder gekozen rood). Fix: een kleurkeuze forceert AUTO niet meer als de stand UIT is (kleur wordt alleen onthouden), de overrule-check wist niet meer bij UIT (alleen nog bij een bewuste WIT/ROOD-keuze op het hoofdscherm of een vaarmodus-wissel), en de overrule wordt nu altijd op `auto_rood` toegepast zolang geldig (niet alleen als `interieur_modus==AUTO`) omdat genummerde lampen onafhankelijk van de hoofdverlichting-aan/uit-stand werken. Nog niet door Brendan bevestigd op hardware. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.3` |
| 239 | Sessie 42 | **Dubbel bestandssysteem (SPIFFS+FATFS), thumbnail-decodeerbug, interactieve foto-crop, dubbele webapp-achtergrond**: Brendan meldde dat de installer het SPIFFS-partitieschema niet aanpast en dat Arduino IDE voor de gewenste 9,5MB-grootte alleen FATFS aanbiedt, niet SPIFFS. (1) **Dubbel bestandssysteem**: `platform_fs.h`/nieuw `platform_fs.ino` herbouwd rond een generieke `fs::FS bkos_fs` (ESP32) die bij het opstarten `SPIFFSFS` of `F_Fat` erin kopieert afhankelijk van welk van de twee daadwerkelijk mount — mogelijk dankzij een niet-virtuele maar wel publieke copy-assignment (`fs::FS` heeft geen virtuele methoden, alleen een `_impl`-pointer, dus toewijzen kopieert die pointer legaal — "object slicing" die hier bewust wordt benut). `#define SPIFFS bkos_fs` herschrijft de rest van de merged Arduino-vertaaleenheid transparant, zodat bestaand code (`SPIFFS.open(...)` overal) ongewijzigd blijft werken, ongeacht welk filesysteem er daadwerkelijk actief is. `bkos_fs_totaal()/bkos_fs_gebruikt()` vervangen de rechtstreekse `SPIFFS.totalBytes()/usedBytes()`-aanroepen (die bestaan niet op het generieke `fs::FS`-type) in `haven_achtergrond.ino`/`screen_lampen.ino`/`screen_paneel.ino`/`app_manager.cpp`. Het daadwerkelijk FORMATTEREN naar FATFS (en een grotere SPIFFS via de installer) is bewust nog niet gebouwd — dat vereist ook de partitietabel zelf (0x8000) kunnen herschrijven, wat noch OTA noch de huidige installer doet (die schrijft alleen het app-partitie op 0x10000); Brendan gevraagd dit apart op te pakken zodra dit deel getest is, inclusief zijn wens om daarbij de opgeslagen pincode te controleren/terug te zetten. (2) **On-device thumbnail-bug** (screen_bestanden.ino, vervolg op taak 237's webapp-kant): dezelfde `getFsJpgSize()`-sluit-het-handle-bug bleek niet alleen de webapp maar ook de eigen BESTANDEN-scherm-thumbnails te raken — zelfde fix (filename+fs-variant voor de afmeting, een vers handle voor de decode). (3) **Interactieve foto-crop bij HAVEN-upload** (`webapp_haven_html.h`): i.p.v. de automatische center/cover-crop kan de gebruiker nu de geselecteerde foto pannen/zoomen binnen een rechthoek met de doelverhouding (Pointer Events voor muis+touch, CSS `transform`, een zoomslider) — alleen de inhoud van die rechthoek wordt opgeslagen; de rest van de kwaliteitsladder/upload-pijplijn (taak 237) is ongewijzigd. (4) **Dubbele webapp-achtergrond**: twee nieuwe, alleen-voor-de-browser bestemde achtergrondfoto's (1 staand, 1 liggend, HD-resolutie, volledige 32-bit kleur — geen RGB565-beperking nodig, dit rendert in de browser) via nieuwe routes `/achtergrond/info`, `/achtergrond/foto`, `/achtergrond/verwijder`, `/achtergrond/upload` (webapp.ino) en nieuwe UI/JS in `webapp_html.h` (`agUpload()`, cover-crop naar 1280×720/720×1280, eigen kwaliteitsladder, past de gekozen foto meteen toe als paginabodyachtergrond met een donkere overlay-gradient). Een nieuwe upload in hetzelfde slot vervangt/verwijdert automatisch de oude foto (`_ag_pad(bool liggend)` geeft een vast bestandspad per slot, dus "vervangen" is gewoon overschrijven). Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.4` |
| 240 | Sessie 42 | **Opstarten in HAVEN/ANKER opent gelijk het dashboard + vaarmodus bij opstarten bepaald door actieve ingangen**: Brendan vroeg dit expliciet vóór het installer-werk (taak 239's format-optie) te doen. (1) Nieuwe, standaard AAN staande instelling `boot_haven_naar_dashboard` (app_state, config-key `boothvd`, UI-rij in CONFIG → WEERGAVE & ENERGIE onder OPSTART VAARMODUS, en `OP.HVN:`-rij op PICO/SCREEN_SMALL): staat hij aan, dan opent het apparaat bij het opstarten in vaarmodus HAVEN of ANKER gelijk het HAVEN-dashboard (`actief_scherm = SCREEN_HAVEN`) i.p.v. het gewone vaardashboard — toegepast in `hardware.ino` vlak na de bestaande `actief_scherm = SCREEN_MAIN;`-regel, ná de calibratie-override zodat een vereiste touch-kalibratie nog altijd voorrang houdt. (2) **Vaarmodus bij opstarten uit actieve ingangen**: hergebruikt bewust de al bestaande per-ingangskanaal-configuratie `io_actie_aan[i] == IO_ACTIE_MODUS_*` (dezelfde instelling die tijdens bedrijf een automatische vaarmodus-wissel triggert, taak 189) in plaats van een nieuw, apart per-kanaal veld te introduceren — een kanaal dat al is ingesteld om bv. MOTOR te activeren doet dat nu ook als het al bij het opstarten actief is. Nieuwe `io_boot_vaarmodus_bepalen()` (io.ino) scant na het opstarten alle ingangskanalen met zo'n actie en kiest bij meerdere gelijktijdig actieve kanalen via de door Brendan opgegeven vaste prioriteit **motor > zeilen > anker > haven**; is geen enkel zo geconfigureerd kanaal actief, dan blijft de gewone `boot_vaar_modus`/onthouden-stand ongewijzigd staan. Alleen actief als `vaarmodus_auto` aanstaat (dezelfde schakelaar als de live automatische wissel). **Nieuwe `io_cyclus(bool stil=false)`-parameter**: nodig omdat de bestaande cyclusfunctie ingangswijzigingen altijd meteen als actie/melding afvuurt (edge-triggered t.o.v. de vorige stand, die bij opstarten op `false` begint) — zonder ingreep zou de allereerste cyclus na opstarten dus per kanaal, in kanaalvolgorde (niet op prioriteit) en vóór `net_setup()`/`app_setup()` al hun eigen acties afvuren, een sluimerende race die toevallig nooit eerder opgemerkt is. `stil=true` (nieuw, alleen gebruikt via één expliciete aanroep in `hw_setup()` vlak na `io_boot()`) werkt `io_input[]` gewoon bij maar slaat de actie/melding-blokken over, zodat `io_boot_vaarmodus_bepalen()` daarna een schone, deterministische keuze kan maken op de echte hardwarestand. Nog niet door Brendan op hardware getest. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.4` |
| 241 | Sessie 42 | **Echte hoofdoorzaak zwarte foto's gevonden + crop hergebruikt voor webapp-achtergrond + haven→fotos hernoemd + gasten-pincodes + webapp herbouwd naar Huis/Boot/IO/Bestanden-tabs**: Brendan meldde dat foto-uploads zwart uitkwamen en dat de mapnaam "haven" niet meer klopt nu foto's voor meerdere doelen dienen; hij vroeg ook direct om een gasten-pincode-systeem en een tabbladen-indeling voor de webapp die het paneel van de boordcomputer spiegelt. (1) **Echte bug**: `cropBevestig()` (webapp_haven_html.h) verborg de crop-modal (`display:none`) VÓÓRDAT de kaderafmetingen (`cropVp.clientWidth/clientHeight`) gemeten werden — een verborgen element geeft daar altijd 0 voor terug, dus `sw`/`sh` werden 0, `drawImage()` tekende niets (leeg/transparant canvas), en `canvas.toBlob('image/jpeg')` exporteert een transparant canvas als volledig ZWART (JPEG kent geen alphakanaal). Fix: meten vóór verbergen. Verklaart zowel de zwarte foto's als het gevoel dat "uploaden niet lukt". (2) Dezelfde (nu gefixte) pan/zoom/crop-modal hergebruikt voor de twee webapp-achtergrondfoto's (staand/liggend, `webapp_html.h`) — voorheen alleen een automatische cover-crop zonder eigen positionering. (3) **Hernoemd**: opslagpad `/haven/foto_<n>.jpg` → `/fotos/foto_<n>.jpg` (met migratie van zowel deze als de oudere platte `/haven_u<n>.jpg`-naam) en alle webroutes `/haven*` → `/fotos*` (`haven_achtergrond.ino`, `webapp.ino`, `webapp_haven_html.h`, `webapp_html.h`, en de op-scherm getoonde webapp-link in `screen_bestanden.ino`) — SCREEN_HAVEN/het HAVEN-dashboard zelf blijft ongemoeid, die naam klopt daar wél. (4) **Gasten-pincodes**: nieuw `gast.h/.ino` — tot 20 tijdelijke codes (auto-gegenereerd, 4 cijfers, uniek t.o.v. elkaar en de eigenaars-pincode) met optionele naam en vervaltermijn (onbeperkt/1/3/7/30 dagen; verloopcontrole faalt bewust "open" zolang de klok niet gesynchroniseerd is — bruikbaarheid voor bezoek op een afgelegen ankerplaats weegt hier zwaarder dan strikte handhaving). Nieuw `pin_niveau()` (0=geen,1=gast,2=eigenaar) is nu de centrale toegangscheck voor zowel de HTTP- als WebSocket-kant van de webapp. Nieuw on-device scherm `screen_gast.h/.ino` (`SCREEN_GAST`, CONFIG → BOOT → GASTEN PINCODES) voor aanmaken/bekijken/verwijderen — bewust geen entry op PICO/SCREEN_SMALL, zelfde bestaande beperking als PANEEL/LAMPEN daar. (5) **Webapp herbouwd naar 4 tabbladen** (HUIS/BOOT/IO/BESTANDEN, `webapp_html.h`): HUIS toont interieurverlichting (hoofdverlichting aan/uit, WIT/ROOD, ALLES AAN/UIT) + alle genummerde lampgroepen (nieuwe WebSocket-boodschap `lampen` + commando's `lamp_toggle`/`lamp_alles`/`interieur_kleur`/`interieur_toggle` in `bkos_client.ino`, zelfde auto-scan als het HAVEN-dashboard op het scherm zelf) en een link naar `/fotos`; BOOT bundelt de bestaande Vaarmodus/Verlichting/Paneel-secties; IO bundelt IO-kanalen + Verbonden modules; BESTANDEN is een nieuwe, simpele SPIFFS/SD-bestandsbrowser (nieuwe routes `/bestanden/info`,`/lijst`,`/verwijder` in `webapp.ino`, eigenaar-only, geen thumbnails — mirror zonder de decodeer-pas van het scherm zelf) + de achtergrond-upload. Een gastcode (niveau 1) ziet alleen de HUIS/BOOT-tabbladen; IO en BESTANDEN blijven zowel client- als server-side (WebSocket-commandogate + `_pin_eigenaar()` op elke gevoelige HTTP-route) aan de eigenaars-pincode voorbehouden — ook de `/fotos`-pagina zelf accepteert voortaan alleen een eigenaarsniveau-code (een geldige gastcode wordt niet als fout behandeld, maar krijgt een duidelijke "geen toegang tot foto's"-melding zonder de gast alsnog uit te loggen op de hoofdpagina). **Compile-valkuil**: de nieuwe `GastPin gast_pin[20]`-array plus de nieuwe lampgroep-arrays in `bkos_client.ino` duwden de classic ESP32-builds (WROOM/CYD28/CYD40V eerst, daarna nog 16 bytes op CYD40H) over hun krappe DRAM-BSS-segment heen — opgelost met dezelfde heap-malloc-aanpak als eerdere sessies (`gast_pin` en de lampgroep-scanarrays nu heap-gealloceerd) plus een paar velden verkleind naar `uint8_t`/`int8_t` waar een `int` onnodig ruim was. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.5` |
| 242 | Sessie 42 | **Crop-uploads gehard tegen stil vastlopen + berichten inklapbaar + FOTOS-tab met alle 3 uploads + nieuwe INSTELLINGEN-tab (boot/eigenaar/pincode/gasten)**: Brendan meldde dat foto-uploaden na de vorige fix "helemaal niet meer" leek te werken; kon geen tweede code-fout vinden bovenop de al gefixte meet-vóór-verbergen-bug, dus is de hele crop-bevestig-stap nu in try/catch gezet (in zowel `webapp_haven_html.h` als `webapp_html.h`) — een onverwachte fout (bv. een leeg kader) gaf voorheen een oneindige stille "wordt verwerkt…"-status zonder ooit een netwerkverzoek te versturen; nu altijd een zichtbare foutmelding. (1) "Iets aan de hand?"-sectie (`webapp_html.h`) is nu inklapbaar: uitgelogd staat 'm net als voorheen open (de enige publieke functie), ingelogd klapt 'm dicht tot een knopje (`berichtSetOpen()`, aangeroepen vanuit `setLock()`). (2) **BESTANDEN-tab hernoemd naar FOTOS** en samengevoegd met de losse foto-upload-flow: de crop-modal is generiek gemaakt (`CROP_TARGETS` met 'haven'/'liggend'/'staand', gedeelde `openCrop()`/`cropBevestig()`) zodat alle 3 upload-manieren (HAVEN-dashboardfoto + de 2 webapp-achtergrondfoto's) rechtstreeks vanuit dit ene tabblad werken, inclusief lijst+verwijderen van eigen HAVEN-foto's (hergebruikt de bestaande `/fotos/*`-routes) — de rauwe SPIFFS/SD-bestandsbrowser blijft in hetzelfde tabblad staan. (3) **Nieuwe INSTELLINGEN-tab** (eigenaar-only, 5e tabblad): boot- en eigenaargegevens (nieuwe generieke veld-accessors `info_boot_veld()`/`info_eig_veld()` in `screen_info.h/.ino`, i.p.v. alleen de twee al-publieke velden), zeilnummer, apparaatnaam, pincode wijzigen (`pin_wijzig`-commando, valideert de huidige pincode server-side vóór `pin_schrijven_pub()`), en **gasten-pincodebeheer direct vanuit de webapp** (aanmaken met naam+duur, lijst met resterende geldigheid, verwijderen) — dezelfde `gast.h`-functies als het on-device GASTEN PINCODES-scherm (taak 241), nu ook bereikbaar zonder de fysieke boordcomputer aan te hoeven raken, wat Brendan expliciet vroeg ("met een computer of telefoon gaat het soms net wat makkelijker"). Nieuwe WebSocket-boodschappen/-commando's in `bkos_client.ino`: `instellingen_get`/`instellingen_set`, `pin_wijzig`, `gast_get`/`gast_toevoegen`/`gast_verwijderen` — stuk voor stuk met een expliciete `GAST_NIVEAU_EIGENAAR`-check, dus een gastcode kan er niet bij (het tabblad is voor die gebruikers toch al verborgen, maar de servercheck is de eigenlijke grens). Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.6` |
| 243 | Sessie 42 | **Vermoedelijke oorzaak "GitHub fout -1" + 4-niveau rechtensysteem (gast/logé/deler/eigenaar) met per-kanaal minimaal niveau**: Brendan meldde "ik krijg een github fout -1" bij het bijwerken — vermoedelijke oorzaak: de hotspot staat sinds taak 238 standaard aan, wat de ESP32 permanent in gecombineerde AP_STA-modus zet (één gedeelde radio) — een bekende bron van verstoorde TLS-handshakes. Nieuwe `_ota_hotspot_pauzeren()/_hervatten()` (ota.ino) stoppen de hotspot tijdelijk vlak vóór elke blokkerende GitHub-HTTPS-aanroep (`ota_git_check`/`ota_laad_releases`/`ota_download_toepassen`) en zetten 'm nadien terug aan (niet nodig op het succespad van een download, die herstart toch). Nog niet door Brendan bevestigd of dit de daadwerkelijke oorzaak was. **Belangrijk inzicht**: zolang OTA faalt, kon geen van de eerdere foto-upload-fixes (taken 241/242) Brendans apparaat ooit bereikt hebben — "uploaden lukt nog niet" kan dus grotendeels hetzelfde onderliggende OTA-probleem zijn geweest. Los daarvan, op Brendans verzoek een uitgebreider rechtensysteem: de eerdere 2-niveau opzet (gast/eigenaar, taak 241) is vervangen door 4 niveaus — **GAST** (standaard: HUIS+BOOT), **LOGE** (zoals gast, plus kanalen die minimaal dit niveau vereisen — bv. een toekomstig deurslot, want een logé moet de boot zelf kunnen open/dicht maken), **DELER** (werktitel — Brendan denkt nog na over een betere naam voor iemand die meer vertrouwen krijgt dan een logé; zoals logé plus eigen kanalen), **EIGENAAR** (de ene hoofdpincode, onbeperkt — nooit toe te kennen via een gastcode). Nieuwe constanten `NIVEAU_GEEN/GAST/LOGE/DELER/EIGENAAR` (gast.h, vervangen `GAST_NIVEAU_*`). `GastPin` kreeg een `niveau`-veld (CSV-formaat uitgebreid met backward-compatibele migratie: ontbrekend niveau-veld → NIVEAU_GAST); `gast_toevoegen()` kreeg een niveau-parameter en nieuwe `gast_bewerken()` laat een bestaande code (naam/duur/niveau) wijzigen zonder de code zelf te veranderen — zowel het on-device `screen_gast.ino` (niveau-kiezer + tik-op-rij-om-te-bewerken i.p.v. alleen verwijderen) als de webapp-INSTELLINGEN-tab (`webapp_html.h`) hebben dit nu. **Per-kanaal afdwinging**: nieuw `io_min_niveau[]` (hw_io.h/.ino, heap-gealloceerd, uitbreiding van het `/io_cfg.csv`-regelformaat met een 9e veld) plus `io_min_niveau_voor_naam()`/`_voor_lamp()` (io.h/.ino) — een nieuwe rij "MINIMAAL NIVEAU" in de IO CFG-overlay (alleen groot scherm/UITGANG, niet PICO — zelfde bewuste beperking als PANEEL/LAMPEN daar) laat een kanaal op LOGE/DELER/EIGENAAR zetten. `bkos_client.ino` checkt dit nu vóór `paneel_toggle`/`lamp_toggle` (met een specifiek, gerichte niveau-vereiste per kanaal i.p.v. de vroegere alles-of-niets eigenaar-only-grens voor IO/foto's/instellingen, die ongewijzigd blijft); `lamp_alles` slaat kanalen die te hoog niveau vereisen stil over i.p.v. de hele actie te weigeren. `_paneel_json()`/`_lamp_json()` sturen nu `minNiveau` mee zodat de webapp ontoegankelijke knoppen grijs+op slot toont (`renderPaneel()`/`renderHuis()`) — puur cosmetisch, de server handhaaft dit sowieso zelf opnieuw. **Compile-valkuil**: `io_min_niveau[MAX_IO_KANALEN]` als 9e statische 240-bytes-array duwde de classic ESP32-builds weer over hun DRAM-BSS-budget (WROOM 216 bytes, CYD40H uiteindelijk 8 bytes na eerdere kleine besparingen) — heap-gealloceerd net als eerder `gast_pin`, plus `screen_gast.ino`'s foutmeldingsbuffer verkleind (48→40 bytes, teksten iets ingekort). Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.7` |
| 244 | Sessie 42 | **Foto-opslag-diagnose zichtbaar gemaakt + zelf gekozen/wijzigbare gasten-pincodes**: Brendan bleef "upload lukt nog niet" melden; via gerichte vervolgvragen bleek de exacte foutmelding "Upload mislukt (opslag vol of ongeldig bestand)" — de crop/upload-code zelf werkt dus, het apparaat weigert de opslag zelf. Twee waarschijnlijke oorzaken, allebei opslag-gerelateerd i.p.v. een codefout: (a) SPIFFS staat nog op het oude 1,5MB-schema (de 9,5MB-vergroting vereist een eenmalige USB-herflash die nog niet gedaan is) en zit mogelijk krap, of (b) de vaste 30-foto-limiet is al vol geraakt met eerdere testfoto's — óók de zwarte exemplaren van vóór de crop-fix "lukten" op opslagniveau prima. `HAVEN_USER_FOTO_MAX` verplaatst naar `haven_achtergrond.h` (was lokaal in de .ino) zodat `webapp.ino` 'm ook kan gebruiken; `/fotos/info` geeft nu ook `maxAantal` terug. Nieuwe zichtbare teller in de FOTOS-tab (`fotosStatus`, bv. "12/30 foto's · 340 KB vrij op het apparaat") die rood kleurt zodra het écht vol zit — ververst na elke upload(poging), zodat Brendan meteen ziet welke van de twee oorzaken van toepassing is en oude foto's kan wissen via de al bestaande lijst. **Zelf gekozen gastcodes**: op Brendans verzoek ("ik wil zelf kunnen bepalen wat de gastenpincodes worden... na het aanmaken kunnen wijzigen of gelijk al bij het aanmaken") kreeg `gast_toevoegen()`/`gast_bewerken()` (gast.h/.ino) een optioneel `gewenste_code`/`nieuwe_code`-argument (leeg = automatisch genereren zoals voorheen); nieuwe `gast_code_beschikbaar()` controleert vooraf op botsing met de eigenaars-pincode of een andere gastcode (met een `negeer_idx` zodat een code bewerken zonder wijzigen niet tegen zichzelf botst) — zowel `screen_gast.ino` (nieuw Code-veld met numeriek toetsenbord, boven Naam) als de webapp-INSTELLINGEN-tab (nieuw code-invoerveld, vooringevuld bij bewerken) gebruiken dit nu. **Compile-valkuil**: het nieuwe Code-veld in `screen_gast.ino` duwde CYD40H weer 8 bytes over de DRAM-BSS-grens — foutmeldingsbuffer verder verkleind (40→32 bytes) om ruimte te maken. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.8` |
| 245 | Sessie 42 | **Webapp: publieke info+berichten naar een eigen INFO-tab, INSTELLINGEN gegroepeerd in 2e-niveau tabbladen, MELDINGEN (CallMeBot) nu ook via de webapp instelbaar**: drie samenhangende verzoeken van Brendan. (1) "Boot & eigenaar" en "Iets aan de hand?" (berichten) stonden nog altijd los bovenaan de pagina, vóór het inloggen; ze zijn nu de inhoud van een nieuwe, altijd-zichtbare **INFO**-tab (eerste in de tabbalk, ook zonder pincode bereikbaar) — de tabbalk zelf staat nu buiten de vroegere `#gated`-wrapper (die is komen te vervallen) en HUIS/BOOT/IO/FOTOS/INSTELLINGEN-tabknoppen verschijnen/verdwijnen nu individueel op basis van het rechtenniveau (`setLock()` herschreven: GAST/LOGE/DELER tonen HUIS+BOOT naast INFO, EIGENAAR toont alles). (2) INSTELLINGEN heeft nu een 2e-niveau tabbalk — **BOOT** (boot/eigenaar-velden, ongewijzigd), **VERBINDINGEN** (nieuw, zie hieronder), **TOEGANG** (pincode wijzigen + gasten pincodes, verplaatst) — via nieuwe `setSubTab()`, zelfde patroon als de hoofd-tabbalk maar gescopet tot `#tabInstellingen`. (3) **MELDINGEN (CallMeBot Signal/WhatsApp) nu volledig instelbaar vanuit de webapp** (`INSTELLINGEN → VERBINDINGEN`): hoofdschakelaar, bij-opstart, hartslag (UIT/DAGELIJKS/WEKELIJKS + uur/dag), eigenaar Signal-/WhatsApp-code + alt.nrs, en alle 4 extra ontvangers (naam/telefoon/Signal-code/WhatsApp-code/alt.nrs/3 categorie-vinkjes) — expliciete reden van Brendan: de lange CallMeBot-code is op een telefoon/computer te *plakken*, op het scherm van de boordcomputer moet die met de virtuele toetsen worden overgetypt. Nieuwe WebSocket-boodschappen/-commando's in `bkos_client.ino`: `melding_get`/`melding_set` (`_melding_json()`, geneste JSON voor het lezen, platte `e<i><veld>`-sleutels voor het schrijven — consistent met de bestaande grove maar allocatievrije parseeraanpak in dit bestand) en `melding_test` (roept de al bestaande, niet-blokkerende `melding_test()` aan) — alle drie eigenaar-only. Nog niet door Brendan getest. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.9` |
| 246 | Sessie 42 | **Foto-uploadfout preciezer gemaakt + pinch-to-zoom bij bijsnijden, berichten krijgen verplichte afzender + vrije tekst, nieuwe noodgeval-knoppen, webapp INFO-tab nu in 11 talen**: vier verzoeken van Brendan na het testen van de vorige batch. (1) **Foto's**: Brendan meldde dat uploaden nog steeds mislukt en vermoedde de nieuwe knijp/zoom-bijsnijdstap. Code-review van de crop-pijplijn in zowel de hoofd-SPA (`webapp_html.h`) als de losstaande `/fotos`-pagina (`webapp_haven_html.h`) leverde geen functionele bug op (de eerder gevonden "zwarte foto"-fix stond er al) — de generieke foutmelding "opslag vol of ongeldig bestand" verhulde echter WÁT er precies misging. `haven_gebruikersfoto_opslaan()` (haven_achtergrond.h/.ino) geeft nu een `reden_out` mee die onderscheid maakt tussen te weinig vrije ruimte, alle 30 slots bezet, en een daadwerkelijke SPIFFS-schrijffout (bekend fragmentatieprobleem: een schrijfactie kan mislukken ondanks "genoeg" gerapporteerde vrije ruimte) — `webapp.ino`'s `/fotos/upload`-route en beide webpagina's tonen nu de exacte reden. Als extra verbetering: **twee-vinger pinch-to-zoom** toegevoegd aan de bijsnijd-modal in beide bestanden (naast de bestaande schuifregelaar) via Pointer Events-multitouchtracking (`cropPointers`), voor natuurlijker zoomen op een telefoon. (2) **Berichten krijgen verplichte afzendergegevens**: `bericht.h/.ino` en de `/bericht/verzend`-route in `webapp.ino` eisen nu naam + telefoonnummer (server-side afgedwongen), meegestuurd ín het bericht (`"<boot> - <naam> (<tel>): <tekst>"`) zodat de eigenaar altijd weet van wie een bericht komt; onthouden via localStorage zodat dit maar één keer per toestel hoeft. Het on-device scherm (`screen_bericht.ino`, geen wijziging van gedrag nodig) stuurt vaste afzender "Boordscherm" mee. Toegevoegd: een vrije-tekstveld naast de vaste presets (`bericht_verzend_vrij()`, max 70 tekens) voor als geen van de standaardteksten past. (3) **Noodgeval-knoppen**: Brendans idee — een aparte, vaste (niet door de eigenaar bewerkbare) knoppenset voor echte noodsituaties: "Je bent op drift" (alléén zichtbaar/bruikbaar in ANKER-vaarmodus — `vaar_modus`/`MODE_ANKER` uit `app_state.h`), "De boot ligt niet (meer) goed vast", "Het regent in", "Je boot zinkt of dreigt te zinken", "Je bent aangevaren". Naam/telefoon zijn hier bewust *optioneel* (nieuwe functies `bericht_verzend_nood()`, routes `/bericht/nood/lijst` en `/bericht/nood/verzend`) — een noodmelding mag nooit blokkeren op ontbrekende velden. Gaat naar meldingscategorie ALARM i.p.v. EIGENAAR, zodat ook extra ontvangers die zich voor alarmen hebben ingeschreven het meekrijgen. Eigen rood-getinte knopstijl (`.mbtn.nood`) voor visuele urgentie. (4) **INFO-tab nu in 11 talen**: Brendan wil de berichtenfunctie toegankelijk maken voor niet-Nederlandstalige gasten. Een vlaggetjesrij bovenaan de INFO-tab (NL/EN/DE/FR/ES/UK/PL/CS/DA/PT/IT) kiest de weergavetaal, onthouden in localStorage — vertaalt alle vaste UI-tekst (`data-i18n`/`data-i18n-ph`-attributen + `taalToepassen()`) én de vaste noodgeval-knoppen en de 6 standaard-berichtpresets (via een vertaaltabel, gematcht op exacte Nederlandse brontekst). Een door Brendan zelf aangepaste presettekst kan niet automatisch vertaald worden (geen vertaaldienst aan boord: het apparaat heeft geen relevante internettoegang daarvoor) en verschijnt dan gewoon in het Nederlands. Berichten worden hoe dan ook altijd in het Nederlands naar de eigenaar verstuurd — alleen de webpagina zelf wordt vertaald. Nog niet door Brendan getest. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260912.10` |
| 247 | Sessie 42 | **Staande/liggende achtergrondfoto's daadwerkelijk laten passen, Berichten-sectie herschikt (nood+presets samengevoegd, altijd bovenaan blijven), tabblad-keuze bij inloggen volgt het apparaat, foto-toegang via IP-adres bevestigd**: eerste testronde van Brendan na de vorige batch. (1) De gewone HAVEN-foto lukte de eerste keer niet maar de tweede wel (waarschijnlijk een eenmalige SPIFFS-hik, geen actie ondernomen); **de staande/liggende webapp-achtergrondfoto lukte echter consequent niet**. Root cause gevonden: die twee sloten stonden op **1280×720/1536KB per stuk** (tot 3MB voor 2 foto's) terwijl de daadwerkelijk geflashte SPIFFS-partitie maar ~2MB is, gedeeld met alle andere data — een achtergrondfoto van bijna 1,5MB paste daar simpelweg vrijwel nooit in. Verkleind naar **960×540/720×960, max 400KB per stuk** (`CROP_TARGETS`/`ACHTERGROND_MAX_BYTES` in webapp_html.h/webapp.ino) — puur een CSS-achtergrond achter een donkere overlay, dus nauwelijks zichtbaar verschil. `/achtergrond/upload` in webapp.ino kreeg ook een vrije-ruimte-precheck en dezelfde reden-granulariteit (ruimte/schrijffout) als de HAVEN-foto's vorige ronde, i.p.v. altijd dezelfde generieke "opslag"-melding. (2) **Layout- en volgorde-wensen voor de INFO-tab**: (a) de pagina scrollde op een telefoon vanzelf omlaag naar het naam-veld, weg van de taalvlaggetjes/het pincode-slotje bovenin — opgelost met een geforceerde `window.scrollTo(0,0)` ná layout (dubbele `requestAnimationFrame`) plus `autocomplete="off"` op de naam/telefoon-velden; (b) "Noodgeval?" en "Iets aan de hand?" zijn samengevoegd tot één **Berichten**-sectie in de gevraagde volgorde: kop "Berichten" → ondertekst "Stuur een bericht aan de eigenaar van deze boot." → naam+telefoon → rood label "Noodgeval" + de nood-knoppen (naam/telefoon blijven daar optioneel) → de gewone preset-knoppen → het vrije-berichtveld als afsluiter. I18N-sleutels `kopNood`/`noodUitleg` vervielen, nieuwe sleutel `labelNood` in alle 11 talen. (3) **Tabblad-keuze bij inloggen volgt het apparaat**: `_state_json()` in bkos_client.ino stuurt er een `"h"`-veld bij (1 als `laatste_hoofdscherm==SCREEN_HAVEN`, anders 0 voor `SCREEN_MAIN`) — de webapp opent na een geslaagde login (`setLock(true,...)`, de enige plek waar dat gebeurt) meteen tabblad **HUIS** of **BOOT**, passend bij welk van de twee "thuis"-schermen op de boordcomputer zelf het laatst actief was, i.p.v. altijd op INFO te blijven staan. (4) **Toegang via IP-adres**: bleek al te werken (BESTANDEN-scherm toont bij een actieve hotspot al `192.168.4.1`, en het WIFI-scherm toont bij een STA-verbinding al het eigen IP) — wél een echte bug gevonden en gefixt: `screen_bestanden.ino` beweerde tijdens een actieve hotspot ten onrechte "geen WiFi-verbinding — webapp niet bereikbaar" (die check keek alleen naar een STA-verbinding, niet naar de hotspot), precies de situatie waarin je dat handmatige IP-adres nodig hebt als het automatische captive-portal-popupje een keer niet verschijnt. Toont nu `http://192.168.4.1/fotos` zodra de hotspot actief is. Nog niet door Brendan bevestigd. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260913.1` |
| 248 | Sessie 42 | **Numerieke foutcodes bij foto-uploads, eenmalige schrijf-herkansing tegen SPIFFS-hikjes, achtergrondfoto via een eigen vaste laag i.p.v. onbetrouwbare background-attachment:fixed**: reactie van Brendan op de vorige batch — de "schrijffout" bleef terugkomen (ook bij een gewone HAVEN-foto, en bij de achtergrond die het daarna weer wél deed), en hij wilde korte foutcodes kunnen doorgeven ("fout 3.4") i.p.v. steeds de hele melding te moeten overtypen. (1) **Foutcodes toegevoegd** in webapp_html.h en webapp_haven_html.h: eerste cijfer = welke foto (1=HAVEN-dashboard, 2=achtergrond liggend, 3=achtergrond staand), tweede cijfer = de reden (1=pin, 2=te groot, 3=te weinig ruimte, 4=schrijffout, 5=alle plekken bezet, 6=leeg/verbinding, 7=bijsnijden mislukt) — rechtstreeks afgeleid van dezelfde "reden"-sleutel die de server al teruggaf, dus geen aparte vertaaltabel nodig (`foutCode()`/`uploadFoutTekst()`). (2) **Onderzoek naar de daadwerkelijke oorzaak van "schrijffout"**: Brendans theorie dat de foto pas ná bijsnijden+comprimeren in het geheugen zou moeten komen (dan maar 30-40KB nodig) bleek al precies te kloppen met hoe het client-side al werkte (`cropBevestig()` snijdt bij en comprimeert op een `<canvas>`, pas de kleine resulterende blob wordt verstuurd) — het probleem zit dus niet in die volgorde, maar in de SPIFFS-partitie zelf op het apparaat: een bekende beperking van (het oudere) SPIFFS is dat een schrijving kan mislukken ondanks voldoende gerapporteerde vrije bytes, omdat vrije ruimte gefragmenteerd kan zijn — dat is verbaal aan Brendan uitgelegd, geen valse belofte dat dit met code alleen volledig op te lossen is (de echte, duurzame oplossing is de al eerder geplande overstap naar een groter/vers geformatteerd bestandssysteem bij de toekomstige herflash). Als praktische verzachting: **één automatische directe herkansing** bij een schrijffout, zowel voor HAVEN-foto's (`_hav_schrijf_probeer()` in haven_achtergrond.ino) als voor de achtergrond (`_ag_schrijf_probeer()` in webapp.ino) — SPIFFS kan een schrijving soms momentaan weigeren (bv. tijdens interne housekeeping) terwijl een meteen daaropvolgende poging wel lukt. (3) **Achtergrondfoto blijft nu staan tijdens scrollen**: `body{background-attachment:fixed}` bleek de reden dat de foto op een telefoon niet "vast" aanvoelde tijdens scrollen (bekende iOS Safari-onbetrouwbaarheid van die CSS-eigenschap) — vervangen door een los `#bgFixed`-element op `position:fixed;inset:0;z-index:-1` met `background-size:cover;background-position:center`, een aanpak die op alle mobiele browsers wél werkt: de foto staat zo groot en gecentreerd mogelijk vast, de pagina-inhoud scrollt er overheen. Nog niet door Brendan bevestigd. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260913.2` |
| 249 | Sessie 42 | **SPIFFS-plafond bevestigd (geheugenmodel al correct), crop-scherm gehard tegen stille storingen, PANEEL-knoppen "op slot"-bug gevonden, HUIS krijgt Paneel + ALLES AAN/UIT omvat nu ook de basisverlichting**: Brendans doorlopende foto-diagnose leverde met fout 3.4 (achtergrond staand) en 1.4 (HAVEN) meteen extra data op, plus twee nieuwe, losstaande klachten over PANEEL-sloten en de HUIS-tab. (1) **SPIFFS-theorie getoetst**: Brendans vermoeden dat de VOLLEDIGE foto (soms 3MB+) vóór het comprimeren al in het geheugen zou belanden, bleek niet te kloppen — dat gebeurt al zoals gewenst (bijsnijden op een klein `<canvas>` ter grootte van de doelresolutie, pas dáárna comprimeren, en alleen die kleine blob van typisch enkele tientallen KB wordt ooit verstuurd/opgeslagen). Dat "verwijderen van een oude foto hielp, terwijl er al genoeg vrije ruimte leek" bevestigt juist de eerder genoemde SPIFFS-beperking: het bestandssysteem reserveert zelf een marge voor zijn eigen huishouding en weigert daaronder ÉLKE nieuwe schrijving, hoe klein ook — verbaal uitgelegd, er is geen valse belofte gedaan dat dit met code alleen te repareren is. (2) **Crop-scherm gehard tegen stille storingen**: Brendan meldde dat het bijsnijdscherm bij een nieuwe achtergrond-upload soms helemaal niet meer opende, zonder enige foutmelding. Twee reële gebreken gevonden en gefixt in zowel webapp_html.h als webapp_haven_html.h: (a) `URL.createObjectURL(file)` werd nooit met `URL.revokeObjectURL()` opgeruimd — bij herhaald uploaden in dezelfde sessie bleven alle volledige originelen (soms 10+MB per stuk) in het browsergeheugen hangen, wat op een telefoon uiteindelijk geheugendruk kan geven; nu opgeruimd zodra een crop wordt geannuleerd, bevestigd, of een nieuwe wordt gestart. (b) Er was geen enkele vangnet als het laden van de foto simpelweg nooit "klaar" meldde — nu een 8-seconden-timeout die altijd een zichtbare (en doorgeefbare, fout X.8) melding toont, zodat "helemaal geen foutmelding" voortaan niet meer kan voorkomen. (3) **PANEEL-knoppen "beginnen soms allemaal op slot, dan ineens niet"-bug gevonden**: de server pusht paneel/lampen/state-data al vóór het inloggen (bij het openen van de WebSocket-verbinding), dus de allereerste render gebeurde met niveau=0 (alles op slot) — en omdat `setTab('huis'/'boot')` na een geslaagde login niet opnieuw rendert, bleef die verouderde "op slot"-weergave staan tot toevallig een latere broadcast een her-render triggerde. Gefixt: `setLock()` roept nu direct na het bijwerken van het niveau `renderPaneel(); renderHuis(); renderState();` aan. (4) **HUIS-tab uitgebreid**: Brendan wil daar dezelfde bediening als op de boordcomputer zelf — Paneel-knoppen (TV e.d.) staan nu ook onder HUIS (`paneelGridHuis`, zelfde data/index als de bestaande BOOT-versie, dus geen aparte state nodig), en **ALLES AAN/ALLES UIT omvat nu ook de basisverlichting** (voorheen bleef de hoofdlamp als enige buiten "ALLES" — `lamp_alles`-handler in bkos_client.ino aangevuld). Nog niet door Brendan bevestigd. Lokaal gecompileerd en groen op alle 6 platforms. Beta `0.2.260913.3` |
| 250 | Sessie 42 | **FATFS i.p.v. SPIFFS via de installer, WiFi-modemslaap uit (trage/gemiste verbindingen), expliciete inlogstatus in de webapp**: Brendans structurele vervolg op de aanhoudende SPIFFS-schrijffouten, plus twee losse klachten. (1) **FATFS-migratie**: `BKOS_NUI/partitions_s3_16mb.csv` (alleen de ESP32-S3-build, `nvs`/`otadata`/`app0`/`app1` ongewijzigd t.o.v. Arduino's eigen `default_8MB`-schema — alleen de data-partitie is al maanden 9,5MB in plaats van 1,5MB, maar nooit als zodanig naar een fysiek apparaat geschreven omdat OTA nooit de partitietabel zelf meestuurt) heeft de data-partitie nu subtype `fat` i.p.v. `spiffs` — FATFS blijkt in de praktijk minder snel schrijvingen te weigeren bij hoge bezetting dan SPIFFS. `platform_fs.h`'s `bkos_fs_mount()` hoefde niet aangepast: die probeert al SPIFFS dan FFat en werkt daardoor vanzelf door op bestaande (nog-SPIFFS) apparaten totdat ze herflashen. Om dit zonder Arduino IDE te kunnen doen is `.github/workflows/build.yml`'s S3-job uitgebreid om ook `bootloader.bin`/`partitions.bin` te publiceren (arduino-cli produceert ze toch al, alleen de app-`.bin` werd voorheen gecommit), en heeft `installer/index.html` een nieuwe, uitdrukkelijk gemarkeerde **"Volledige herinstallatie"**-optie (alléén zichtbaar bij ESP32-S3 + reguliere BKOS-NUI-firmware) die bootloader+partitietabel+app in één keer wegschrijft op exact dezelfde offsets als arduino-cli's eigen esptool-commando (0x0/0x8000/0xE000/0x10000) — met een aparte, uitgeschreven waarschuwing plus een `confirm()`-bevestiging vóór het echt begint, want dit **wist alle op het apparaat opgeslagen bestanden** (foto's, WiFi, gasten-pincodes, kanaalnamen, meldingsinstellingen) onherstelbaar. Nog niet door Brendan getest (vereist eenmalig USB). (2) **Webapp opent soms traag/onbetrouwbaar via IP**: `WiFi.setSleep(false)` toegevoegd in zowel `_wifi_verbinden_intern()` als `wifi_hotspot_starten()` (wifi.ino) — WiFi-modemslaap (standaard aan) laat de radio tussen beacons in slapen, waardoor een nieuwe inkomende verbinding tot een seconde of meer kan blijven hangen vóór 'm opgepikt wordt; precies het "een nieuwe poging of lang wachten helpt"-gedrag dat Brendan meldde. Een boordcomputer aan vaste voeding hoeft niet op stroomverbruik te letten. (3) **Expliciete inlogstatus**: Brendan wil "uitgelogd" ook als zichtbare status i.p.v. alleen het kleine slotje-icoon — de headerregel (`hdrSub`) toont nu verbinding + inlogstatus samen (bv. "verbonden · Eigenaar" / "verbonden · Uitgelogd"). Het "per boordcomputer een pincode onthouden zodat je (en een gast) maar één keer hoeft in te loggen" bleek al te bestaan (localStorage, per-origin dus vanzelf al per boordcomputer-IP gescheiden, met stille automatische herauthenticatie bij het openen van de WebSocket) — geen aparte cookie-implementatie toegevoegd, dat zou een bestaande, werkende oplossing dupliceren. Nog niet door Brendan bevestigd. Lokaal gecompileerd en groen op alle 6 platforms (incl. testcompile met de nieuwe FAT-partitietabel). Beta `0.2.260914.1` |
| 251 | Sessie 42 | **Installer: pincode verplicht vóór een volledige herinstallatie + automatisch instellen erna**: vervolg op taak 250, waarvan Brendan de "Volledige herinstallatie" nog niet zag verschijnen (GitHub Pages bleek inmiddels wél bijgewerkt te zijn — vermoedelijk een cache- of timingkwestie, of hij had ESP32-S3 nog niet als hardware geselecteerd, wat nodig is vóór die sectie zichtbaar wordt) en expliciet vroeg om een pincode te eisen vóór het formatteren én die na afloop meteen actief te maken — al eerder genoemd als openstaande wens bij taak 239. Puur `installer/index.html`, geen firmwarewijziging nodig: een nieuw pincode-veld bij de "Volledige herinstallatie"-sectie is verplicht (4 cijfers, gevalideerd in `doFlash()` vóór de bevestigingsvraag) — dat wordt de NIEUWE eigenaarspincode. Nieuwe **Stap 5** (verschijnt alleen als de herinstallatie-optie aangevinkt is): een knop "Pincode nu instellen" die, ná het sluiten van het installatievenster, verbinding maakt met `ws://192.168.4.1:8080/` (het vaste adres van een vers apparaat — geen WiFi-instellingen meer na de formattering, dus het start automatisch zijn eigen open hotspot), inlogt met de bekende **fabriekspincode 0000** (`screen_config.ino`'s standaardwaarde zolang er nog geen pincode-bestand bestaat) en meteen `pin_wijzig` naar de zelfgekozen code stuurt — volledig hergebruik van bestaande websocket-commando's, geen nieuwe firmwarefunctionaliteit. Omdat de installer-pagina zelf over HTTPS draait (GitHub Pages) terwijl het apparaat plain `ws://` op een lokaal IP is, kan een browser dit als "gemixte inhoud" blokkeren (niet met zekerheid te verifiëren zonder een echte browser+apparaat) — daarom staat er een handmatige terugvaloptie naast de knop: een link naar `http://192.168.4.1/` (zelf-http dus geen mixed-content-probleem) om de pincode alsnog via de gewone webapp-UI te wijzigen. Nog niet door Brendan getest. Betreft alleen `installer/index.html` — geen firmwarewijziging, dus geen versiebump (conform bestaande afspraak dat niet-firmware-pushes geen bump krijgen). |
| 252 | Sessie 42 | **Keuze tussen FATFS en SPIFFS bij een volledige herinstallatie**: Brendan kreeg geen keuze te zien en wilde die wel — beide op dezelfde ~9,5MB opslagpartitie, met de kanttekening dat hij zelf al vermoedt dat SPIFFS op zo'n grote partitie minder geschikt is (bevestigd: dat blijft het bekende risico uit taak 249/250). Nieuwe `BKOS_NUI/partitions_s3_16mb_spiffs.csv` — identiek aan `partitions_s3_16mb.csv` (dezelfde nvs/otadata/app0/app1/coredump-offsets) op één regel na: subtype `spiffs` i.p.v. `fat`. De partitietabel raakt de app-code zelf niet, dus geen tweede volledige compile nodig — `build.yml`'s S3-job zet er een losse stap bij die deze CSV met `gen_esp32part.py` (dezelfde tool die arduino-cli zelf intern gebruikt) omzet naar `firmware/bkos_esp32s3_8048s070.partitions_spiffs.bin`, naast de al bestaande FATFS-variant. Zowel `installer/index.html` (twee keuzerondjes FATFS/SPIFFS bij de "Volledige herinstallatie"-sectie, FATFS als aanbevolen standaard) als de BKOS-installer .exe (BKOS-installer-repo, aparte radiobuttons + `bootloader_bin`/`partitions_bin_fat`/`partitions_bin_spiffs` in de platformconfig) gebruiken 'm om de juiste partitietabel-URL te kiezen vóór het flashen. Nog niet door Brendan getest. Betreft alleen de installer/CI-kant — geen firmwarewijziging, dus geen versiebump. |
| 253 | Sessie 42 | **Echte oorzaak gevonden: partitietabel alleen vervangen wist de data niet — data-partitie moet expliciet gewist worden**: Brendan probeerde de "Volledige herinstallatie" (via de BKOS-installer .exe): met FATFS liep het vast, met SPIFFS "lukte" het, maar zijn oude foto's stonden er nog op en alles werd traag. (1) **Root cause**: de installer verving alleen de partitietabel (grotere ~9,5MB data-partitie) maar wiste nooit de daadwerkelijke flash-inhoud erachter. Bij SPIFFS→SPIFFS bleef de oude, kleinere filesystem aan het begin van die range gewoon een geldige SPIFFS-structuur vormen — `SPIFFS.begin(false)` (platform_fs.h) mount die dus stilzwijgend weer, in plaats van te falen en een verse format af te dwingen: vandaar de overgebleven foto's, én de traagheid (de gemounte filesystem-grootte klopt niet meer met de nieuwe, grotere partitiegrootte). De FATFS-"vastloper" is vermoedelijk geen echte hang: `SPIFFS_BEGIN()` (`hardware.ino`) draait vóór `tft_setup()` (het scherm moet nog de PCLK uit SPIFFS lezen), dus een eerste-opstart-format kan tot een minuut het scherm zwart/onveranderd laten zónder enige feedback — niet met zekerheid te onderscheiden van een echte hang zonder hardware, dus verbaal uitgelegd i.p.v. een risicovolle wijziging aan de vroege opstartvolgorde te wagen. (2) **Fix in de BKOS-installer .exe**: nieuwe `_esptool_uitvoeren()`-hulpfunctie (gedeeld tussen erase/write) en een expliciete `erase_region`-stap op precies `DATA_PARTITIE_OFFSET`/`_GROOTTE` (0x670000/0x980000, dezelfde range als beide partitietabellen) vóórdat bootloader/partitietabel/app geschreven worden — dwingt nu in alle gevallen (FATFS én SPIFFS) een verse format af. (3) **Fix in de webinstaller**: ESP Web Tools kent geen los adres-bereik om te wissen (alleen een alles-of-niets "wis dit apparaat"-prompt) — `new_install_prompt_erase` staat daarom nu aan bij een volledige herinstallatie, met een tekst die Brendan erop wijst dat vinkje ook echt aan te zetten. Beide installers waarschuwen nu ook expliciet dat de eerste opstart na een volledige herinstallatie tot een minuut stil kan lijken te staan. Nog niet door Brendan opnieuw getest. Betreft alleen installer-code — geen firmwarewijziging, dus geen versiebump. |
| 254 | Sessie 43 | **Snel opstarten: paneel/IO eerst, achtergrond-init (meteo/apps/net/enz.) verplaatst naar ná de eerste hertekening + on-screen boot-timing**: Brendan wilde expliciet dat het paneel/de schakelaars zo snel mogelijk bruikbaar zijn ("dat is wel mijn schakelpaneel"), en meldde daarna dat een fysiek apparaat (met echte ATtiny/IO-modules) 75 seconden nodig heeft om op te starten, ook bij een gewone herstart (niet alleen na formatteren) — dus een structureel probleem, geen eenmalig format-effect. `hw_setup()` herschikt: `io_boot()` + `io_cyclus(true)` + `io_boot_vaarmodus_bepalen()` + `io_verlichting_update()` + `info_laden()` + `haven_gebruikersfotos_scannen()` + `paneel_laden()` + `lamp_laden()` draaien nu VÓÓR de splash/scherm-klaar-status, in plaats van ertussen/erna. Nieuwe `_hw_achtergrond_init_eenmalig()` bundelt alles wat na codereview veilig een tel later kan: `data_setup()`, `meteo_setup()`, `getijdata_init()`, `ota_setup()`, `fout_log_setup()`, `slaap_reset_reden_verwerken()`, `victron_setup()` (compileert toch naar een no-op op de huidige core 2.0.17-build — expliciet geverifieerd en de eerdere aanname dat BLE-init de trage boot verklaarde ingetrokken), `brug_setup()`, `app_setup()`, `net_setup()`, `melding_setup()`, `gast_laden()`, `bericht_laden()`, en (indien `BKOS_REMOTE_ENABLED`) `bkos_client_setup()`/`webapp_setup()` — aangeroepen als eenmalige actie vanuit `hw_loop()` ná de eerste iteratie (`scherm_bouwen=true` aan het eind forceert een verse hertekening zodra dit klaar is, voor schermen die van deze data afhangen). Overbodige `if (splash) delay(1000);` verwijderd (`recovery_check()` blokkeert toch al 2000ms). **On-screen boot-timing-instrumentatie** toegevoegd (`_splash_stap_toon()`, overschrijft één regel onder het logo met "stapnaam: Xms", `tft_flush(true)` erna) op vier meetpunten: bij het tonen van het logo ("boot tot scherm"), ná `io_boot()` ("IO-detectie") en ná `lamp_laden()` ("config geladen") — bewust op het scherm i.p.v. Serial, want `Serial`≡`IO_SERIAL` op S3 (UART naar de ATtiny) en de gebruiker heeft niet altijd een seriële monitor paraat. Codeonderzoek sloot `data_setup`/`getijdata_init`/`app_setup`/`fout_log_setup`/`brug_setup`/`net_setup` al uit als blokkerend; `io_boot()`/`io_detect()` (ATtiny/module-UART-detectie, moet schalen met het werkelijke aantal aangesloten modules) blijft de meest waarschijnlijke boosdoener voor de 75 seconden — nog niet bevestigd, de drie nieuwe schermregels moeten dat op Brendans eigen apparaat blootleggen. **Nog niet op echte hardware getest.** Lokaal gecompileerd en groen op S3 en WROOM (Brendans expliciete instructie: alleen voor de 7" S3 hoeft dit nu te werken, andere platforms zijn voor later als ze falen) — CYD28/CYD40H/CYD40V/Pico nog niet herbevestigd met deze exacte code. Beta `0.2.260914.2` |
| 255 | Sessie 43 | **Webapp: bewerk-potlood terug op de INFO-tab + instellingen exporteren/importeren (backup)**: Brendan miste een manier om boot/eigenaar-info in te vullen "als ik bij info zit". Editen bestond al (INSTELLINGEN → BOOT, sinds taak 242) maar was vanaf de publieke INFO-tab niet vindbaar — na inloggen als eigenaar springt de pagina bovendien meteen naar HUIS/BOOT (taak 247), dus INSTELLINGEN werd nooit toevallig gezien. Nieuw: een potlood-icoontje (✎) naast "Boot & eigenaar" op de INFO-tab, alleen zichtbaar als eigenaar ingelogd, dat direct naar INSTELLINGEN → BOOT springt (`infoEditBtn`, zichtbaarheid gekoppeld aan dezelfde `eigenaar`-vlag als de andere eigenaar-only tabs in `setLock()`). Het bestaande on-device BEWERK-knopje op `screen_info.ino` zelf liet bij codereview geen regressie zien (PIN-overlay/`info_bewerkbaar`-flow ongewijzigd) — nog te bevestigen door Brendan of dat op het apparaat zelf ontbreekt. **Instellingen-export/import**: nieuwe subtab **BACKUP** in INSTELLINGEN (`webapp_html.h`) plus twee nieuwe HTTP-routes in `webapp.ino`: `/instellingen/export` (GET, eigenaar-only) leest een vaste whitelist van 15 bekende configuratiebestanden (`BACKUP_BESTANDEN[]`: boot/eigenaar, IO-namen+configuratie, PANEEL/LAMPEN, meldingen, gasten-pincodes, berichtpresets, pincode, netwerkrol, WiFi-credentials, Lua-datastore — bewust geen foto's/achtergronden) en bundelt ze als één JSON-bestand (`{"files":{"/pad":"inhoud",...}}`, handmatige JSON-escape, geen ArduinoJson nodig voor deze platte structuur) met `Content-Disposition: attachment` zodat de browser het als bestand downloadt; `/instellingen/import` (POST, eigenaar-only, raw JSON-body) schrijft uitsluitend naar paden uit diezelfde whitelist terug (nooit een pad rechtstreeks van de client — voorkomt willekeurige bestandsoverschrijving) en plant een herstart 1,2s later (`webapp_loop()`) zodat alle bij-opstart-ingelezen state (config/info/paneel/lampen/IO-cfg/enz.) vers wordt ingelezen. Bedoeld voor Brendans wens om bij een FS-wissel (SPIFFS↔FATFS, taak 250-253) of een nieuwe boordcomputer niet alles handmatig opnieuw te hoeven instellen. Nog niet door Brendan getest. Lokaal gecompileerd en groen op S3. Beta `0.2.260914.3` |
| 256 | Sessie 43 | **Boot-timing: veel meer, persistent bekijkbare checkpoints + HAVEN-fotoscan uit het kritieke pad**: Brendan bevestigde dat IO-detectie zelf snel is (~1025ms), maar dat er daarna "heel lang" gewacht wordt — en de op-scherm regel (steeds dezelfde regel overschreven) toonde de derde checkpoint ("config geladen") nooit, omdat die ofwel te lang op zich liet wachten ofwel meteen weer overschreven werd. Twee concrete stappen. (1) **`_boot_stap()`** (hernoemd van `_splash_stap_toon()`) logt voortaan ALTIJD naar een nieuw, klein `boot_log.h/.ino`-moduletje (los bestand i.p.v. in `hardware.h`, want die include zelf al `screen_info.h` — cirkel voorkomen) — het op-scherm overschrijven blijft een optionele live-indicator, maar is niet langer de enige manier om de data te zien. De ene lump-som "config geladen"-checkpoint is opgesplitst in losse metingen na élke stap: "IO-detectie", "IO stil ingelezen", "vaarmodus+verlichting", "info geladen", "paneel+lamp geladen", "hw_setup klaar" en (vanuit `_hw_achtergrond_init_eenmalig()`) "achtergrond-init klaar" — zodat precies zichtbaar wordt welke van deze op zichzelf kleine SPIFFS/FATFS-stappen de tijd kost. Nieuw **OPSTARTLOG-knopje** in INFO → SYSTEEM (`screen_info.ino`, alleen groot scherm) opent een overlay met alle stappen+duur; vaste, niet-sequentiële rijpositie (`INFO_SYS_LOG_Y`/`_H`) om het "getekend hier, hittest daar"-mismatchpatroon te vermijden dat dit project al eerder trof — de rechterkolom (netwerk/master-info) is een rij lager opgeschoven om ruimte te maken, zonder de bestaande overlap met de apparaatnaam-rij verder te verstoren. (2) Brendans eigen voorstel — "laat het paneel eerst zonder foto zien als het laden daarvan traag is" — direct toegepast: `haven_gebruikersfotos_scannen()` (SPIFFS-scan van eigen HAVEN-foto's) verplaatst van het synchrone `hw_setup()`-pad naar `_hw_achtergrond_init_eenmalig()`; niet nodig om het PANEEL/hoofdscherm te tonen, alleen voor de achtergrondfoto op het HAVEN-dashboard (die dan pas na de deferred-init verschijnt, met een geforceerde herteken via `scherm_bouwen=true`). Brendans hypothese dat het probleem in "algemeen SPIFFS-lezen op zo'n grote schijf" kan zitten (en dat dan FATFS écht moet gaan werken, of terug naar kleine SPIFFS) blijft openstaand — deze checkpoints moeten dat empirisch bevestigen of ontkrachten. Nog niet op echte hardware getest. Lokaal gecompileerd en groen op S3. Beta `0.2.260914.4` |
| 257 | Sessie 43 | **Boot-timing bevestigd voldoende (geen verdere actie nodig) + HAVEN-tegels: drukte-afhankelijke oplichting + zwarte standaardkleur + nieuw buitenlicht-icoon**: Brendan bevestigde dat de opstarttijd na taak 256 (HAVEN-fotoscan uit het kritieke pad + gesplitste checkpoints) al acceptabel is — geen verdere diagnose nodig, dit blijft zo staan. Drie nieuwe, losse verzoeken voor het HAVEN-dashboard. (1) **Drukte-afhankelijke tegeloplichting**: nieuwe `_hv_busyheid()` (screen_haven.ino) sampelt een grof 6×4-rooster fotopixels binnen een tegel en middelt het verschil in ruwe helderheid tussen opeenvolgende samples — een rustig stukje foto (vlakke lucht/water) geeft een lage waarde, een druk/contrastrijk stukje (want, golven, bebouwing) een hoge. `_hv_sterkte()` zet dat om in een oplicht-sterkte tussen een basis- en maximumwaarde (`HV_TILE_LICHT`/`_MAX`=128/225 inactief, `HV_TILE_GROEN_LICHT`/`_MAX`=150/235 actief) — een tegel op een drukke foto-plek wordt dus minder doorschijnend (lichter/meer dekkend) dan een tegel op een rustige plek, precies Brendans vraag. `_hv_licht()`/`_hv_licht_groen()` nemen de sterkte nu als parameter i.p.v. een vaste macro. (2) **Standaardkleur tegelinhoud naar zwart**: nieuwe `HV_CONTENT_UIT` (zwart) vervangt `C_TEXT_DIM` voor icoon/tekst van NIET-actieve tegels (lampgroepen, "overige lampen", PANEEL/APPARATEN, het ALLES-UIT-symbool) — alleen binnen screen_haven.ino, het hoofdscherm behoudt zijn gewone `C_TEXT_DIM`. `teken_icoon_lamp()` (screen_main.h/.ino, gedeeld met het hoofdscherm) kreeg een nieuwe optionele `uit_kleur`-parameter (default `C_TEXT_DIM`, dus het hoofdscherm compileert/gedraagt zich ongewijzigd) zodat HAVEN specifiek `HV_CONTENT_UIT` kan meegeven zonder de gedeelde functie voor het hoofdscherm te veranderen. (3) **Nieuw buitenlicht-icoon** (`I_DEKLICHT`, screen_main.ino, gedeeld icoon-canvas `teken_icoon()`): Brendan vond het vorige icoon (rond lampje + 3 stralen omhoog) te weinig onderscheidend van het interieur-peertje — vervangen door een dekschijnwerper-silhouet (driehoekige kap boven een lichtbron, bundel waaiert NAAR BENEDEN i.p.v. rondom/omhoog) — ander silhouet én andere straalrichting dan het interieur-icoon. Lokaal gecompileerd en groen op S3. Nog niet door Brendan gezien op het scherm. Beta `0.2.260915.1` |
| 258 | Sessie 43 | **HAVEN-fotoscan naar eigen achtergrondtaak + gedimde placeholder tot bekend is welke foto klopt**: Brendan merkte dat hij weer de ingebakken voorbeeldfoto zag, en dat pas na het bezoeken van CONFIG → BESTANDEN de eigen foto verscheen. Verklaring gevonden: `haven_gebruikersfotos_scannen()` (tot 30 `SPIFFS.exists()`-lookups + evt. naam-migratie, dus tot 90 lookups) liep als EERSTE stap in `_hw_achtergrond_init_eenmalig()` — pas als de HELE rest van die functie (data/meteo/getij/ota/foutlog/victron/brug/apps/net/melding/gast/bericht/webapp) ook klaar was, kwam `scherm_bouwen=true` en dus de kans om de zojuist bekende fotostatus alsnog te tonen. Tot die tijd bleef het HAVEN-dashboard de (mogelijk verkeerde) ingebakken foto tonen. `screen_bestanden.ino` roept dezelfde scanfunctie ook aan (zorgt dat `/fotos` bestaat/gemigreerd is) — vandaar dat een bezoek daaraan toevallig leek te "triggeren" dat de juiste foto verscheen, terwijl het gewoon min-of-meer toeval van timing was. Brendans concreet voorstel, één-op-één gebouwd: (1) de EERSTE scan (bij opstarten) draait voortaan op een eigen FreeRTOS-taak op Core 0 (`_hav_scan_taak`, `haven_achtergrond.ino`) i.p.v. synchroon binnen de achtergrond-init-keten — die keten (meteo/ota/net/webapp enz.) hoeft er niet meer op te wachten. Latere aanroepen (ná een upload/verwijdering, altijd al in reactie op een gebruikersactie) blijven bewust synchroon, ongewijzigd. (2) Nieuwe `haven_gebruikersfoto_scan_klaar()`-vlag: zolang de scan nog loopt, dimt `haven_achtergrond_pixel()`/`_klem()` (transparant voor alle aanroepers — `screen_haven.ino`'s tegels/busyheid-meting hoeven hier niets van te weten) elke opgevraagde fotokleur richting zwart (`HAVEN_LAAD_DIM`=150/255) en toont `screen_haven_teken()` een klein "foto's laden..."-label in de statusbalk (links van de TERUG-knop, schaalt vanzelf mee met SCREEN_SMALL/groot-scherm posities). Zodra de achtergrondtaak klaar is zet die zelf `scherm_bouwen=true`: is de HAVEN-tab op dat moment actief, dan hertekent `haven_achtergrond_teken()` vanzelf met de nu bekende foto (eigen foto's indien aanwezig, anders gewoon de voorbeeldfoto op volle sterkte — geen "voor altijd gedimd" als er geen eigen foto's blijken te zijn). Het paneel/de tegels blijven de hele tijd al gewoon bedienbaar (ongewijzigd t.o.v. eerder — dit raakt alleen de achtergrondfoto-weergave). Pico krijgt een simpele `haven_gebruikersfoto_scan_klaar(){return true;}`-stub (geen async-onzekerheid nodig, telt daar toch altijd 0 eigen foto's). Lokaal gecompileerd en groen op S3 en WROOM. Nog niet door Brendan getest. Beta `0.2.260915.2` |
| 259 | Sessie 43 | **HAVEN-achtergrond: gevonden dat de vorige dim-fix de foto zelf niet raakte + vervangen door een klein laad-icoon i.p.v. de voorbeeldfoto**: Brendan meldde dat hij bij opstarten nog steeds de ingebakken voorbeeldfoto op VOLLE sterkte/grootte zag (wel automatisch vervangen zodra de eigen foto's bekend zijn). Root cause: taak 258's dimming zat in `haven_achtergrond_pixel()`/`_klem()` — die functies worden alleen door de TEGELS gebruikt (screen_haven.ino), niet door de achtergrondfoto zelf. `_hab_output()` (de TJpg_Decoder-callback) tekent elk gedecodeerd JPEG-blok rechtstreeks en ongewijzigd naar het scherm (`tft.draw16bitRGBBitmap`) — de dimming raakte dat pad dus nooit. Brendans vervolgwens verving de hele aanpak: i.p.v. de (mogelijk verkeerde) voorbeeldfoto gedimd te tonen, nu een klein, herkenbaar "hier komt een foto"-icoon (fotolijst met bergje+zon, `_hab_laden_icoon()`, dim `C_TEXT_DIM`-kleur, 40×28px, gecentreerd in het contentgebied) zolang `haven_gebruikersfoto_scan_klaar()` false is — er wordt dan HELEMAAL geen JPEG gedecodeerd (`hav_fb` blijft leeg), dus de tegels erboven tonen gewoon hun effen getinte kleur i.p.v. foto-inhoud, en er is niets dat naderhand hoeft te "wijken". Zodra de achtergrondtaak (taak 258) klaar is en `scherm_bouwen=true` zet, volgt de normale, definitieve decodering (eigen foto's, of anders gewoon de voorbeeldfoto op volle sterkte). De inmiddels overbodige `HAVEN_LAAD_DIM`-blend in de pixel-getters is teruggedraaid (ongebruikt na deze aanpak). Los, kleine correctie: Brendan noemde het scherm consequent "huis paneel" i.p.v. "HAVEN" (de codenaam, gekoppeld aan de HAVEN/ANKER-vaarmodus die het opent, en losstaand van de webapp's HUIS-tab die hetzelfde soort bediening biedt) — nog niet hernoemd, aan Brendan gevraagd of dat gewenst is. Lokaal gecompileerd en groen op S3. Nog niet door Brendan getest. Beta `0.2.260915.3` |
| 260 | Sessie 43 | **Webapp-export downloadde niet + HAVEN-laadicoon groter en met grijs vlak+zwarte tekening**: (1) Brendan meldde dat de nieuwe INSTELLINGEN → BACKUP → EXPORTEREN-knop niet leek te downloaden. `instellingenExport()` (webapp_html.h) bouwde een `<a download>`-element en verwijderde het meteen na `.click()` — een bekend patroon dat in sommige browsers de net gestarte download afbreekt omdat de navigatie asynchroon start terwijl het element al uit de DOM is. Herschreven naar `fetch()` + `blob()` + een Object-URL (verwijderd ná een korte vertraging i.p.v. meteen), inclusief een zichtbare foutmelding bij een mislukte export (onjuiste pincode of verbindingsfout) — voorheen zou een foute pincode gewoon stil niets opleveren, nu duidelijk zichtbaar in `instBackupMelding`. (2) HAVEN-laadicoon (taak 259) op Brendans verzoek groter (40×28 → 64×45) en herontworpen naar een licht grijs gevuld vlak (`RGB565(210,210,210)`, grijze rand) met een ZWARTE tekening erin (was: alleen een dunne omtrek in `C_TEXT_DIM`) — beter zichtbaar tegen de effen achtergrond zonder op te vallen als een bewuste "geen foto"-keuze. Lokaal gecompileerd en groen op S3. Nog niet door Brendan getest. Beta `0.2.260915.4` |
| 261 | Sessie 43 | **HAVEN-laadscherm: hele contentgebied grijs (niet alleen een kadertje) + icoon 3x groter**: Brendan verduidelijkte dat taak 260's grijze vlak alleen het KLEINE kadertje rond het icoon grijs maakte, terwijl hij de HELE achtergrond (het volledige contentgebied, niet alleen de tekening) grijs wilde, met het bergje+zon-icoon daar los in getekend, en nog eens 3x zo groot als toen. `haven_achtergrond_teken()` vult nu bij een nog-onbekende fotostatus het volledige contentgebied (`CONTENT_Y` tot `NAV_Y`, volle breedte) met `RGB565(210,210,210)` i.p.v. alleen een klein kadertje; `_hab_laden_icoon()` tekent nu alleen nog de zwarte lijnen zelf (geen eigen vlak/rand meer, overbodig op de al-grijze achtergrond) op 192×135 i.p.v. 64×45 (3x). Lokaal gecompileerd en groen op S3. Nog niet door Brendan getest. Beta `0.2.260915.5` |
| 262 | Sessie 43 | **Webapp-export download bevestigd werkend (v.4) + tabbladbalken horizontaal scrollbaar op smalle telefoon**: Brendan bevestigde dat de instellingen-export op de computer al werkte in v.4 (taak 260's fetch+blob-fix was dus goed). Nieuwe klacht: op een telefoon (staand) liepen de tabbladknoppen (zowel de hoofdbalk INFO/HUIS/BOOT/IO/FOTOS/INSTELLINGEN als de 2e-niveau balk BOOT/VERBINDINGEN/TOEGANG/BACKUP binnen INSTELLINGEN) "in elkaar over". Oorzaak: `.tabbtn` had `flex:1;min-width:0`, waardoor alle knoppen gedwongen gelijk breed kromp om binnen de balk te passen — bij 4-6 knoppen op een smal scherm (met een woord als "VERBINDINGEN") werd elke knop smaller dan zijn eigen tekst, die dan (door `white-space:nowrap`) buiten de knop uitsteekt en overlapt met de buren. `.tabbar` had overigens al `overflow-x:auto` staan, maar kreeg nooit de kans om te scrollen omdat de knoppen zich juist steeds naar de beschikbare breedte lieten samenpersen i.p.v. hun eigen breedte te behouden. Fix: een media query (`max-width:480px`, dus alleen telefoon-formaat) zet `.tabbtn` daar naar `flex:0 0 auto` — de knoppen houden hun natuurlijke breedte, en zodra die samen breder zijn dan de balk krijgt de balk vanzelf een horizontale scrollbalk (i.p.v. te knijpen). Breder scherm (tablet/desktop) behoudt de bestaande, mooi gevulde gelijk-verdeelde balk. Geldt voor beide tabbalken (zelfde CSS-klasse), niet alleen de door Brendan genoemde 2e-niveau balk. Lokaal gecompileerd en groen op S3. Nog niet door Brendan getest op telefoon. Beta `0.2.260915.6` |
| 263 | Sessie 43 | **App-platform: opstarten in een app, volledig-scherm-apps met optionele koptekst, en vergrendeld-openhouden (basis voor kiosk-achtige toepassingen zoals een fotolijstje of escape-room-software)**: Brendan wil BKOS als basis kunnen gebruiken voor heel andere software dan het bootpaneel, bv. een fotolijstje of escape-room-app die ook de IO-functionaliteit gebruikt — te bouwen als losse Lua-apps in een aparte sessie (dus bewust GEEN app-content in deze sessie, alleen het platform). Drie samenhangende, nieuwe primitieven. **(1) Opstart-app**: nieuwe `boot_app_id[24]` (app_state.h/.ino, config-key `bootapp`, leeg=normale opstart). Omdat apps/Lua normaal pas in `_hw_achtergrond_init_eenmalig()` (na de eerste schermtekening) geladen worden — te laat om al bij de allereerste tekening in de juiste app te kunnen starten — roept `hw_setup()` nu `app_setup()` SYNCHROON aan zodra `boot_app_id` gezet is (dubbel init in de achtergrond-keten is onschadelijk, `app_setup()`/`lua_setup()` zijn beide idempotent); wie geen opstart-app instelt betaalt hier niets voor. Na de bestaande kalibratie-override zet dit `lua_forceer_app`+`actief_scherm=SCREEN_LUA_APP`, exact het bestaande mechanisme dat ook een handmatige "OPEN" op een app-rij gebruikt. **(2) Volledig scherm (app-aangevraagd)**: `AppManifest` (app_manager.h) kreeg `volledig_scherm`/`toon_header` (gelezen/geschreven via manifest.json — legitiem app-content, de app MAG dit zelf vragen). `lua_app_laden()`'s bestaande `sandbox`-parameter (bepaalde al of een app het volle scherm 0..TFT_H krijgt of alleen het content-gebied) wordt nu gevoed door `is_standalone && !volledig_scherm` i.p.v. kaal `is_standalone` — een fullscreen-app krijgt zo automatisch de volle tekenruimte via een bestaand mechanisme, geen nieuwe coördinaten-logica nodig. Koptekst/navigatiebalk worden overgeslagen tenzij `toon_header` (koptekst) resp. nooit (navigatiebalk, altijd weg bij fullscreen — anders zou de universele navbar-taphoek de vergrendeling zinloos maken, zie punt 3). Aangepast op BEIDE plekken waar dit gebeurt (ESP32 GUI-taak + Pico hw_loop). **Veiligheidskritiek**: de universele "tik overal in de navbalk-zone navigeert weg"-regel en de hoek-sluitknop van een standalone app zijn voor een fullscreen-app volledig UITGESCHAKELD (aanraking gaat dan altijd rechtstreeks naar `lua_app_run()`) — bewust als een simpele wrapper (`if (fullscreen-app-actief) { lua_app_run(...); } else { <ongewijzigde bestaande logica> }`) i.p.v. de bestaande geneste logica zelf te herschikken, om het risico op een subtiele regressie in dat al complexe dispatchpad te minimaliseren. **(3) Vergrendeld openhouden**: nieuwe `app_vergrendeld()`/`app_zet_vergrendeld()` (app_manager.cpp) — BEWUST een eigen bestand (`/apps/<id>/bkos_lock.txt`) los van manifest.json, dat de installatie-/update-flow (`apps[idx] = wm` bij het installeren vanuit de winkel, overschrijft anders het hele manifest inclusief zulke vlaggen) nooit aanraakt: een app kan dit zo onmogelijk zelf zetten, alleen de gebruiker via de nieuwe **app-instellingen-overlay** in `screen_apps.ino` (lang indrukken op een geïnstalleerde-app-rij — bewust geen extra knop in de al-krappe rij zelf, weer om een teken/hittest-mismatch te vermijden — opent AAN/UIT-schakelaars voor "opstart-app" en "vergrendeld openhouden"). **Uitgang uit een fullscreen-app**: lang indrukken (bestaande `LANG_DRUK_MS`-mechanisme, uitgebreid met een derde tak naast SCREEN_MAIN/SCREEN_APPS) sluit de app direct, of — als vergrendeld — toont eerst de bestaande PIN-overlay (`pin_vereist_tonen()`/`pin_overlay_run()`, hergebruikt zoals screen_info.ino al doet) en sluit pas na een juiste boordcomputer-pincode; zolang die overlay open staat gaan tikken daar naartoe, niet naar de app. Lokaal gecompileerd en groen op S3 én Pico (bewust ook Pico getest, want dit raakt een gedeeld, kritiek stuk touch-dispatchcode in hardware.ino). Nog volledig ongetest op echte hardware/met een echte app — Brendan bouwt de eerste testapp (fotolijstje of escape-room) in een aparte sessie. Beta `0.2.260916.1` |
| 264 | Sessie 44 | **Eerste testapp voor het fullscreen-app-platform: "Digitale Fotolijst" + nieuwe `bkos.foto.*` Lua-API**: vervolg op taak 263 — Brendan vroeg om de eerste concrete app te bouwen die het net gebouwde fullscreen-platform gebruikt. Een echte fotolijst kan geen bestaande `bkos.*`-functie gebruiken om een foto te tekenen (de sandbox heeft geen bestands-/afbeeldingstoegang) — in plaats van dat te ontwerpen als iets nieuws is de BESTAANDE fotopool van het HAVEN-dashboard hergebruikt: `haven_achtergrond.ino`'s tekenkern is losgetrokken in een gedeelde `_hab_teken_gebied(top_y, hoogte)` (identieke logica, nu parametrisch i.p.v. hardcoded op het HAVEN content-gebied), met twee dunne wrappers: `haven_achtergrond_teken()` (ongewijzigd gedrag) en nieuwe `haven_achtergrond_teken_volledig()` (tekent naar het VOLLEDIGE fysieke scherm, voor fullscreen-apps). Nieuwe `haven_achtergrond_volgende()` (directe wissel + reset van de gedeelde 60s-slideshowklok `hav_laatste_wissel`, voorheen function-local static in `haven_achtergrond_tick()`) en `haven_achtergrond_aantal_actief()`. Nieuwe sandboxed Lua-API `bkos.foto.{tekenen,tick,volgende,aantal}()` (lua_runtime.cpp) — alleen-lezen, geen bestandstoegang voor de app zelf: alleen "teken de huidige foto" en "ga naar de volgende", exact dezelfde pool die de gebruiker al via de webapp (`/fotos`) vult. Nieuwe app `appstore/apps/fotolijst/` (`volledig_scherm: true`, `toon_header: false`): tekent de foto + een klok/datum-overlay rechtsonder, tikken wisselt direct naar de volgende foto. **Belangrijke ontdekking tijdens het bouwen**: `app_installeer_uit_winkel()` downloadt bij store-installatie NOOIT het per-app `manifest.json` van GitHub — het gebruikt uitsluitend de al bij `app_winkel_laden()` geparste `appstore/index.json`-entry als manifest en haalt daarna alleen `main.lua` op. Een nieuwe fullscreen-app moet dus zijn `volledig_scherm`/`toon_header`-velden ook in `index.json` zetten, niet alleen in zijn eigen `manifest.json` (dat laatste blijft wel de brondocumentatie voor de PR-review) — nergens expliciet vastgelegd, nu meegenomen. `docs/BKOS_APP_HANDLEIDING.md` uitgebreid met §5.7 + de machine-leesbare §14-referentie. Lokaal gecompileerd en groen op S3 (68% flash). WROOM faalt in déze ontwikkelomgeving op een DRAM-BSS-overflow van 512 bytes — bevestigd pre-existing (ook op ongewijzigde `main`, dus een lokale toolchain/library-versiekwestie, geen regressie van deze wijziging). Nog niet op echte hardware getest. Beta `0.2.260916.2` |
| 265 | Sessie 44 | **ECHTE BUG gevonden: een fullscreen-app kon zichzelf direct na openen weer sluiten + fotolijst herbouwd met eigen besturing**: Brendan testte taak 264's fotolijst-app op echte hardware — "snel een foto te zien en daarna wordt het paneel geladen". Root cause in `hardware.ino`: `touch_start_ms` (de klok voor de 700ms lang-druk-detectie die een fullscreen-app sluit, taak 263) wordt alleen gereset op de RIJZENDE FLANK van een aanraking, niet bij een schermwissel — bleef de vinger van de OPEN-tik nog even liggen, of duurde het laden/eerste tekenen (een JPEG-decode, zoals deze app doet) net iets langer dan gewoonlijk, dan kon de 700ms-drempel al verstreken zijn tegen de tijd dat de app zichtbaar werd: sluit zichzelf ogenblikkelijk weer. Fix: `touch_start_ms`/`lang_druk_verwerkt` worden nu ook gereset bij elke NIEUWE app-lading (dezelfde transitie-detectie die al `lua_app_laden()` triggert), in zowel de ESP32 GUI-taak als het Pico `hw_loop()`-pad — een platformbrede fix, niet fotolijst-specifiek. **Nieuwe app-besturingsprimitieven** (Brendans concrete UX-wens: tik toont een tijdelijke besturingsbalk i.p.v. permanent zichtbare knoppen): `_fs_app_lang_druk()` hernoemd naar niet-static `fs_app_sluiten_aanvragen()` (gedeclareerd in `app_state.h`, geïmplementeerd in `hardware.ino`) zodat een app 'm ook zelf kan aanroepen → nieuwe `bkos.app.sluiten()` (lua_runtime.cpp) — dezelfde beveiligde uitgang (incl. pincode-vergrendeling) als lang indrukken, geen tweede los pad. Nieuwe `haven_achtergrond_vorige()` (mirror van `_volgende`) → `bkos.foto.vorige()`. **Fotolijst-app herbouwd**: geen instant-fullscreen-foto zonder bediening meer — één tik toont een besturingsbalk (X sluiten/◀ vorige/▶ volgende/INSTELLINGEN) die na 4s vanzelf verdwijnt; instellingen-overlay laat het wisselinterval kiezen (presets 5s–10min, gepersisteerd via `bkos.data.writeFloat("fotolijst.interval_s", ...)`) via een EIGEN klok in `bkos.update()` — bewust losgekoppeld van de gedeelde 60s-HAVEN-slideshowklok, zodat instelbaarheid hier het HAVEN-dashboard niet raakt. Lang indrukken sluit nog steeds direct, ongewijzigd. **Documentatie-gat gedicht**: `docs/BKOS_APP_HANDLEIDING.md` bleek `volledig_scherm`/`toon_header` en het hele sluitmechanisme (uit taak 263) nooit gedocumenteerd te hebben — nieuwe §8 "Volledig scherm (fullscreen-apps)" toegevoegd (secties 9-15 hernummerd, alle interne §-verwijzingen meegetrokken), §5.8 "App sluiten", `bkos.foto.vorige()`, §15 machine-referentie bijgewerkt. Lokaal gecompileerd en groen op S3 (68% flash). Nog niet opnieuw op hardware bevestigd. Beta `0.2.260916.3` |
| 266 | Sessie 44 | **Taak 265's fix loste het probleem NIET op — echte oorzaak: reset-volgorde verkeerd om + fotolijst-versienummer nooit gebumpt**: Brendan bevestigde dat het probleem er nog steeds was en meldde er los bij dat updaten van de fotolijst-app niet werkte. (1) **Echte oorzaak lang-druk-bug**: taak 265's reset van `touch_start_ms`/`lang_druk_verwerkt` gebeurde bij het detecteren van een nieuwe app-lading, maar VÓÓR de aanroep van `lua_app_teken()` (die de mogelijk trage `bkos.draw()`/JPEG-decode uitvoert) — omdat dit allemaal synchroon binnen dezelfde loop-iteratie gebeurt, liep de klok gewoon door tijdens het tekenen en kon de 700ms-drempel al (deels) verstreken zijn tegen de tijd dat de lang-druk-check verderop in DEZELFDE iteratie draaide. Fix: een `nieuwe_app_geladen`-vlag onthoudt de transitie tot NA de teken-aanroepen — de klok reset nu pas als het scherm daadwerkelijk zichtbaar is, ongeacht hoe traag het eerste tekenen was. Toegepast in beide dispatchpaden (ESP32 GUI-taak + Pico `hw_loop`). (2) **Update-detectie**: `screen_apps.ino` vergelijkt geïnstalleerde vs. winkel-versie met een kale `strcmp()` (`update_av = ... strcmp(apps[inst_idx].versie, m.versie) != 0`) — de fotolijst-app bleef "1.0.0" over twee inhoudelijke revisies (taak 264 én 265) heen, dus een al geïnstalleerd exemplaar zag nooit een update. Versie naar `1.2.0` (weerspiegelt beide gemiste bumps) in zowel `appstore/apps/fotolijst/manifest.json` als `appstore/index.json` (handmatig, zodat de sync-bot hier niets meer aan hoeft te doen). Lokaal gecompileerd en groen op S3 (68% flash). Nog niet opnieuw op hardware bevestigd. Beta `0.2.260916.4` |
| 267 | Sessie 43 | **APPS herbouwd: "Windows-bureaublad" van app-icoontjes als nieuw hoofdscherm, oude 2-panelen-scherm één laag dieper als SCREEN_APPSTORE, plus SET/UPD-knoppen bij elke geïnstalleerde app**: Brendan wil dat de APPS-knop meteen een herkenbaar, icoon-gebaseerd overzicht toont in plaats van het beheerscherm. **(1) Nieuw bureaublad** (`SCREEN_APPS`, herschreven `screen_apps_teken()/run()` in screen_apps.ino): grid van tegels — icoon + naam eronder — voor elke ACTIEVE geïnstalleerde app, plus een vaste laatste "APPSTORE"-tegel (tasje-icoon) om te installeren/bijwerken/verwijderen. Tikken op een app-tegel tekent 'm eerst NEGATIEF (kleuren omgekeerd) en flusht dat DIRECT naar het fysieke scherm (`tft_flush(true)`) vóórdat de app geladen wordt — Brendans expliciete wens voor meteen zichtbare feedback ook als het starten zelf traag is. Tikken op APPSTORE navigeert naar het nieuwe `SCREEN_APPSTORE`. Scrollbaar (hergebruikt `ui_scrollbar`) voor meer apps dan er in één keer passen. **(2) Icoon-systeem**: nieuw `AppManifest.icoon[12]` (manifest.json-veld, legitiem app-content — een app MAG dit zelf kiezen) met een vaste, kleine set namen ("tv","usb","water","licht", enz. — `_apps_icoon_van_naam()`, screen_apps.ino) die mappen op de AL BESTAANDE `teken_icoon()`-iconenset (geen los plaatjesformaat/decodeerpad nodig, blijft snel te tekenen voor een heel bureaublad tegelijk). Geen/onbekend icoon → nieuw `I_APP_GENERIEK` (2×2-blokjes-rooster, het klassieke "apps"-symbool) — Brendans expliciete wens voor apps die nog geen icoon hebben. Nieuw `I_WINKEL` (tasje) voor de vaste APPSTORE-tegel. (3) **Oude scherm één laag dieper**: het bestaande 2-panelen-scherm (links geïnstalleerd+beheer, rechts installeren vanuit de store) is volledig hernoemd naar `screen_appstore_teken()/run()/lang_indruk()` en draait nu op nieuw `SCREEN_APPSTORE` (23) — bereikbaar via de APPSTORE-tegel op het bureaublad, niet meer rechtstreeks vanuit de navigatiebalk; een nieuwe "< TERUG"-knop (rechtsboven, naast de klok, zelfde vaste-positie-aanpak als elders in de app om een teken/hittest-mismatch te vermijden) brengt terug naar het bureaublad. (4) **SET/UPD-knoppen**: de oude "OPEN"-knop bij elke geïnstalleerde-app-rij in APPSTORE is vervangen door twee kleinere knoppen — "SET" (opent dezelfde app-instellingen-overlay uit taak 263, nu ook met een knop bereikbaar i.p.v. uitsluitend lang indrukken) en "UPD" (alleen gekleurd/actief als er daadwerkelijk een nieuwere versie in de al-geladen winkel-lijst staat — nieuwe `_apps_update_beschikbaar()`, vergelijkt via `strcmp` tegen `winkel[]`; tikken opent de bestaande installeer/bijwerken-popup voor die app). Openen van een app zelf gaat voortaan primair via het bureaublad-icoon. **Niet aangepakt (bewust, buiten scope)**: bij het uitschrijven van de nieuwe SET/UPD-hitboxen viel op dat de bestaande TOGGLE/X-knoppen in de liggende (S3-)APPSTORE-rij getekend worden op `APPS_PNL_W`-relatieve posities maar hittest worden op `rw`-relatieve posities (`rw = APPS_PNL_W - 1 - UI_SB_W`, een verschil van 35px) — een mogelijk al langer bestaande mismatch, losstaand van dit werk; de nieuwe SET/UPD-knoppen volgen bewust dezelfde (bestaande) `rw`-conventie als hun buren voor consistentie, maar dit verdient een aparte check op echte hardware. Lokaal gecompileerd en groen op S3. Pico kon dit keer niet lokaal gecompileerd worden (herhaaldelijk afgebroken wegens geheugendruk in deze gedeelde ontwikkelomgeving — meerdere sessies actief tegelijk, geen codeprobleem) — CI dekt dit bij de push. Nog niet door Brendan gezien. Beta `0.2.260916.5` |
| 268 | Sessie 44 | **Fotolijst: doorschijnende besturingsbalk + de ECHTE oorzaak van het sluit-zichzelf-probleem (taak 265/266 loste het niet volledig op)**: Brendan meldde na taak 266 twee dingen: de knoppenbalk oogde nog steeds als een hard zwart vlak (bedoeling was doorschijnend), en de app sloot zichzelf nog steeds direct na het verschijnen van de knoppen. **Echte oorzaak**: `bkos.draw()` in de fotolijst-app riep bij ELKE tik (ook alleen om de knoppenbalk te tonen) `bkos.foto.tekenen()` opnieuw aan — een trage JPEG-decode, synchroon binnen dezelfde `_gui_taak()`-iteratie als de touch-dispatch. Dat is exact dezelfde lang-druk-klok-race als taak 265/266, alleen tot dusver alleen gefixt voor het LADEN van een app, niet voor een gewone tik erna. Twee fixes: **(a) app-niveau** — de knoppenbalk/instellingen tonen of aanpassen tekent nu alleen de knoppen/het paneel zelf (`overlay_knoppen_teken`/`instellingen_teken`), nooit meer de hele foto; alleen instellingen sluiten of de balk verbergen (tik ernaast) doet nog een volledige `bkos.draw()` (nodig om de knoppen weer weg te vegen — geen "gum"-primitief). **(b) platform-niveau (defense in depth)**: `touch_start_ms`/`lang_druk_verwerkt` resetten nu ook direct ná de normale (niet-laad-gerelateerde) `lua_app_run(...,aanraking=true)`-aanroep voor een fullscreen-app, in beide dispatchpaden — dekt elke toekomstige fullscreen-app die op een tik traag reageert, niet alleen fotolijst. **Doorschijnendheid**: nieuwe sandboxed `bkos.foto.pixel(x,y)` (lua_runtime.cpp) geeft de kleur van de AL getekende foto terug (geen herdecodering — hergebruikt `haven_achtergrond_pixel_klem()`, dezelfde aanpak als de HAVEN-tegels) zodat de app zelf een kleur kan mengen; `main.lua` mengt nu elke knop-/paneelachtergrond met zwart op basis van de onderliggende fotokleur (Lua 5.4 bitwise operatoren) i.p.v. een effen vlak. **Icoon** (Brendans 4e, optionele verzoek uit taak 264: "als bekend is hoe dat werkt, voeg toe"): nieuw `I_FOTO`-icoon (bergje+zon, zelfde motief als het bestaande "foto komt eraan"-laadicoon) + `"icoon": "foto"` in fotolijst's manifest — gebruikmakend van het app-icoonsysteem dat een PARALLELLE sessie (taak 267, zelfde moment) toevallig net had gebouwd. **Concurrency**: deze sessie en de taak-267-sessie draaiden gelijktijdig in dezelfde gedeelde checkout; via `ListAgents`/`SendMessage` gecoördineerd (peer bevestigde welke bestanden ze actief bewerkte: screen_main.h/.ino, screen_apps.ino, de 6 overige app-manifests, index.json) — mijn commit liet die bestanden bewust ongemoeid (`git stash push` op precies die bestanden, rebase+push, direct `git stash pop`, geverifieerd byte-voor-byte intact) zodat beide sessies' werk elkaar niet overschreef. `sync_index.yml` kreeg ook een `"icoon"`-veld in de whitelist (nodig voor de peer-sessie's rollout). Lua-logica lokaal geverifieerd met een gemockte `bkos`-API (`lupa`/Lua 5.5 via pip): alle knop/instellingen-paden bevestigd GEEN `bkos.foto.tekenen()` aan te roepen behalve waar bedoeld, sluiten/vorige/volgende/auto-interval correct bekabeld. Geen aparte C++-compile gedraaid (acute gedeelde geheugendruk tijdens de peer's gelijktijdige build) — de C++-wijzigingen zijn kleine, mechanische toevoegingen naar een al bewezen patroon; CI dekt dit bij de push. `fotolijst v1.3.0`, firmware beta `0.2.260916.6`. Nog niet opnieuw op hardware bevestigd. |
| 269 | Sessie 45 | **5 appstore-apps installeerden niet — hotspot-AP_STA TLS-verstoring, zelfde bekende oorzaak als OTA-fix uit taak 243**: Brendan meldde dat Voorbeeld Klok, Schaakspel, Damspel, Teken App en Zeeslag niet installeerden (voortgangsscherm eindigde in een foutmelding). Systematisch uitgesloten via directe checks tegen de live raw.githubusercontent-URL's: `index.json` is geldig en actueel, alle `main.lua`-bestanden downloaden correct (HTTP 200, juiste bytegrootte) en zijn syntactisch geldig Lua (lokaal gecontroleerd met een echte parser via `lupa`/pip) — dus geen server- of scriptprobleem. **Root cause**: `app_manager.cpp`'s eigen HTTPS-aanroepen (`app_winkel_laden()` voor de winkelindex, `_installeer_taak()` voor elke `main.lua`-download) gebruiken hetzelfde `WiFiClientSecure`+`setInsecure()`-patroon dat bij OTA (taak 243) al eens "GitHub fout -1" gaf door de standaard-AAN hotspot die de ESP32 in gecombineerde AP_STA-modus zet — die fix (`_ota_hotspot_pauzeren`/`_hervatten`) ging destijds alleen naar `ota.ino`, nooit naar de appstore-download-code. Verklaart het schijnbaar willekeurige patroon (welke app faalt hangt af van radio-timing op dat moment, niet van de app zelf — bke/fotolijst installeerden toevallig wel). Fix: nieuwe `_app_hotspot_pauzeren()`/`_hervatten()` (zelfde patroon, `app_manager.cpp`) rond beide HTTPS-aanroepen. **Bijvangst**: tijdens dit onderzoek bleek de icoon-uitrol voor de resterende 5 apps (uit taak 267/268, gebouwd door een parallelle sessie in dezelfde gedeelde checkout) nog steeds ongecommit te staan nadat die sessie was afgesloten — overgenomen, samen met de hotspot-fix lokaal gecompileerd en groen op S3 geverifieerd, en in een eigen, apart toegeschreven commit alsnog gecommit (per ongeluk eerst samengevoegd met de hotspot-fix in één commit door een kale `git commit` die alle staged bestanden meenam i.p.v. alleen de net toegevoegde — gecorrigeerd met `git reset --soft` vóór het pushen, dus nooit in die vorm gepusht). Lokaal gecompileerd en groen op S3 (68% flash). Firmware beta `0.2.260917.1`. Nog niet door Brendan bevestigd op hardware. |
| 270 | Sessie 46 | **Zelfde installatiefout opnieuw gemeld — retry-lus toegevoegd aan de taak-269-fix**: Brendan meldde (in een nieuwe sessie, na een onderbroken vorige) dat Voorbeeld Klok/Schaak/Dam/Teken App/Zeeslag nog steeds niet installeren — exact het symptoom van taak 269, waarvan de hotspot-pauze-fix (`0.2.260917.1`) al op `main` stond maar nog niet door Brendan op hardware bevestigd was. Codereview van die fix liet geen fout zien, maar één verschil met de al langer bestaande, beproefde OTA-fix viel op: `ota_git_check()` heeft een retry-lus (1 herkansing, 300ms wachttijd) specifiek voor verbindingsfouten (code≤0) — `app_winkel_laden()`/`_installeer_taak()` gaven tot nu toe direct op bij de eerste mislukte poging. Zinvol omdat de AP_STA→STA radio-overgang na `wifi_hotspot_stoppen()` niet gegarandeerd al voltooid is bij de allereerste HTTPS-poging. Zelfde retry-patroon nu ook toegepast op beide appstore-HTTPS-aanroepen (`app_manager.cpp`). **Aan Brendan gevraagd**: of dit gemeld gedrag was op `0.2.260917.1` (dus de hotspot-pauze-fix zelf bleek onvoldoende) of op een oudere versie (dan is dit de eerste echte test van die fix). Lokaal gecompileerd en groen op S3 (68% flash, arduino-cli/core 2.0.17). Firmware beta `0.2.260917.2`. Nog niet door Brendan bevestigd op hardware. |
| 271 | Sessie 46 | **"HTTP fout -1" bevestigd + APPSTORE-voortgangsscherm update niet zonder aanraking**: Brendan bevestigde "HTTP fout -1" (dezelfde connectiefout als OTA's bekende "GitHub fout -1") en meldde er los bij dat hij tijdens het installeren zelf moet tikken om de voortgang te zien veranderen — anders lijkt het net of de installatie is vastgelopen. **Root cause tweede bug gevonden**: `screen_appstore_run()` had al langer code om de voortgangs-popup (stap-indicator/balk/statusbericht) ook zonder aanraking bij te werken (`apps_voortgang_actief`-blok, bestond al vóór deze sessie), maar `SCREEN_APPSTORE` ontbrak als enige van de "loopt een achtergrondtaak"-schermen in de periodieke niet-aangeraakt-dispatch in `hardware.ino` — `SCREEN_OTA` heeft exact hetzelfde patroon en stond daar al wel in. Nu aangevuld in beide dispatchpaden (ESP32 GUI-taak + Pico `hw_loop`). Verklaart vermoedelijk ook Brendans indruk dat Voorbeeld Klok "leek te installeren" maar niet in de lijst kwam: zonder de periodieke update bleef het scherm gewoon op de laatste getoonde stand staan tot een toevallige tik, wat een gestopt/mislukt installatieproces kan doen lijken alsof het nog bezig is of net geslaagd. **Openstaand**: het gemelde "HTTP fout -1" was vermoedelijk nog op `0.2.260917.1` (vóór de in dezelfde sessie toegevoegde retry-lus, taak 270) — nog niet bevestigd of de retry op `0.2.260917.2`/`.3` het verschil maakt; als het blijft falen na deze versie is de hotspot-AP_STA-theorie zelf twijfelachtig en moet breder gekeken worden (bv. of het apparaat op dat moment daadwerkelijk internettoegang via STA heeft, los van de hotspot). Lokaal gecompileerd en groen op S3 (68% flash). Firmware beta `0.2.260917.3`. Nog niet door Brendan bevestigd op hardware. |
| 272 | Sessie 46 | **Dam/schaak installeren nu wél (na taak 271's fix), maar dam tekent door achter de navbar, schaak reageert niet op zijn spelerskeuze-menu, geen reactie op SLUITEN na installatie, voorbeeld_klok blijft falen + 2 nieuwe features (ANNULEER-knop, statusbalk-icoon-uitlijning)**: Brendan's tweede testronde. **Dam gefixt**: header zegt "800×480 designresolutie" en het bord (10×10×46px=460px hoog) is hardcoded op die volle hoogte getekend, maar het manifest vroeg geen `volledig_scherm` — de app kreeg dus maar de gesandboxde ~396px content-hoogte en het onderste stuk bord liep door achter/onder de navbar. Simpelste, laagste-risico fix (geen Lua-wijziging): `volledig_scherm:true`+`toon_header:false` in zowel manifest.json als index.json (installatie leest alleen dat laatste, taak 264) — zelfde patroon als fotolijst. Exit voortaan alleen via lang-indrukken (fullscreen-app-mechanisme), niet meer via navbar. dam v1.1.0. **Voorbeeld_klok layout bijgevangen**: lampknopje stond hardcoded op y=340..400 (net over de ~396px-grens) — nu berekend uit `bkos.H` i.p.v. een vaste 480px-aanname; dit is bovendien de documentatie-referentie-app, dus meteen een beter voorbeeld. v1.0.1. **Schaak NIET opgelost**: codereview (touch-coördinatentransformatie in `lua_app_run()`, `_callback("touch",...)`-dispatch, schaak.lua's eigen y=300-344 menu-knoppen — expliciet becommentarieerd door de auteur als "past in 396px") laat geen bug zien; in tegenstelling tot dam heeft schaak zijn hele layout al bewust binnen de sandbox-grens ontworpen. Kon het dus niet reproduceren via code-inspectie — aan Brendan gevraagd of ECHT NIETS gebeurt bij een tik op "2 Spelers"/"vs Computer" (ook geen rode Lua-foutmelding), of dat het scherm daarna nog wel ergens op reageert. **SLUITEN-knop na installatie**: zelfde — code van de knop/hittest (`screen_appstore_run()`'s `apps_voortgang_actief`-blok) is structureel correct bevonden, geen mismatch gevonden ondanks grondige review (coördinaten, navbar-zone-overlap uitgesloten, dubbele-buffering-flush aanwezig). Niet opgelost, wel 2 concrete verbeteringen die de kans op timing-gerelateerde varianten van dit soort klachten verkleinen (zie hieronder) — nog te bevestigen of het onderliggende probleem daarmee ook weg is. **Voorbeeld_klok download blijft falen** ("HTTP fout -1", ook na taak 271's ene retry) — extra WiFi-robuustheid toegevoegd: 250ms settle-delay ná het pauzeren van de hotspot vóór de eerste HTTPS-poging (de AP_STA→STA radio-overgang is niet gegarandeerd al voltooid direct na de aanroep) + retries van 1 naar 2 herkansingen (3 pogingen totaal), voor zowel `app_winkel_laden()` als `_installeer_taak()`. **Nieuwe features op Brendans verzoek**: (1) ANNULEER-knop tijdens een lopende installatie (`app_ins_annuleren`-vlag, `_installeer_taak()` checkt 'm op veilige tussenpunten — WiFi-wachtlus, tussen download-pogingen — en ruimt WiFi-vergrendeling/hotspot-pauze netjes op i.p.v. een externe `vTaskDelete`, wat die opruimstappen zou overslaan). (2) Statusbalk-iconen (wifi/hotspot/alert) uitgelijnd: ze deelden geen verticaal midden (7px/2px/2px uit het midden t.o.v. elkaar, ontdekt via nauwkeurige bounding-box-berekening) en stonden op ad-hoc x-posities (8/36/56) met ongelijke tussenruimte — nieuwe gedeelde constanten in nav_bar.h (`SB_ICON_CY/X0/GAP`, `SB_TITEL_GAP`) lijnen ze nu allemaal uit op hetzelfde verticale midden met consistente 12px tussenruimte, plus een duidelijkere 18px marge vóór de schermtitel (schuift van x=86 naar x=104). Lokaal gecompileerd en groen op S3 (68% flash); WROOM faalt lokaal op een pre-existing, ongerelateerd omgevingsprobleem (`Arduino_ESP32RGBPanel` niet gevonden in `hw_scherm.ino`, een bestand dat deze sessie niet aanraakte). Firmware beta `0.2.260919.1`. Nog niet door Brendan bevestigd op hardware. |
| 273 | Sessie 47 | **Huispaneel/vaarpaneel herbouwd op IO-vinkjes i.p.v. handmatige namen** (plan-mode sessie, uitgebreid vooroverleg + 3 parallelle Explore-agents over IO/paneel/HAVEN/webapp vóór implementatie): Brendan wil dat de webapp HUIS/BOOT niet meer bijna identiek aanvoelen, dat een IO-kanaal per stuk "opnemen op huispaneel"/"opnemen op vaarpaneel" kan zijn (gelijknamige kanalen automatisch gekoppeld/gelijke instelling), dat een knop zonder gekoppelde IO nergens meer getoond wordt, en dat de HAVEN-dashboard-apparaten-kolom vierkante 3×3-tegels wordt (paginering bij >9 bewust uitgesteld, wel alvast ruimte gereserveerd rechts voor een latere "volgende"-knop). **Kernontwerp**: 2 nieuwe heap-gealloceerde vlaggen `io_huispaneel[]`/`io_vaarpaneel[]` (hw_io.h/.ino, zelfde patroon als `io_min_niveau`), `/io_cfg.csv` uitgebreid naar 11 velden (backward-compatible). Nieuw `huispaneel.h/.ino` — letterlijke kopie van het bestaande `paneel.h/.ino` (`HUISPANEEL_KNOP_MAX=30` vs. vaarpaneel's bestaande 9), eigen `/bkos_huispaneel.csv`. Nieuw in `io.ino`: `io_naam_gelijk()` (EXACTE naamgelijkheid, bevestigd met Brendan via AskUserQuestion — bewust strenger dan de bestaande tolerante prefix-match `io_naam_match()`, om te voorkomen dat bv. "USB" en "USB-lader" onbedoeld koppelen), `io_paneel_vinkje_toepassen()` (zet de vlag op het kanaal + alle exact gelijknamige kanalen, houdt `paneel_knop[]`/`huispaneel_knop[]` als compacte ordelijsten bij, weigert een nieuwe vaarpaneel-naam als de lijst al vol is), `io_paneel_migratie_indien_nodig()` (eenmalige, zelf-gatende migratie — als nog geen kanaal `vaarpaneel==true` heeft maar de oude PANEEL-lijst wel gevuld is, backfillt 'm beide nieuwe vlaggen + `huispaneel_knop[]` vanuit die oude lijst, zodat Brendans bestaande 6-9 knoppen na de update gewoon blijven staan). **screen_io_cfg.ino**: nieuwe "OP HUISPANEEL"/"OP VAARPANEEL"-rijen (alleen groot scherm, zelfde bewuste beperking als MINIMAAL NIVEAU/PANEEL/LAMPEN op Pico). **Bijgevangen bug**: MINIMAAL NIVEAU's hit-test (taak 243) had een verdwaalde extra "32 +" in de cy-berekening — de tikzone lag 32px onder de daadwerkelijk getekende knoppen, dus die rij is vermoedelijk nooit bruikbaar geweest; gefixt met dezelfde `cy += 44 + 18`-pitch als de andere secties. **screen_paneel.ino** herbouwd tot één gedeelde implementatie (`_pn_gedeeld_teken/_run(bool is_huis,...)`, hergebruikt door nieuw `screen_huispaneel.h/.ino`/`SCREEN_HUISPANEEL`): niet meer tikken-om-te-typen, maar naam (alleen-lezen) + aantal gekoppelde kanalen + ⬆/⬇ om te herordenen — lidmaatschap zelf gaat voortaan uitsluitend via het IO CFG-vinkje. **screen_main.ino**: vaarpaneel-knoppen zonder (meer) gekoppelde IO worden nu gefilterd (`_paneel_zichtbaar_lijst()`, zelfde patroon als `_apps_desk_lijst()`) i.p.v. inert zichtbaar te blijven — toegepast op zowel het grote scherm als de Pico-variant. **screen_haven.ino**: `_hv_scan()` leest nu `huispaneel_knop[]` i.p.v. het vaarpaneel te lenen (dat was de daadwerkelijke oorzaak van "HUIS en BOOT voelen hetzelfde"); APPARATEN-kolom is nu een vast, niet-scrollend 3×3-raster vierkante tegels (`_hv_app_layout()`/`_hv_app_tegel_rect()`, nieuwe `HV_APP_NAV_RESERVE`-marge rechts voor de toekomstige paginaknop, render begrensd tot 9 ook al kan de lijst groter zijn) — VERLICHTING-kolom (links) ongewijzigd, scroll ontkoppeld (alleen VERLICHTING scrolt nog). Nieuwe `dev_lokaal_huis[]` (naast bestaande `dev_lokaal[]`) voorkomt dat de no-hardware-testfallback van huis- en vaarpaneel door elkaar loopt nu het onafhankelijke lijsten zijn. **bkos_client.ino/webapp_html.h**: nieuw `huispaneel`-WebSocket-bericht (`licht`-veld via de gedeelde `paneel_naam_is_exterieur()`) + `huispaneel_toggle`-commando; webapp HUIS-tab kreeg eigen Buitenverlichting/Apparaten-secties (`.tilegrid`-CSS, vierkante tegels) gevoed door die nieuwe lijst — `#paneelGridHuis` (die tot nu toe gewoon de BOOT/vaarpaneel-data hergebruikte) is vervangen. **screen_config.ino**: "PANEEL-KNOPPEN" → "VAARPANEEL-KNOPPEN" + nieuwe "HUISPANEEL-KNOPPEN"-rij (CONFIG → BOOT); de nav-bar-brede "PANEEL"-thuisknop (taak 218, een ander concept) bewust niet aangeraakt. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). WROOM faalt lokaal op een pre-existing, ongerelateerd omgevingsprobleem (`Arduino_ESP32RGBPanel` niet gevonden in `hw_scherm.ino`) — kon de DRAM-BSS-impact van de nieuwe heap-allocaties daardoor niet apart op WROOM bevestigen, maar volgt exact hetzelfde bewezen patroon als eerdere sessies. Firmware beta `0.2.260922.1`. Nog niet door Brendan getest op hardware. |
| 274 | Sessie 47 | **Huispaneel-tegels groter + gekleurde/dominante symbolen, ook in de webapp**: eerste feedbackronde op taak 273. Brendan vond de vierkante APPARATEN-tegels op het HAVEN-dashboard te klein, wilde ze zeker groter kunnen laten worden zonder het 3×3-raster te blokkeren, de symbolen dominanter/groter met meer typekleur (water blauw, 230V een geel bliksempje), de naam-tekst kleiner zodra er een symbool is (symbool moet het al verklaren) — met uitzondering van knoppen zonder herkend symbool, die de tekst op de huidige grote schaal houden. In de webapp vond hij de nieuwe vierkante HUIS-tegels mooi en wilde daar hetzelfde: symbool + kleine naam eronder, met dezelfde uitzondering voor onherkende namen. **screen_haven.ino**: `HV_APP_NAV_RESERVE` van `UI_SCX(44)` + een extra tussenruimte naar een smalle `UI_SCX(24)`-strook (de breedte was de bottleneck t.o.v. de royalere hoogte-ruimte, dus tegels groeiden merkbaar zonder de gereserveerde ruimte voor de latere paginaknop te laten vervallen). Nieuwe `_hv_app_icoon_teken()` — geschaalde (`constrain(min(w,h)/32, 1.2, 4.5)`) versie van USB/230V/TV/WATER/DEKLICHT met dezelfde relatieve vormen als het bestaande, vast-16px `teken_icoon()` (die blijft zelf ongemoeid voor zijn overige kleinformaat-aanroepers elders in de app — nav bar, vaarmodus-knoppen, apps-bureaublad). Nieuwe `_hv_app_icoon_kleur()` geeft WATER een blauwe en 230V een gele icoonkleur, losgekoppeld van de aan/uit-status (die blijft afleesbaar aan de tegelrand/accentbalk); overige symbolen volgen nog gewoon de aan/uit-kleur tot er meer concrete voorbeelden komen. Naam-tekst nu size 1 zodra er een symbool is, size 2 (ongewijzigd) zonder herkend symbool. **webapp_html.h**: nieuwe `tileIcoon()` (zelfde naam/licht-herkenningsvolgorde als `paneel_icoon()` op de boordcomputer) kiest een emoji-symbool voor de HUIS-tab-tegels (Buitenverlichting/Apparaten) — 🔌 USB, ⚡ 230V, 📺 TV, 🚰 water, 🔦 buitenverlichting — met de naam klein eronder (`.ticon`/`.tlabel`); zonder herkend symbool toont de naam op leesbare grootte (`.tlabelGroot`). Alleen de HUIS-tab: Brendans wens gold expliciet daarvoor, BOOT/vaarpaneel blijft ongewijzigd. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260922.2`. Nog niet door Brendan bevestigd op hardware — exacte tegelgrootte/icoonschaal blijft een eerste inschatting zonder hardware, waarschijnlijk nog een correctieronde nodig. |
| 275 | Sessie 47 | **Huispaneel-symbolen mooier getekend + tegels nóg iets groter, webapp-USB-icoon vervangen**: derde feedbackronde. Grootte van de boordcomputer-symbolen was nu goed, maar de vormen zelf vond Brendan "saaie lijntekeningen" — vroeg om een bliksemschicht die van dikte verandert, een echt USB-logo, een mooiere druppel en een TV met meer "body"; verder de tegels nog een fractie groter. In de webapp bevielen alle iconen behalve USB (de 🔌-stekker-emoji). **screen_haven.ino** (`_hv_app_icoon_teken()`): USB nu het echte drietand-logo (cirkel+stam+vierkantje/driehoekje via `drawCircle`/`drawRect`/`fillTriangle`) i.p.v. de generieke stekker-met-pinnen; 230V nu 2 gevulde driehoeken die een knik delen (breed aan de uiteinden, smal bij de knik) voor daadwerkelijk wisselende dikte i.p.v. steeds-even-dikke lijnen; WATER nu een gevulde druppel (driehoekige punt vloeit over in een gevulde bol) i.p.v. een omtrek-cirkel met 2 losse lijnen (oogde als een ijsje); TV krijgt een dubbele omtrek (dikkere rand) + een stevige gevulde voet i.p.v. dunne lijntjes. `HV_APP_NAV_RESERVE` verder van `UI_SCX(24)` naar `UI_SCX(8)` (nog steeds ruimte voor een toekomstig paginaknopje, tegels zelf iets groter). **webapp_html.h**: nieuwe `USB_SVG` — kleine inline SVG van hetzelfde drietand-logo (geen bruikbare Unicode-emoji hiervoor bestaat) i.p.v. de stekker-emoji, vast lichtgrijs zodat 'm net als de overige emoji-iconen zijn eigen kleur houdt los van de knop-status; overige webapp-iconen ongewijzigd. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260922.3`. Nog niet door Brendan gezien — exacte vormgeving blijft een eerste poging zonder hardware. |
| 276 | Sessie 47 | **USB-icoon naar het officiële drietand-logo (ball-onderin + 3 takken), TV herkenbaarder gemaakt**: vierde feedbackronde. Brendans eerdere USB-tekening (cirkel bovenaan, 2 takken) was niet de echte USB-tridentlogo die hij bedoelde — hij beschreef 'm precies: een balletje onderin, een stam die naar boven vertakt in 3 lijnen, elk met een eigen symbool aan de top. Voor TV vroeg hij om een herkenbaardere vorm: een scherpe buitenkast met een licht afgeronde binnenkant als scherm, een grotere voet, en klassieke "konijnenoren"-antenne (ook al heeft een tv dat nu niet meer — puur voor herkenbaarheid). **screen_haven.ino** (`_hv_app_icoon_teken()`): USB herbouwd — `fillCircle` (balletje onderin) → stam omhoog naar een vertakpunt → 3 takken: links naar een vierkant (`drawRect`), midden (hoogste) naar een cirkel (`drawCircle`), rechts naar een gevulde driehoek (`fillTriangle`). TV herbouwd — 2 diagonale antennelijnen boven de kast, scherpe buitenkast (`drawRect`) met een ingezette, licht afgeronde binnenkant (`drawRoundRect`, apart van de buitenkast i.p.v. een dubbele-dikte-omtrek-truc), en een grotere gevulde voet. **webapp_html.h**: `USB_SVG` bijgewerkt naar dezelfde ball-onderin/3-takken-vorm, consistent met de boordcomputer (TV in de webapp blijft de 📺-emoji, daar kwam geen klacht over). Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260923.1`. Nog niet door Brendan gezien. |
| 277 | Sessie 47 | **USB-vorm rechtgetrokken (symmetrisch + alles gevuld), TV-voet als losse hals+voet-lijn, 3D-glans/schaduw-accenten op water en bliksem**: vijfde feedbackronde. USB was nog niet goed: taklengtes scheef en een inconsistente mix van omlijnde/gevulde eindsymbolen — Brendan wil dat ALLE vormen óf gekleurd óf omlijnd zijn, nooit gemengd. Voor TV bedoelde hij met "binnenkant gekromd, buitenkant niet" één enkele lijn (niet 2 geneste rects), met minstens afgeronde hoeken als praktisch alternatief voor een echt asymmetrische rand; verder een dunnere hals tussen kast en voet, met de voet zelf als een losse, bredere lijn (niet één dikke balk). Voor water en bliksem vroeg hij om lichte/donkere kleuraccenten voor een subtiel 3D-effect (wit/lichtblauw+donkerblauw voor water, geel-variant voor de bliksem). **screen_haven.ino** (`_hv_app_icoon_teken()`): USB — symmetrische taklengte voor alle 3 takken, vierkant/cirkel nu ook `fillRect`/`fillCircle` i.p.v. omlijnd (driehoek was al gevuld). TV — teruggebracht naar één `drawRoundRect`-omtrek (overal afgeronde hoeken, de gevraagde asymmetrische rand is met een 1px-lijn niet zinvol te bouwen), `drawFastVLine` voor een dunne hals + losse `drawFastHLine` als voet. WATER — kleine `fillCircle`-accenten: lichtblauw linksboven (glans), donkerblauw rechtsonder (schaduw), boven op de bestaande gevulde druppel. 230V — een lichtgele lijn langs de voorkant en een donkerdere langs de achterkant van de bliksemschicht, zelfde idee met geel. **webapp_html.h**: `USB_SVG` meegetrokken naar dezelfde symmetrische/alles-gevuld vorm. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260923.2`. Nog niet door Brendan gezien. |
| 278 | Sessie 47 | **USB-takken diagonaal-dan-recht-omhoog, TV als dubbele scherp/afgeronde rand-laag + dikkere voet, maanvormige waterglans**: zesde feedbackronde op de huispaneel-iconen. USB nog niet goed: de takvorm moest eerst diagonaal vanaf de stam, dan recht omhoog naar het symbool (niet één rechte diagonale lijn). TV: Brendan bedoelde met "binnenkant gekromd" letterlijk 2 dikke (~5pt) lijnen exact over elkaar — één scherp, één afgerond — en een voet als één dikkere (~8pt) hals recht omlaag met een aparte horizontale voetplaat (~5pt dik), niet de dunne-hals-plus-lijn-voet van de vorige ronde. Waterdruppel: de bestaande licht/donker-accenten (positie al goed) moesten een maanvorm krijgen i.p.v. gewone rondjes. Bliksem: "nog niet echt mooi" zonder verdere specificatie. **screen_haven.ino** (`_hv_app_icoon_teken()`): USB — elke buitenste tak nu een `drawLine` (diagonaal) gevolgd door een `drawFastVLine` (recht omhoog) naar het symbool; middentak blijft recht omhoog. 230V — bliksemschicht nu opgebouwd uit 4 driehoeken (2 per segment, elk met eigen breedte) voor een strakkere "Z" dan de vorige 2-driehoekenversie. TV — 2 concentrische dikke omtrekken op precies dezelfde plek (`drawRect`-laag + `drawRoundRect`-laag, elk via een lus voor de dikte), hals nu een `fillRect`-balk (dikker dan voorheen) met een losse `fillRect`-voetplaat eronder. WATER — de licht/donker `fillCircle`-accenten krijgen een tweede, iets verschoven cirkel in de basiskleur "gehapt" uit de rand, wat een maanvorm oplevert. **webapp_html.h**: `USB_SVG` meegetrokken naar dezelfde diagonaal-dan-omhoog-tak-vorm. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260923.3`. Nog niet door Brendan gezien — de TV-dubbele-rand-laag in het bijzonder is een beredeneerde interpretatie van een lastig in tekst te specificeren vorm, mogelijk nog niet raak. |
| 279 | Sessie 47 | **Bliksem herbouwd op een echt zigzag-silhouet (na WebSearch-onderzoek), USB-verhoudingen ruimer, TV pootje/voetplaat/antenne aangepast, druppel-sikkel-richting rechtgetrokken**: zevende feedbackronde. Bliksem was "er nog niet" zonder verdere specificatie — voor deze ronde eerst gericht research gedaan (`WebSearch`) i.p.v. nog een keer blind gokken: bevestigde de USB-drietand-structuur (balletje onderaan, 3 takken naar cirkel/driehoek/vierkant — de opzet klopte al, de uitvoering nog niet) en vond een beproefd, eenvoudig bliksem-icoon-pad (Bootstrap Icons "lightning": platte bovenkant, 2 horizontale kepen halverwege elke kant, spitse punt onderaan) als concrete referentie. Verder: het maantje bij de druppel moest "meekrommen met de zijkant van de druppel" — stond nu de verkeerde kant op. USB-hoeken/verhoudingen "moeten echt anders". TV (zelf al "heel mooi" bevonden): pootje ongeveer de helft korter, voetplaat bijna net zo lang als de TV (~10px smaller, niet de veel kortere balk van ervoor), antenne-sprieten dikker (2-3px). **screen_haven.ino** (`_hv_app_icoon_teken()`): 230V volledig herbouwd — 5 driehoeken (fan vanuit de bovenste keep) voor een echte platte-top/2-kepen/spitse-punt-zigzag i.p.v. de vorige simpele diamant. USB — takken met een ruimere diagonaal + langere verticale oploop, middentak duidelijk het langst. TV — hals gehalveerd, voetplaat-breedte nu `kastbreedte - 10px` (bewust een vaste pixel-aftrek, schaalt niet mee met de icoongrootte, zodat de voet op elke tegelgrootte "bijna even lang" blijft), antenne 3x getekend (±1px) voor meer dikte. WATER — de hap-richting uit de accentcirkels omgedraaid: nu náár het druppelcentrum toe (was ervandaan), zodat de zichtbare sikkel met de buitenrand van de druppel meekromt. **webapp_html.h**: `USB_SVG` meegetrokken naar de ruimere verhoudingen. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260923.4`. Nog niet door Brendan gezien. |
| 280 | Sessie 47 | **Bliksem-zigzag opnieuw herleid uit de referentiecoördinaten (was per ongeluk een ruit), USB compacter, TV-voet trapezium + antenne-marge, groter druppel-maantje**: achtste feedbackronde. Vorige bliksempoging bleek "meer een ruit, niet zoals bedoeld" — bij het "netjes afronden" naar eigen ronde getallen was per ongeluk de linkerflank van het silhouet bijna recht komen te staan (in plaats van een echte tegengestelde zigzag), wat het diamant-effect verklaart; deze keer de 7 punten rechtstreeks herleid uit de eerder gevonden Bootstrap Icons-referentie (herschaald naar dit icoon-formaat) zonder ze verder te "vereenvoudigen". USB: structuur al bevestigd correct, maar "nog echt te hoog" — verder zonder concrete maat. TV (voorheen al goedgekeurd): poot een fractie hoger, en onderaan breder dan bovenaan (letterlijke pixelmaten: 9px onder, 6px boven) i.p.v. een rechte balk — dus een trapezium; antenne-sprieten mochten niet strak tegen de bovenkant van de tegel zitten. Druppel-maantje: positie/richting al goed bevonden, maar Brendan wil 'm groter en beter met de druppelrand meekrommend — erkende zelf dat dit lastig is met een simpele 2-cirkel-hap-techniek. **screen_haven.ino** (`_hv_app_icoon_teken()`): 230V — 7 punten (p1..p7) rechtstreeks overgenomen uit de Bootstrap-referentie (platte top, keep, sprong, lange diagonaal naar de punt, keep, sprong terug), fan-triangulatie vanaf p3. USB — kleinere o-waarden (o7/o6 i.p.v. o9/o8) voor een compactere hoogte, verhoudingen ongewijzigd. TV — voet nu 2 `fillTriangle`s die samen een trapezium vormen (vaste 6/9px breedtes, niet schalend met de icoongrootte), antennetip van o13 naar o12 voor wat marge tot de tegelrand. WATER — de accentcirkels zijn nu bijna zo groot als de druppel zelf (was een klein rondje) en dicht tegen de rand gepositioneerd, met de hap net iets dichter naar het centrum — geeft een merkbaar bredere sikkel die een groter deel van de rand volgt. **webapp_html.h**: `USB_SVG` meegetrokken naar de compactere hoogte. Lokaal gecompileerd en groen op S3 (68% flash, 43% RAM). Firmware beta `0.2.260923.5`. Nog niet door Brendan gezien. |

---

## App Systeem (Sessie 13)

### Architectuur

```
SPIFFS/
  /apps/<id>/
    manifest.json    ← app-metadata (naam, versie, vervangt, api_versie)
    main.lua         ← Lua 5.4 script
  /bkos_data.json    ← gestructureerde data-opslag (key-value + tijdstempel)
```

### Data-opslag sleutels (genaamde conventies)
| Sleutel | Type | Beschrijving |
|---|---|---|
| `meteo.temp` | float | Actuele temperatuur |
| `meteo.wind_kn` | float | Windsnelheid in knopen |
| `meteo.code` | int | Open-Meteo weather code |
| `getij.station` | string | Naam van getij-station |
| `sys.tijd` | string | Huidige tijd HH:MM (NTP) |
| `sys.datum` | string | Huidige datum |

### Lua BKOS API
```lua
-- Scherm (gecoördineerd in app-ontwerpruimte, automatisch geschaald)
bkos.W, bkos.H                   -- ontwerp-dimensies
bkos.vul(x, y, b, h, kleur)
bkos.lijn(x1, y1, x2, y2, kleur)
bkos.tekst(x, y, str, size, kleur)
bkos.cirkel(cx, cy, r, kleur, gevuld)
bkos.rgb(r, g, b)                 -- RGB565 kleur maken
bkos.kleur.bg/tekst/cyaan/groen/amber/rood  -- palette kleuren

-- IO (per nummer of naam)
bkos.io.lees(nr)  bkos.io.lees_naam(naam)
bkos.io.zet(nr, staat)  bkos.io.zet_naam(naam, staat)
bkos.io.wissel(nr)  bkos.io.wissel_naam(naam)
bkos.IO_AAN, bkos.IO_UIT

-- Data store
bkos.data.lees(k)  bkos.data.lees_f(k, std)
bkos.data.schrijf(k, v)  bkos.data.schrijf_f(k, v)
bkos.data.leeftijd(k)    -- seconden geleden

-- Systeem
bkos.sys.versie()  bkos.sys.millis()  bkos.sys.log(str)

-- App callbacks (ingesteld door het script)
bkos.teken = function() ... end
bkos.aanraking = function(x, y) ... end
bkos.update = function() ... end
```

### App manifest formaat
```json
{
  "id": "mijn_app",
  "naam": "Mijn App",
  "versie": "1.0.0",
  "auteur": "naam",
  "beschrijving": "...",
  "scherm_b": 800,
  "scherm_h": 480,
  "schaal": "geen",
  "vervangt": -1,
  "api_versie": 1,
  "actief": true,
  "volledig_scherm": false,
  "toon_header": true
}
```
- `vervangt` is een SCREEN_* constante (0=PANEEL, 2=METEO, 5=INFO, enz.) of -1 voor geen override
  (dan is de app alleen standalone te openen vanuit het APPS-scherm/de navigatiebalk).
- `schaal`: `"geen"` (1:1 pixels, standaard) / `"evenredig"` (aspect ratio behouden, gecentreerd) /
  `"onevenredig"` (elke as apart uitgerekt naar het beschikbare gebied).
- `volledig_scherm` (bool, standaard false): de app vraagt het VOLLEDIGE scherm (0..TFT_H) i.p.v.
  alleen het content-gebied — geen navigatiebalk, en de universele "tik in de navbalk-zone
  navigeert weg"-snelkoppeling is dan uitgeschakeld. Legitiem door de app zelf te zetten (staat in
  zijn eigen manifest.json). Enige uitgang: lang indrukken op het scherm.
- `toon_header` (bool, standaard true, alleen relevant als `volledig_scherm`): houdt de koptekst
  (app-naam bovenin) toch zichtbaar.
- **"Vergrendeld openhouden"** is bewust GEEN manifest-veld — dat kan een app dus nooit zelf
  aanzetten. Het leeft in een apart bestand (`/apps/<id>/bkos_lock.txt`, zie `app_vergrendeld()`/
  `app_zet_vergrendeld()` in `app_manager.cpp`) dat alleen de gebruiker zelf zet, via lang
  indrukken op de app-rij in het APPS-scherm (instellingen-overlay). Staat dit aan, dan is de
  boordcomputer-pincode vereist om de app af te sluiten (long-press-uitgang, zie `hardware.ino`).
- **Opstart-app**: `boot_app_id` (app_state.h, ingesteld via diezelfde instellingen-overlay) laat
  het apparaat direct in een gekozen app opstarten i.p.v. het gewone paneel.
- Deze drie mogelijkheden samen (volledig scherm + vergrendeld + opstart-app) zijn de basis om
  BKOS als platform voor heel andere kiosk-achtige software te gebruiken (bv. een fotolijstje of
  een escape-room-app die ook `bkos.io.*` gebruikt) — zie Taakoverzicht taak 263 voor de volledige
  toelichting/afwegingen.

### App store
- Index URL: `https://raw.githubusercontent.com/brennyc86/BKOS-NUI/main/appstore/index.json`
- Apps: `https://raw.githubusercontent.com/brennyc86/BKOS-NUI/main/appstore/apps/<id>/main.lua`
- Lokale map: `appstore/` in de repo-root

### Lua library (LuaBKOS)
De LuaBKOS library staat in `BKOS_NUI/libraries/LuaBKOS/`. Arduino IDE detecteert dit automatisch als sketch-local library. arduino-cli (CI) vereist de `--library BKOS_NUI/libraries/LuaBKOS` flag — deze staat in alle `arduino-cli compile` commando's in `build.yml`. Zonder deze flag geeft `__has_include("lua.h")` false terug en is `LUA_BESCHIKBAAR = 0`.

## Conventies

- **Taal in code**: Nederlands (variabelen, functies, commentaar)
- **Naamgeving**: `screen_X_teken()` / `screen_X_run()` voor schermen; `hw_` prefix voor hardware drivers; `io_` voor IO logica
- **Geen Serial.print** in productie tenzij achter `#ifdef DEBUG`
- **Versienummer formaat**: `MAJOR.MINOR.YYMMDD.I` (bv. `0.1.260528.2` = 28 mei 2026, iteratie 2)
- Compileer altijd met 8MB partitie schema, ook op 16MB hardware
- Push nooit zonder bijgewerkte `versie.txt` en geëxporteerde `firmware.bin`

---

## Kleurenpaletten (ui_colors.h)

Thema-afhankelijke kleuren zijn runtime `uint16_t` variabelen (niet meer #defines):
`C_BG`, `C_SURFACE`, `C_SURFACE2`, `C_SURFACE3`, `C_STATUSBAR`, `C_NAVBAR`, `C_TEXT`, `C_TEXT_DIM`, `C_TEXT_DARK`, `C_DARK_GRAY`, `C_CYAN`, `C_NAV_ACTIVE`, `C_NAV_NORMAL`

7 paletten gedefinieerd in `ui_colors.ino` (MARINE=0, ROOD=1, GOUD=2, BLAUW=3, GROEN=4, WIT=5, NACHT=6).
`palette_toepassen(schema)` in `hw_setup()` aanroepen na `state_load()`.

Vaste kleuren (ongewijzigd, #defines): `C_GREEN`, `C_RED_BRIGHT`, `C_AMBER`, `C_BLUE`, `C_HAVEN`, `C_ZEILEN`, `C_MOTOR`, `C_ANKER`, `C_LIGHT_*`

## GitHub Actions — FQBN verificatie

De workflow `.github/workflows/build.yml` gebruikt nu:
```
esp32:esp32:esp32s3:CDCOnBoot=cdc,PartitionScheme=default_8MB,FlashSize=16M,FlashMode=qio,FlashFreq=80
```

**Verificatie stap**: Compileer eenmalig in Arduino IDE met verbose output (Bestand → Voorkeuren → "Uitgebreide uitvoer weergeven tijdens compilatie"). Zoek in de uitvoer naar `--fqbn` en vergelijk met bovenstaande. Als het afwijkt, pas de workflow aan.

Als de GitHub Actions build succesvol is, verschijnt een nieuw commit met bijgewerkte `firmware.bin`. De OTA op het apparaat pikt dit automatisch op bij de volgende 5-minutencheck.

## Toekomstige uitbreidingen (roadmap)

- ESPnow sub-controllers die opdrachten sturen naar hoofdcomputer
- Telefoon-app (React Native of Flutter)
- Webapp geserveerd vanaf ESP32: publieke pagina voor boot-eigenaar berichten, ingelogde pagina voor volledige bediening
- Multi-gebruiker sessie management op de webapp
